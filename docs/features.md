# Возможности OneBase

<!--
  Единый каталог возможностей платформы — источник правды для документации
  и для раздела «Тестируем» на сайте.

  Каждая возможность = секция уровня "##" с метаданными в HTML-комментариях:
    status: testing | stable | needs-description
    since:  build-270   (или vX.Y.Z) — необязательно
    date:   2026-06-18  (дата готовности к тестированию, ISO)
    issue:  75          (необязательно: номер issue/PR для обсуждения)

  Тело секции — короткое «что это», затем по необходимости:
    **Как настроить.** где включается/настраивается (пункт меню, YAML, CLI-флаг).
    **Как попробовать.** конкретные шаги.

  Сайт показывает возможности со статусом `testing`. Когда фича стабилизировалась —
  поменяйте статус на `stable`: текст остаётся и переходит в документацию.
  `needs-description` — заготовка, на сайт не попадает, пока не дописана.

  ВАЖНО: тематические подписи ниже — это HTML-комментарии (`<!-- ═══ … ═══ -->`),
  а НЕ заголовки. Каждая возможность обязана оставаться заголовком уровня "##",
  иначе парсер сайта её не увидит. У каждой "##"-секции обязателен `status`.

  ПОДСКАЗКА: `testing` оставляйте только у того, что реально просите потестировать;
  давно работающее держите в `stable`, чтобы не висело в «Тестируем».
-->

<!-- ═══════════════════════════ ИИ ═══════════════════════════ -->

## ИИ-помощник в конфигураторе
<!-- status: testing -->
<!-- since: build-273 -->
<!-- date: 2026-06-16 -->
<!-- issue: 75 -->

Конфигуратор умеет использовать большие языковые модели прямо в процессе разработки:
**генерация и доработка конфигурации** по описанию словами (file-level staging для YAML,
`.os` и управляемых форм, предпросмотр diff, выбор файлов к применению, inline-правка,
авто-`check` перед записью), **объяснение ошибок** проверки конфигурации человеческим
языком и **подсказка запросов** по описанию. Генератор использует инструменты
`проверить_конфигурацию`, `форматировать`, `прогнать_запрос`, `объяснить_impact` и умеет
делать self-correction loop после неудачного `check`; история показывает tool trace.
В базах с конфигурацией в БД успешное применение создаёт снимки до/после, поэтому есть
понятный rollback-путь через историю версий конфигурации. Недоверенный DSL-код, когда он
выполняется, запускается в песочнице с ограничениями.

**Как настроить.** Войдите в **Конфигуратор** под пользователем с ролью **admin** (под
обычным пользователем пункт меню не виден). В шапке: **Меню ▾ → «ИИ-помощник»**. Откроется
форма настроек с заготовкой для пустой базы: провайдер
(`gemini` / `anthropic` / `openai` / `compatible`), модель, профили задач и `api_key`.
Впишите ключ, нажмите **«Проверить»** (делает пробный запрос — проверяет ключ и маршрут), затем
**«Сохранить»** — внизу справа появятся плавающие кнопки 🤖/🏗️, перезапуск не нужен. Ключи
хранятся в служебной таблице базы (`_settings`) и **не попадают в `.obz`-бэкап**. Для
деплоя/демо тот же конфиг задаётся секцией `llm` в `config/app.yaml`, а ключи — через
`${env:…}`. Там же задаются лимиты `ai.daily_token_cap` и `ai.max_tool_rounds`. Подробно —
[`docs/ai-assistant.md`](ai-assistant.md).

**Как попробовать.** После настройки нажмите 🏗️ «Генерация каркаса», опишите вертикальный
срез словами (например «справочник Контрагенты с ИНН, формой, отчётом и проверкой дублей»),
посмотрите diff/tool trace, снимите лишние файлы при необходимости и примените результат.

## ИИ-функции в языке (DSL)
<!-- status: stable -->
<!-- date: 2026-06-09 -->

Обращаться к моделям можно прямо из кода конфигурации: `ЗапросИИ`, `ЗапросИИДжейсон`,
`РаспознатьДокумент` (vision по файлу — png/jpg/webp/gif/pdf), `РаспознатьИзображение`
(vision по данным из памяти). Второй аргумент `Параметры` (`Структура`) задаёт `Задача`
(профиль), `Система` (роль модели), `Формат` (`"json"`), `Температура`. Маршрутизация
«задача → цепочка моделей» с фолбэком — в профилях конфига ИИ.

**Как настроить.** Тот же конфиг, что у ИИ-помощника (меню → ИИ-помощник или секция `llm`
в `config/app.yaml`). Профили задач (`анализ`, `чат`, `документы`, `конфигуратор`)
определяют, какая модель отвечает. Подробно и примеры — [`docs/ai-assistant.md`](ai-assistant.md).

**Как попробовать.** Вызовите `ЗапросИИ("Привет, кто ты?")` из обработки и выведите ответ
через `Сообщить(...)`.

## ИИ-чат в интерфейсе
<!-- status: stable -->
<!-- date: 2026-06-18 -->

Чат с моделью прямо в пользовательском интерфейсе: модель видит структуру конфигурации
(справочники, регистры, отчёты) и отвечает на вопросы по данным. Есть ограничение частоты
запросов и суточный потолок токенов; обращения можно журналировать.

**Как настроить.** Требует настроенного ИИ (см. «ИИ-помощник в конфигураторе»). Запись
истории диалогов — опциональна (флаг `log_history` в конфиге ИИ); просмотр — меню
**«История ИИ»** в конфигураторе.

**Как попробовать.** Откройте ИИ-чат в интерфейсе и спросите что-нибудь по своим данным
(например «сколько документов поступления за месяц»).

## Действия из ИИ-чата: создание документов и переходы
<!-- status: stable -->
<!-- date: 2026-07-17 -->

ИИ-чат теперь не только отвечает, но и **готовит действия**: создаёт черновики
документов и элементов справочников по описанию словами и даёт кнопки перехода к
спискам, записям, отчётам и обработкам. Запись выполняется только после нажатия
кнопки **«Создать»** в карточке подтверждения; документы никогда не проводятся
автоматически. Ссылочные реквизиты ИИ подбирает по наименованию (неоднозначность —
переспросит) или находит запросом; права, ролевой и строковый доступ проверяются как
при ручном вводе, создание пишется в аудит ИИ. Окно чата теперь можно растянуть за
левую кромку — ширина запоминается (двойной клик по кромке — сброс).

**Как настроить.** Ничего дополнительно: работает у пользователей, которым доступны
инструменты данных ИИ-чата (администраторы; в режиме `rbac` — пользователи и роли с
флагом «Доступ ИИ-чата к данным»).

**Как попробовать.** Напишите в чат «создай заказ для Ромашки: 2 стола и 4 стула» —
проверьте карточку подтверждения, нажмите «Создать» и откройте документ по ссылке.
Затем попросите «открой список заказов».

<!-- ═══════════════════════ Отчёты и печать ═══════════════════════ -->

## Опциональные знаки после запятой в формате показателя
<!-- status: testing -->
<!-- date: 2026-07-28 -->
<!-- issue: 460 -->

В формате числа отчёта (`composition`, `measures[].format`) символ `#` после
запятой стал **опциональным разрядом**, как в 1С и Excel: `0` печатается всегда
(добивается нулём), `#` — только если в этом разряде есть значащая цифра. Так
`#,##0.00####` означает «минимум 2 знака, максимум 6»: `409` → `409,00`, а мелкое
`0,000111` показывается целиком, а не схлопывается в ложный `0,00`. Округление —
до максимального числа знаков. Совместимость: форматы из одних `0`
(`#,##0.00`) работают ровно как раньше.

**Как попробовать.** В показателе отчёта задайте `format: "#,##0.00####"` и
выведите значение с длинной дробной частью (например среднюю цену или курс) —
лишние нули уберутся, а значащие цифры до шестого знака сохранятся.

## Компоновка отчётов (СКД)
<!-- status: stable -->
<!-- date: 2026-06-18 -->

Декларативная «лёгкая СКД» в YAML отчёта: блок `composition` собирает группировки с
итогами, вычисляемые показатели (`sum`/`count`/`avg`/`min`/`max` и выражения, в т.ч.
проценты), сворачиваемые группы, условное оформление, графики (bar/line/pie) и расшифровку
ячейки (drill-down к документам-источникам). Агрегация считается на сервере.

**Как настроить.** Блок `composition` в `reports/<имя>.yaml` (`groupings`, `measures`,
`totals`, `conditional`, `chart`, `detail_link`/`detail_entity`). Можно собирать визуально
в конфигураторе — вкладки конструктора отчёта «Структура / Оформление / График» и кнопка
предпросмотра.

**Как попробовать.** Откройте отчёт с блоком `composition` (примеры в
`examples/trade/reports/`), сверните группу кликом, переключите «Таблица ⇄ График»,
кликните по числу для расшифровки и выгрузите результат в Excel.

## Кросс-таблица (сводный отчёт)
<!-- status: stable -->
<!-- date: 2026-06-19 -->

Сводный режим компоновки: если в блоке `composition` задан список `columns`, отчёт строится
кросс-таблицей — измерения `groupings` уходят в строки, значения `columns` разворачиваются в
колонки, на пересечении выводятся показатели (в т.ч. вычисляемые). Выравнивание, формат и
условное оформление показателей применяются к ячейкам; есть итоговая строка «ВСЕГО» и выгрузка
в Excel. Обычные (несводные) отчёты не меняются — режим включается только при заданных `columns`.

**Как настроить.** Добавьте `columns: [Поле]` в `composition` (рядом с `groupings`). В
конфигураторе — зона «Колонки (кросс-таблица)» во вкладке «Структура» конструктора отчёта.
Для предсказуемого порядка колонок используйте сортируемое значение периода (дата/число).

**Как попробовать.** В отчёте-СКД добавьте измерение в «Колонки» и сформируйте — данные
развернутся в сводную таблицу по столбцам; выгрузите результат в Excel.

## Оформление вывода отчёта (линии сетки, зебра)
<!-- status: stable -->
<!-- date: 2026-06-22 -->

Общий вид таблицы компоновки настраивается отдельно от условного оформления: блок
`appearance` задаёт **линии сетки** (`lines`: `horizontal` — как было, только нижние;
`vertical` — вертикальные; `both` — полная сетка; `none` — без линий) и **зебру** (`zebra`
— чередование фона строк). По умолчанию (без блока) вид прежний — горизонтальные линии,
существующие отчёты не меняются. Работает и для обычных отчётов, и для кросс-таблиц.

**Как настроить.** В `reports/<имя>.yaml` добавьте в `composition` блок `appearance`:

```yaml
composition:
  groupings: [Номенклатура]
  measures: [{ field: Сумма, agg: sum }]
  appearance: { lines: both, zebra: true }
```

В конфигураторе — вкладка **«Оформление»** конструктора отчёта, секция «Оформление вывода».
Кроме того, каждый пользователь может задать линии/зебру **себе** в панели **«Настройка
отчёта»** на форме отчёта — это презентационная настройка, она применяется поверх
конфигурации и хранится персонально.

**Как попробовать.** Откройте отчёт, разверните **«Настройка отчёта»**, выберите
«Линии сетки → и те и те», включите зебру и нажмите **«Применить»** — таблица перерисуется
с вертикальными линиями.

## Отчёт открывается свёрнутым (appearance.collapse_to)
<!-- status: testing -->
<!-- date: 2026-08-05 -->
<!-- issue: 575 -->

Отчёт-«выписка» с несколькими уровнями группировок и `detail: true` открывался простынёй
на сотни строк: первым делом пользователь жал «Свернуть всё». Ключ `collapse_to` задаёт
начальное состояние: **уровни 0..N развёрнуты, глубже — свёрнуто**. `collapse_to: 0` —
видны только группы верхнего уровня со своими итогами. Без ключа поведение прежнее —
отчёт открывается развёрнутым.

Заодно изменилось раскрытие: клик по свёрнутой группе показывает **следующий уровень**, а
не всё поддерево до деталей (иначе первый же клик возвращал ту же простыню). Кнопки
«Развернуть всё» / «Свернуть всё» работают как раньше.

**Как настроить.** В `reports/<имя>.yaml`, в `composition.appearance` (и так же в любом
элементе `variants`):

```yaml
composition:
  groupings: [Участник, Категория, Позиция]
  detail: true
  measures: [{ field: Сумма, agg: sum }]
  appearance:
    collapse_to: 0   # развёрнуты уровни 0..N; глубже — свёрнуто
```

В конфигураторе — вкладка **«Оформление»** конструктора отчёта, поле «Свернуто до уровня»
рядом с линиями сетки и зеброй. Пользователь может задать свой уровень **себе** в панели
**«Настройка отчёта»** на форме отчёта: это презентационная настройка, она хранится
персонально и попадает в сохранённый вариант настроек.

**Как попробовать.** Добавьте `appearance: { collapse_to: 0 }` в отчёт с двумя-тремя
группировками и откройте его: видны только верхние группы с итогами. Клик по группе
раскрывает ровно один уровень.

## Варианты компоновки отчёта
<!-- status: testing -->
<!-- date: 2026-06-19 -->
<!-- issue: 68 -->

Один отчёт может иметь несколько именованных компоновок по одному и тому же запросу
(как «варианты отчёта» в 1С). На форме отчёта появляется выпадающий список «Вариант»:
выбор переключает группировки/показатели/колонки/оформление без правки конфигурации.
Вариант по умолчанию — основной блок `composition`. Выгрузка в Excel учитывает выбранный
вариант.

**Как настроить.** Рядом с `composition` в `reports/<имя>.yaml` добавьте блок `variants`
— список `{ name: "Название", composition: { … } }`. Каждый вариант — полноценная
компоновка (можно с `columns` для кросс-режима). Конструктор отчёта в конфигураторе
сохраняет варианты при правке основной компоновки (не затирает их).

```yaml
composition:          # вариант «Основной»
  groupings: [Номенклатура]
  measures: [{ field: Сумма, agg: sum }]
variants:
  - name: "По складам"
    composition:
      groupings: [Склад, Номенклатура]
      measures: [{ field: Сумма, agg: sum }]
  - name: "Кросс по месяцам"
    composition:
      groupings: [Номенклатура]
      columns: [Месяц]
      measures: [{ field: Сумма, agg: sum }]
```

**Как попробовать.** Добавьте в отчёт блок `variants` с парой компоновок, откройте отчёт
и переключайте «Вариант» в форме — таблица перестраивается под выбранную компоновку.

## Настройка отчёта пользователем
<!-- status: testing -->
<!-- date: 2026-06-19 -->
<!-- issue: 68 -->

Структуру отчёта можно менять прямо на форме — без правки конфигурации. На сформированном
отчёте появляется панель **«Настройка отчёта»**: чекбоксы группировок и показателей по
колонкам результата, отборы строк (поле / оператор / значение: `=`, `≠`, `>`, `≥`, `<`, `≤`,
содержит) и три кнопки. **«Применить»** перестраивает отчёт по выбранным настройкам,
**«Сохранить»** запоминает их персонально для пользователя, **«Стандартные настройки»**
возвращают вид из конфигурации. Сохранённые настройки переживают переоткрытие отчёта и
учитываются при выгрузке в Excel. Настройки пользователя не меняют конфигурацию (YAML) —
хранятся отдельно в базе.

**Как попробовать.** Откройте любой отчёт с компоновкой, разверните **«Настройка отчёта»**,
отметьте группировки/показатели или добавьте отбор и нажмите **«Применить»**. Понравилось —
**«Сохранить»**; вернуть исходный вид — **«Стандартные настройки»**.

## Именованные варианты настроек отчёта
<!-- status: testing -->
<!-- date: 2026-07-03 -->
<!-- issue: 228 -->

Пользовательские настройки СКД-отчёта сохраняются как именованные варианты:
**«Сохранить как»** создаёт вариант со своим именем, селектор **«Вариант
пользователя»** на форме отчёта переключает их, чекбокс **«по умолчанию»** делает
вариант стартовым — он применяется при каждом открытии отчёта. **«Стандартные
настройки»** возвращают вид из конфигурации, ненужный вариант удаляется. Варианты
персональны и хранятся в базе — YAML конфигурации не меняется.

**Как попробовать.** На отчёте с компоновкой измените настройки → **«Сохранить
как»**, задайте имя. Создайте второй вариант, попереключайте их селектором,
отметьте один «по умолчанию» и переоткройте отчёт.

## Значение параметра отчёта по умолчанию
<!-- status: testing -->
<!-- date: 2026-08-29 -->

Параметр отчёта может иметь значение по умолчанию — ключ `default` в
`reports/<имя>.yaml`. Отчёт с необязательной датой раньше открывался пустым:
условие `ГДЕ Срок < &НаДату` при незаполненном параметре сравнивается с NULL и
не выбирает ничего, и вместо просроченных задач пользователь видел пустую
таблицу.

```yaml
params:
  - name: НаДату
    type: date
    default: "{{today}}"
  - name: ТолькоМои
    type: bool
    default: "true"
```

Умолчание понимает ту же грамматику подстановок, что параметры виджетов и
регламентных заданий: `{{today}}`, `{{now}}`, `{{today | minus_days:7}}`.
Обычная строка (`default: ВРаботе`) подставляется как есть.

Правило одно на все точки запуска — форму отчёта, выгрузку в Excel/PDF, REST
`/api/v2/report/<имя>` и перестроение после сохранения настроек или варианта
пользователя: умолчание подставляется, только когда параметр **не задан**.
Заданное значение всегда важнее, и пустое значение тоже считается заданным —
очищенное поле остаётся пустым, снятая галка остаётся снятой.

**Как попробовать.** Добавьте параметру отчёта `default` и откройте отчёт: поле
уже заполнено — видно, с чем поедет отчёт, и это можно изменить до построения.
Очистите поле и нажмите **«Сформировать»** — умолчание не вернётся. Затем в
блоке «Настройка отчёта» нажмите **«Сохранить»**: отчёт перестроится, и поле
останется пустым.

## Импорт печатной формы из PDF
<!-- status: stable -->
<!-- date: 2026-06-17 -->

Загрузите готовый векторный PDF (например реальный УПД, счёт или акт) — платформа
распознаёт сетку, тексты и границы и собирает черновик макета, который дорабатывается в
визуальном редакторе. Кириллица извлекается корректно; для сканов (картинок) выдаётся
честная ошибка.

**Как настроить / попробовать.** В конфигураторе: **«Создать макет из PDF»**, загрузите
файл и укажите номер страницы — получите черновик макета в редакторе.

## Печатная форма из бланка Excel
<!-- status: testing -->
<!-- date: 2026-08-25 -->
<!-- issue: 1124 -->

Бланк рисуется в Excel — там дешевле расставить объединения, ширины колонок,
границы и поля листа, — а платформа собирает из него макет печатной формы.
Переносятся тексты с тегами, объединения, границы по сторонам, шрифты, заливка,
выравнивание, размеры колонок и строк, формат и поля листа, картинки (логотип).
Что не перенесено — формулы (берётся вычисленное значение), условное
форматирование, повороты текста — показывается списком, а не теряется молча.

**Как настроить.** В ячейки бланка пишутся те же теги, что понимает макет:

```
ТОВАРНАЯ НАКЛАДНАЯ № {{Номер}} от {{Дата | date}}
Покупатель: {{Покупатель.Наименование}}

{{@row}}  {{Товары.Номенклатура}}  {{Товары.Количество}}  {{Товары.Цена | number:2}}

Итого: {{Итог.Товары.Сумма | number:2}}
```

Строка с тегами, написанными через имя табличной части, становится
размножаемой: при печати она повторится для каждой строки документа, а
приставка внутри области снимается. Разбиение на области можно задать явно —
именами Excel (Формулы → Диспетчер имён), а «Сквозные строки» листа становятся
шапкой, повторяемой на каждой странице.

**Как попробовать.** В конфигураторе: **«Создать макет из Excel»**, выберите
файл и документ. Или из командной строки:

```bash
onebase printforms import --project examples/trade --file бланк.xlsx --name ТоварнаяНакладная --document РеализацияТоваров
```

Дальше это обычный макет: открывается в редакторе, печатается в HTML и PDF.

## Визуальный редактор макетов печатных форм
<!-- status: testing -->
<!-- since: build-274 -->
<!-- date: 2026-06-16 -->
<!-- issue: 73 -->

Печатные формы собираются визуально — расстановка элементов, ширины колонок и высоты
строк, границы по сторонам ячеек, привязка данных и предпросмотр, без ручной правки YAML.
Серверный PDF использует родные кириллические шрифты; поддерживаются картинки в HTML и PDF.

**Как настроить.** Макеты лежат в `printforms/*.layout.yaml` (+ опциональный `.os`).
Старые печатные формы переносятся командой `onebase printforms migrate --project <dir>`.

**Как попробовать.** Откройте печатную форму в конфигураторе и поработайте с макетом; на
форме документа нажмите «Печать ▾» → HTML или PDF.

## Нестандартный размер страницы и многостраничные печатные формы
<!-- status: testing -->
<!-- since: v0.9.1 -->
<!-- date: 2026-07-13 -->
<!-- issue: 313 -->

Макет печатной формы может задавать точный физический размер PDF в миллиметрах —
например `229x162mm` для конверта, ярлыка, вкладыша или нестандартного бланка.
Длинные табличные части автоматически разбиваются на страницы, а указанная область
шапки повторяется на каждом новом листе. Для этого не требуется генерировать HTML
в реквизите или писать модуль печати.

**Как настроить.** Укажите размер как `page.format: 229x162mm`. Строки табличной
части выводятся через `binding.repeat`, повторяемая на каждой странице область —
через `binding.repeat_header`. Кастомный размер задаётся буквально как ширина ×
высота, поэтому `orientation` к нему не применяется.

**Как попробовать.** Запустите `examples/archive`, выполните обработку
**«Заполнить демоданные для печатных форм»**, откройте дело `Д-000001` и выберите
**«Печать → ВнутренняяОпись → PDF»**. Опись из 60 строк займёт несколько страниц,
и шапка таблицы повторится на каждой. Форма **«ОбложкаДела»** в том же документе
создаёт PDF точного размера 229 × 162 мм.

## Отчёты на DSL
<!-- status: stable -->
<!-- date: 2026-05-15 -->

Отчёт как DSL-модуль (`.rep.os`) с запросом и параметрами (string/date/number/bool/
select/reference) и выводом таблицы — для случаев, когда декларативной компоновки мало.

**Как попробовать.** Откройте отчёт из раздела навигации, задайте параметры и сформируйте.

## Печать в PDF и экспорт в Excel
<!-- status: stable -->
<!-- date: 2026-05-30 -->

Печатные формы выгружаются в PDF, списки и отчёты (включая сгруппированный вид компоновки) —
в Excel (`.xlsx`).

**Как попробовать.** Откройте отчёт или печатную форму и выгрузите результат.

## Выгрузка отчёта в PDF
<!-- status: testing -->
<!-- date: 2026-07-01 -->
<!-- issue: 218 -->

Любой отчёт выгружается в реальный бинарный PDF тем же движком, что и печатные формы
документов, — это серверный артефакт, а не «Сохранить как PDF» из браузера. Данные и
порядок строк совпадают с экраном и Excel-выгрузкой (компоновка, сводная таблица, плоский
результат). В метаданных отчёта можно задать `output_format: pdf`, чтобы кнопка PDF стала
основной (по умолчанию первой идёт Excel).

**Как попробовать.** Сформируйте отчёт и нажмите «PDF ↓» рядом с «Excel ↓» — либо дёрните
`GET /ui/report/<имя>/pdf` напрямую.

## Фоновые выгрузки отчётов
<!-- status: stable -->
<!-- date: 2026-07-07 -->

Выгрузка отчёта в Excel или PDF запускается как фоновое задание: пользователь сразу
попадает на страницу состояния и видит этап `в очереди`, `выполняется`, `готово` или
ошибку. Тяжёлая выгрузка не удерживает исходный HTTP-запрос и ожидает свободный
`export_concurrency`-слот, не создавая лишнюю нагрузку. Готовый файл доступен для
скачивания 30 минут; задания принадлежат запустившему их пользователю.

**Как попробовать.** Сформируйте достаточно большой отчёт и нажмите **«Excel ↓»**
или **«PDF ↓»**. На странице задания дождитесь статуса **«Готово»** и скачайте файл;
при занятом лимите параллельных экспортов новое задание сначала будет показано в
очереди.

## ТабличныйДокумент (печатные формы в DSL)
<!-- status: stable -->
<!-- date: 2026-05-13 -->

1С-подобный объект для программного формирования печатных форм: ячейки, именованные
области (`ПолучитьОбласть`), объединения, стили и параметры — макет строится кодом в `.os`.

**Как попробовать.** Откройте печатную форму с `.os`-обработчиком, использующим
`ТабличныйДокумент`.

<!-- ═══════════════════════ Формы и интерфейс ═══════════════════════ -->

## Индикатор выполнения обработки и обязательные параметры
<!-- status: testing -->
<!-- date: 2026-08-23 -->

Форма обработки показывает, что работа идёт: после нажатия кнопка выключается,
получает крутящийся индикатор и подпись «Выполняется…». Раньше страница не
менялась ничем, пока сервер не ответит, и обработка, которая качает каталог
минуту, выглядела как несработавшее нажатие — человек жал «Выполнить» второй раз
и запускал ту же работу поверх первой. Системную настройку «меньше движения»
индикатор уважает: остаётся, но перестаёт крутиться.

Параметр обработки можно пометить обязательным. Поле получает звёздочку, форма
не отправится пустой, а сервер проверяет то же самое до запуска и называет
незаполненные поля по подписям — атрибут в разметке лишь подсказка, запрос
приходит и мимо браузера. Нужнее всего это файловому параметру: вернуть
выбранный файл в поле после перезагрузки браузер не позволяет, поэтому повторный
запуск уходил на сервер с пустым файлом, и человек получал ошибку прикладного
модуля вместо подсказки формы.

**Как настроить.** Ключ `required` у параметра в YAML обработки:

```yaml
name: ИмпортИзYML
params:
  - name: Файл
    type: file
    label: Файл выгрузки (YML)
    required: true
```

У параметров отчёта такого ключа нет намеренно: `report.Param` его не знает, и
разрешённый ключ молча ничего бы не делал.

**Как попробовать.** Откройте обработку «Импорт каталога из YML-выгрузки» в
примере `examples/cms`, нажмите «Выполнить» без файла — форма остановит отправку
сама. Приложите `examples/cms/demo/catalog.yml` и нажмите ещё раз: кнопка
погаснет и скажет, что работа идёт.

## Значения по умолчанию у реквизитов
<!-- status: testing -->
<!-- date: 2026-08-22 -->
<!-- issue: 1095 -->

Реквизит нового объекта может заполняться сам. Раньше это писалось руками в
каждой форме и добивалось в `ПриЗаписи` каждого документа: в конфигурации на
два десятка документов — около тридцати одинаковых мест, и про них забывали,
из-за чего документ уезжал в базу с пустой организацией.

Дефолт применяется на **всех** путях создания: в форме, в обработке
(`Документы.X.Создать()`, `Справочники.X.Создать()`) и по REST. Уже заданное
значение не перетирается никогда — дефолт это то, с чего человек начинает.

**Как настроить.** Ключ `default:` у реквизита в YAML сущности:

```yaml
fields:
  - name: Дата
    type: date
    default: сейчас                        # или `сегодня` — начало дня
  - name: Организация
    type: reference:Организация
    default: константа.НашаОрганизация      # значение константы
  - name: Ответственный
    type: string
    default: текущийпользователь            # логин вошедшего
  - name: Склад
    type: reference:Склад
    default: единственный                   # если в справочнике ровно один
  - name: СпособУчетаНДС
    type: enum:СпособУчетаНДС
    default: ВТомЧисле                      # литерал: строка, число, Истина/Ложь
```

Список источников закрыт: неизвестный отвергается на `onebase check` вместе с
несовместимым типом, несуществующей константой и значением вне перечисления.
Молча не сработавший дефолт искать в рантайме дороже, чем исправить опечатку
сразу. По той же причине `check` отвергает `default` у реквизита табличной
части: строка ТЧ добавляется уже после создания объекта, и пока этот ключ там
не работает — значения новой строки заполняйте в обработчике формы.

**Про `единственный`.** Подставляется элемент, который не помечен на удаление,
не является группой, активен (если у справочника объявлен `activity`) и виден
текущему пользователю по строковым политикам. Как только в справочнике
появляется второй такой элемент, поле снова остаётся пустым. Поэтому источник
включается **на конкретном реквизите**, а не работает для всех ссылок сразу:
поведение формы, зависящее от количества строк в справочнике, должно быть
осознанным решением конфигуратора.

**Хук `ПриСозданииНового`.** Если умолчание вычисляется (склад по
подразделению пользователя, организация из персональных настроек), объявите в
модуле объекта `src/<имя>.os`:

```
Процедура ПриСозданииНового(Объект)
    Объект.Склад = СкладПоУмолчанию(ТекущийПользователь().Имя);
КонецПроцедуры
```

Порядок такой: сначала декларативные `default`, затем хук (он главнее — видит
контекст), затем ввод на основании или копирование, если создание идёт от
источника. Персональные настройки пользователя платформа не хранит: их состав
и права у каждой конфигурации свои, поэтому регистр сведений с настройками
заводится в конфигурации, а хук — место, где он читается.

**Как попробовать.** В `examples/trade` дефолты объявлены у документа
«Реализация товаров» (`documents/РеализацияТоваров.yaml`): дата, организация из
константы, склад — единственный. Заполните константу «Наша организация»,
заведите один склад и нажмите «Создать».


## Этапы объекта: маршрут, гарантия переходов и «где застряло»
<!-- status: testing -->
<!-- date: 2026-08-12 -->
<!-- issue: 121 -->

Состояние объекта («Черновик → На согласовании → Утверждена») теперь можно объявить
платформе, а не описывать заново в каждом модуле. Объявленный маршрут даёт три вещи,
которых раньше не было: **перескочить этап нельзя** (проверка стоит в слое хранения,
поэтому её не обойти ни формой, ни обработкой, ни REST, ни импортом), **видно, кто и
когда двигал объект** (история переходов пишется сама и не зависит от журнала
регистрации), и **видно, где всё застряло** — сколько объектов стоит на каждом этапе
и сколько висит дольше срока.

**Как настроить.** В YAML сущности, рядом с `fields`, добавьте блок `stages` на
существующий реквизит-перечисление:

```yaml
stages:
  field: Состояние                  # реквизит типа enum:...
  order: [Черновик, НаСогласовании, Утверждена, Отклонена]
  transitions:                      # всё, чего здесь нет, — запрещено
    - { from: Черновик,       to: [НаСогласовании] }
    - { from: НаСогласовании, to: [Утверждена, Отклонена] }
    - { from: Отклонена,      to: [Черновик] }
  deadline_days: { НаСогласовании: 2 }   # опц.: сколько ждём на этапе → просрочка
  enforce: warn                          # warn (умолчание) | strict
```

`enforce: warn` — нарушение пишется в лог, запись проходит; это умолчание, чтобы
включение блока в работающей базе ничего не сломало. `strict` — недопустимый переход
отвергается. Объект с незаполненным этапом считается стоящим в начале маршрута.
Новых объектов метаданных не заводится, откат — удаление блока из YAML.

**Как попробовать.** Возьмите `examples/callcenter` (блок уже объявлен у документа
«Заявка»): откройте заявку — над формой появится схема маршрута с подсвеченным
текущим этапом; переведите её по маршруту и посмотрите «История» — переходы идут
отдельной таблицей, с автором и временем. Сводка по всем маршрутам — пункт меню
**«Этапы — где застряло»** (`/ui/stages`): объекты по этапам, просрочка по
`deadline_days`, отдельной строкой — «Вне маршрута» (состояния, накопленные до
объявления блока: их стоит разобрать до включения `strict`). Чтобы увидеть гейт,
поменяйте `enforce` на `strict` и попробуйте перевести заявку из «Черновик» сразу в
«Утверждена» — запись будет отклонена, в том числе из обработки и по REST.

## Команды и пикеры управляемой формы работают без ручной разметки
<!-- status: testing -->
<!-- date: 2026-08-02 -->
<!-- issue: 522 -->

Управляемая форма теперь сама рисует то, что раньше приходилось размещать руками.
**Команды** из секции `commands:` попадают на форму автоматической командной панелью —
объявили команду, она сразу видна и кликается. Если команду нужно поставить в
конкретное место, по-прежнему используйте элемент `kind: Кнопка`: такая команда в
автопанель не дублируется. **Реквизиты формы** (`attributes` с `save: false`)
ссылочного типа (`CatalogRef.X`, `DocumentRef.X`) получают полноценный выбор из
справочника — выпадающий список плюс кнопка подбора, как у полей объекта.

**Как попробовать.** Объявите в `.form.yaml` команду без единого `kind: Кнопка` —
кнопка появится сама; клик вызовет процедуру из `action` в `.form.os`. Добавьте
реквизит формы с `type: CatalogRef.<Справочник>` и `save: false`, покажите его
элементом `ПолеВвода` с `data_path: <ИмяРеквизита>` — поле станет выбором из
справочника вместо простого ввода.

## Своя подпись кнопки «Провести»
<!-- status: testing -->
<!-- since: build-275 -->
<!-- date: 2026-07-31 -->
<!-- issue: 497 -->

Кнопку проведения документа можно назвать по смыслу задачи — например «Создать
начисление» для документа-мастера, который раскладывает сумму и создаёт черновики, а
не проводит финансовую операцию напрямую. Работает и в автогенерируемой, и в
управляемой форме; вторая кнопка получает подпись «‹ваша подпись› и закрыть» и при
желании скрывается.

**Как настроить.** В YAML документа рядом с `posting: true` — поле
`post_caption: "Создать начисление"`; чтобы убрать вторую кнопку, добавьте
`post_and_close_hidden: true`. То же — в конфигураторе → карточка документа → блок
«Свойства». Без этих полей поведение прежнее («Провести»).

**Как попробовать.** Задайте `post_caption` любому проводимому документу и откройте
его форму — на кнопке будет ваша подпись.

## Страницы (произвольные экраны на DSL)
<!-- status: stable -->
<!-- date: 2026-06-18 -->

Объект «страница» — именованный экран раздела, который формируется обработчиком из
структурных блоков: заголовки, показатели, таблицы, списки, графики, кнопки и
кнопки-действия (вызов серверной процедуры). Можно вставлять произвольный HTML — он
проходит через санитайзер, остальное содержимое автоматически экранируется.

**Как настроить.** `pages/<имя>.yaml` + обработчик `src/<имя>.page.os`
(`Процедура ПриФормировании(Страница, Параметры)`); регистрация в разделе —
строкой `pages:` в `subsystems/<раздел>.yaml`. Создаётся и правится в конфигураторе.

**Как попробовать.** Откройте страницу из меню раздела (`/ui/page/<Имя>`) — пример
«Панель» в `examples/trade`.

## Режимы отображения списков (плитка, лента, дерево)
<!-- status: stable -->
<!-- date: 2026-06-16 -->

Список сущности можно показывать постранично (по умолчанию), «лентой», «плиткой»
(карточки с картинкой) или деревом (для иерархических справочников). Выбор запоминается
по каждой сущности.

**Как настроить.** Режим догрузки по умолчанию — поле `list_mode` (`feed` или
`pages`) в `<сущность>.yaml`. Состав плиточной карточки задаётся блоком
`tile_view`:

```yaml
fields:
  - name: Наименование
    type: string
  - name: Артикул
    type: string
  - name: Фото
    type: image
  - name: Цена
    type: number
  - name: Остаток
    type: number

tile_view:
  image: Фото
  title: Наименование
  subtitle: Артикул
  fields: [Цена, Остаток]
```

Если `tile_view` не задан, плитка строится автоматически: картинка берётся из
поля типа `image`, заголовок — из первого обычного реквизита, остальные поля
показываются ниже. Явное `fields: []` оставляет только картинку, заголовок и
подзаголовок.

**Как попробовать.** В списке нажмите переключатель вида (например ▦ «Плитка»).

## Поиск, фильтры и пагинация списков
<!-- status: stable -->
<!-- date: 2026-05-30 -->

Списки объектов поддерживают постраничный вывод, быстрый поиск и фильтры по полям, чтобы
большие справочники и документы не загружались в браузер целиком. Обычный UI использует
пагинацию, а режимы отображения списка (страницы/лента/плитка/дерево) выбираются поверх
этого механизма.

**Как попробовать.** Откройте большой справочник или список документов, введите строку
поиска и переключайтесь между страницами результата.

## Вкладочный режим работы с формами
<!-- status: testing -->
<!-- date: 2026-06-21 -->
<!-- issue: 129 -->

Пользовательский интерфейс может открывать карточки объектов во внутренних вкладках:
список остаётся открыт, несколько документов можно держать рядом, вкладку можно
продублировать, а несохранённые изменения защищены предупреждением. Режим выбирается
пользователем в топбаре и может задаваться глобально в параметрах базы.

**Как настроить.** Пользовательский режим → переключатель открытия форм в шапке или
**Параметры** базы (`ui.form_open_mode`) для значения по умолчанию.

**Как попробовать.** Включите режим вкладок, откройте два документа из списка, измените
один из них и попробуйте закрыть вкладку без сохранения.

## Поле-картинка (image)
<!-- status: stable -->
<!-- date: 2026-06-16 -->

Тип реквизита `image`: к элементу справочника или документу привязывается изображение
(фото товара, логотип) с превью на форме и на плитке списка. Файл хранится отдельно,
в колонке — ссылка.

**Как настроить.** Задайте реквизиту `type: image` в метаданных сущности:

```yaml
fields:
  - name: Фото
    type: image
```

Картинка допустима только в реквизитах самой сущности — в табличных частях тип
`image` не поддерживается (`onebase check` скажет об этом прямо). Файл кладётся
в хранилище базы (по умолчанию на диск, `~/.onebase/files/<база>/_blobs/<uuid>`;
режим переключается настройкой `ui.file_storage` = `disk` | `db` | `s3`), а в
колонке остаётся UUID.

Картинку можно проставить и из DSL. Для реквизита `image` передайте третьим
аргументом ссылку на целевую запись и сохраните картинку с объектом в одной
транзакции:

```bsl
НачатьТранзакцию();
Попытка
  Реф = Справочники.Номенклатура.НайтиПоИдентификатору(ИД);
  Объект = Реф.ПолучитьОбъект();
  Объект.Фото = СохранитьКартинку(Base64, "image/png", Реф);
  Объект.Записать();
  ЗафиксироватьТранзакцию();
Исключение
  Ошибка = ОписаниеОшибки();
  ОтменитьТранзакцию();
  ВызватьИсключение(Ошибка);
КонецПопытки;
```

Ссылка-владелец задаёт область проверки прав для `/image/{uuid}`; builtin
проверяет право записи строки и требует активную транзакцию. Старый вызов с
двумя аргументами оставлен для UUID в строках/константах, но не подходит для
реквизита сущности: у такого blob нет владельца, для отдачи достаточно
аутентификации, а GC намеренно не может признать его сиротой.

Транзакция гарантирует общий исход строк БД. Для `disk` и `s3` содержимое лежит
вне БД: при обработанном rollback платформа выполняет компенсационное удаление
(S3 — с ограниченным таймаутом и предупреждением при ошибке), но это не строгая
атомарность на случай аварийного завершения процесса или недоступности внешнего
хранилища.

**Как попробовать.** В `examples/trade` у номенклатуры есть реквизит «Фото»:
заполните базу демо-данными, запустите обработку **«Демо — проставить фото
номенклатуре»** и откройте список номенклатуры — переключатель вида ▦ «Плитка»
покажет карточки с картинками, в обычном списке будут миниатюры. Своё фото
грузится кнопкой «Загрузить…» в карточке товара.

Две вещи, которые удивляют при первом знакомстве:

- файл уходит на сервер **сразу при выборе**, ещё до сохранения формы; если
  запись не сохранить, картинка останется в хранилище (загруженные через форму
  сироты вычищает `onebase gc-blobs --delete`; при обычном rollback owner-aware
  вызова из DSL платформа также пытается удалить внешнее содержимое, а legacy-
  вызов без владельца GC не трогает — такой UUID мог быть положен прикладным
  кодом куда угодно);
- если в `tile_view` задан блок `fields`, он определяет **и** колонки табличного
  списка — поле с картинкой пропадёт из таблицы, пока не впишете его в этот
  список явно.

## Картинки в S3-хранилище
<!-- status: testing -->
<!-- date: 2026-07-30 -->
<!-- issue: 393 -->

Содержимое картинок (поле `image`) можно хранить в S3-совместимом объектном
хранилище (AWS S3, MinIO, Ceph RGW, Backblaze B2, Wasabi) — третий режим рядом с
«на диске» (по умолчанию) и «в базе». В строке сущности остаётся только ссылка-UUID;
раздача идёт **через приложение** тем же авторизованным обработчиком (проверка прав,
защита от IDOR сохраняется), публичные URL бакета не используются. Клиент S3 —
встроенный, **без внешних зависимостей** (подпись AWS SigV4 на стандартной
библиотеке). Каждый блоб помнит, где лежит (`disk`/`db`/`s3`), поэтому смена режима
не рвёт уже загруженные картинки. Сборка мусора (`onebase gc-blobs`) удаляет и
объекты в бакете.

Так же в S3 хранятся **вложения** к документам/справочникам — тот же режим
`ui.file_storage = s3` и та же секция `file_storage.s3`. Загрузка идёт прямо в
бакет (объекты под `<prefix>/attachments/<владелец>/<id>`), отдача — через
приложение (`http.ServeContent` с поддержкой докачки). По умолчанию для отдачи
вложение скачивается во временный файл (просто и полно по фичам; заодно
развязывает скорость S3 от медленного клиента). Опция **`stream: true`** в
`file_storage.s3` включает раздачу **потоком через Range** — вложение не
копируется на диск, из S3 тянутся только реально запрошенные байты (выигрыш на
больших файлах и докачке; полезно для видео/крупных PDF). DSL `ПутьКВложению` для
S3-вложения в любом случае материализует объект во временный файл и возвращает
путь к нему (файл живёт до конца запроса/прогона обработки).

**Как настроить.** Режим включается в базе (настройка `ui.file_storage = s3`), а
креды — в `config/app.yaml` (секция `file_storage.s3`), ключи через `${env:VAR}`:

```yaml
file_storage:
  s3:
    endpoint: s3.amazonaws.com     # или minio.local:9000
    region: us-east-1
    bucket: my-onebase-files
    prefix: base1/                 # ключ-префикс (объекты лягут в <prefix>/blobs/<id>)
    access_key: ${env:FS_S3_KEY}
    secret_key: ${env:FS_S3_SECRET}
    use_ssl: true                  # для локального MinIO по http — false
    path_style: true               # совместимо с MinIO/кастомными эндпойнтами
```

Если режим `s3`, а секция `file_storage.s3` не задана или неполна — сервер не
стартует (чтобы картинки не «пропали» молча).

**Как попробовать.** Локальный MinIO: создайте бакет, пропишите `file_storage.s3`
(`use_ssl: false`), переключите базу в режим S3, загрузите картинку в поле `image` —
объект появится в бакете под `<prefix>/blobs/<id>`, а превью на форме отдаётся
как обычно.

## RichText-поле
<!-- status: stable -->
<!-- date: 2026-06-16 -->

Тип реквизита `richtext` — поле форматированного текста (заметки, описания, комментарии
с разметкой). Редактор работает офлайн через Quill, хранит HTML с base64-картинками,
пропускает содержимое через санитайзер и умеет выводить richtext в HTML/PDF печатных
формах.

**Как настроить.** Задайте реквизиту `type: richtext` в метаданных сущности.

**Как попробовать.** Откройте форму с richtext-полем, отформатируйте текст, вставьте
картинку и выведите объект в печатную форму.

## Управляемые формы
<!-- status: stable -->
<!-- date: 2026-05-25 -->

Декларативные формы объектов: страницы-закладки, группы, поля ввода (с `data_path`,
multiline, выбором, readonly, required), табличные части, команды формы и автоматическая
командная панель; многоязычные заголовки. Обработчики событий (`ПриОткрытии`,
`ПриЧтенииНаСервере`, `ПередЗаписью`, `ПриЗаписи`, `ПослеЗаписи`, изменение поля) — в
`.form.os`. В управляемых формах работают вложения, поле даты, скрытие штатной кнопки
удаления и серверные события строк табличной части.

`required: true` у элемента — настоящая проверка этой формы: совместимый контрол
получает нативный HTML-атрибут, а сервер проверяет пустое значение и при создании,
и при изменении. Это UI-правило не изменяет метаданные и не действует на REST, DSL или
импорт. Для глобальной гарантии задайте `required: true` у реквизита сущности — тогда
он проверяется на всех путях записи по финальному состоянию, а реквизит шапки
автоматически отмечается звёздочкой в управляемой форме. Нативный HTML-атрибут при
этом не ставится: значение может заполнить обработчик записи до проверки финального
состояния. Для раннего запрета пустого submit добавьте `required: true` именно на
элемент формы. Отдельный ключ `not_null` для полей не поддерживается. Ноль и `Ложь`
считаются заполненными. Поля только для чтения form-level проверку не блокируют, а
`Наименование` справочника не становится обязательным неявно.

**Как настроить.** `forms/<сущность-в-нижнем-регистре>/<форма>.form.yaml` (+ опциональный
одноимённый `.form.os`). Правится в конфигураторе.

**Как попробовать.** Откройте документ с управляемой формой (примеры —
`examples/tasks/forms/`, `examples/trade/forms/`).

## Состав поиска по строке (search_fields)
<!-- status: testing -->
<!-- date: 2026-08-11 -->

Строка поиска над списком и подбор ссылки ищут подстроку по всем строковым
реквизитам объекта. Теперь состав можно задать явно — в YAML справочника или
документа:

```yaml
search_fields: [Наименование, Артикул, Штрихкод]
```

Зачем это нужно:

- **добавить нечисловое в поиск.** Артикул или штрихкод часто хранят числом, и
  по умолчанию такой реквизит в поиск не попадал — а именно по нему набирают
  позицию те, кто работает с клавиатуры. В явном списке тип не ограничен;
- **сузить поиск.** По умолчанию ищется и по описанию: набор «яб» вытягивает
  товары, у которых слово встретилось в тексте описания. Короткий список делает
  подбор точнее и быстрее на больших справочниках;
- `search_fields: []` — поиск по строке выключен: запрос вернёт пусто, а не весь
  список.

Ключ независим от `fulltext:` — тот управляет глобальным поиском по базе.
Ссылочный реквизит в списке отклоняется на `onebase check`: в колонке UUID,
искать по нему подстроку бессмысленно.

**У документа не забудьте включить `Номер`.** Явный список заменяет умолчание
целиком, а номер документа в `fields:` не объявлен — платформа синтезирует его
из блока `numerator:`. Поэтому `search_fields: [Контрагент]` у документа молча
отключит поиск по номеру, хотя именно по нему документ набирают с клавиатуры.
Правильно — `search_fields: [Номер, Контрагент]`.

**Как попробовать.** В `examples/trade` у справочника «Номенклатура» состав уже
задан: откройте документ, встаньте на колонку «Номенклатура» и наберите артикул
вместо наименования.

## Ввод по строке в табличной части
<!-- status: testing -->
<!-- date: 2026-08-10 -->

Колонка-ссылка табличной части (например «Номенклатура» в заказе) заполняется
с клавиатуры, без мыши: начните печатать часть наименования — список отберётся
по набранному, `↑`/`↓` ведут по вариантам, `Enter` подставляет выбранный,
`Tab` подставляет и переходит в следующую колонку. Поиск идёт **по всему
справочнику** на сервере (с учётом прав, RLS и маскирования полей), а не только
по позициям, предзагруженным в форму.

`F4` (или `Alt`+`↓`) открывает форму выбора — в ней тоже работают `↑`/`↓`,
`Enter` и `Esc`. `Esc` в ячейке сначала закрывает выпадающий список и лишь
затем отменяет правку. Если набранное не находится ни в одной позиции,
значение не записывается молча: ячейка остаётся в режиме ввода с подсказкой.

**Как попробовать.** Откройте документ с табличной частью, встаньте на колонку
со ссылкой, нажмите `Enter`, наберите несколько букв наименования и пройдите
строку до конца по `Tab` — мышь не нужна.

## Работа с клавиатуры: списки, формы, строки табличной части
<!-- status: testing -->
<!-- date: 2026-08-11 -->

Привычные по 1С сочетания работают везде без настройки — ни в конфигурации,
ни в форме их объявлять не нужно. Табличные сочетания одинаково доступны в
автогенерируемой форме и в управляемой форме — как в SlickGrid, так и при
`no_grid: true`.

| Где | Клавиша | Действие |
|---|---|---|
| Список | `Ins` | Создать |
| Список | `↑` / `↓` | Курсор по строкам |
| Список | `Enter` | Открыть (на группе — войти в группу) |
| Список | `F2` | Открыть элемент |
| Список | `Delete` | Пометить на удаление |
| Список | `Ctrl`+`F` | Перейти в строку поиска |
| Форма | `Ctrl`+`Enter` | Провести и закрыть (если кнопки нет — записать) |
| Форма | `Ctrl`+`S` | Записать |
| Форма | `Esc` | Закрыть (с подтверждением, если есть изменения) |
| Табличная часть | `Ins` | Добавить строку и сразу начать ввод |
| Табличная часть | `F9` | Копировать текущую строку |
| Табличная часть | `Delete` | Удалить текущую строку; в DOM-таблице с флажками — все отмеченные строки |
| Табличная часть | `Ctrl`+`↑` / `Ctrl`+`↓` | Переместить строку |

Незавершённый ввод в ячейке не теряется: перед записью формы правка
принимается, а если принять её нельзя (например, в колонке-ссылке набрано
ненайденное) — запись отменяется с указанием причины.

Структурные клавиши относятся к последней видимой табличной части, с которой
работал пользователь; наличие `activeCell` в другой таблице её не перехватывает.
Поставляемый SlickGrid не включает модель множественного выделения, поэтому в
нём `Delete` работает с активной строкой; `no_grid` сохраняет удаление нескольких
строк по флажкам. В
обычных полях ввода `Insert`/`Delete` остаются клавишами редактирования, а у
табличной части только для чтения добавление, копирование, перемещение и
удаление строк отключены.

Сочетания подписаны на самих кнопках (всплывающая подсказка), а буквенные
разбираются по физической клавише — при русской раскладке `Ctrl`+`S` работает
так же, как при английской.

**Как попробовать.** Откройте список, нажмите `Ins`, заполните шапку по `Tab`,
на табличной части нажмите `Ins` и наберите строку целиком с клавиатуры,
затем `Ctrl`+`Enter`.

## Горячие клавиши управляемых форм
<!-- status: stable -->
<!-- date: 2026-07-08 -->

Кнопке управляемой формы можно назначить F-клавишу, которая запускает тот же обработчик
`Нажатие`, что и обычный клик. Поддерживаются `F2`, `F4`, `F7`, `F8`, `F9` и `F10`.
Для полей и других элементов доступен стандартный браузерный `accesskey` с системным
модификатором браузера/ОС. `onebase check --lint` предупреждает о неподдерживаемых и
повторяющихся горячих клавишах.

**Как настроить.** Добавьте кнопке `hotkey: F7`. Для браузерной активации элемента
задайте `accesskey: "7"`; конкретное сочетание зависит от браузера и ОС, обычно это
Alt/Option + клавиша.

**Как попробовать.** Откройте managed-форму с кнопкой `hotkey: F7`, установите фокус
в любое поле формы и нажмите F7 — выполнится обработчик кнопки. Затем запустите
`onebase check --project <dir> --lint`, продублировав F7 на второй кнопке, и проверьте
диагностику конфликта.

## Поле со списком значений (СписокВыбора)
<!-- status: stable -->
<!-- date: 2026-06-22 -->

Реквизит на управляемой форме можно показать выпадающим списком с произвольным набором
значений, заданным прямо в форме — без отдельного перечисления и без справочника (аналог
`СписокВыбора` в 1С). Подписи значений многоязычные. Выбор из списка вызывает событие
`ПриИзменении`, поэтому обработчик в `.form.os` может показать или подгрузить связанные
данные при выборе.

**Как настроить.** Элемент `kind: ПолеСписка` с блоком `choices` (`value` + `title`) и
обработчиком `events: { ПриИзменении: ИмяПроцедуры }`; реквизит — строкового типа.

**Как попробовать.** В `examples/crm` откройте «Сделку», поле **«Валюта»**: выберите
валюту из списка — появится сообщение-расшифровка (обработчик `ВалютаПриИзменении`).

Список можно строить и **динамически** в коде (обработчик `НачалоВыбора` + билтин
`ДобавитьЗначениеСписка`) — когда состав зависит от данных.

## Серверное событие чтения формы
<!-- status: testing -->
<!-- date: 2026-06-21 -->
<!-- issue: 148 -->

Перед показом объектной управляемой формы можно выполнить обработчик
**`ПриЧтенииНаСервере`**. Он получает объект формы уже после загрузки данных и может
дополнить реквизиты, скрыть чувствительные поля или отказать в чтении через
`ВызватьИсключение`. Это практичный hook для RLS-проверок на уровне прикладной
конфигурации.

**Как настроить.** В `forms/<сущность>/<форма>.form.yaml` добавьте событие формы
`ПриЧтенииНаСервере: ИмяПроцедуры` и реализуйте процедуру в одноимённом `.form.os`.

**Как попробовать.** В обработчике проверьте текущего пользователя и вызовите
`ВызватьИсключение("Нет доступа")` — открытие формы вернёт отказ вместо данных.

## Серверные события записи формы и строк ТЧ
<!-- status: stable -->
<!-- date: 2026-06-22 -->

Управляемая форма выполняет серверные обработчики при сохранении: **`ПередЗаписью`**
и **`ПриЗаписи`** (до записи — валидация и автозаполнение; `ВызватьИсключение`
отменяет запись) и **`ПослеЗаписи`** (после записи — уведомления, связанные данные,
Объект уже с номером). Грид табличной части дёргает **`ПриДобавленииСтроки`**,
**`ПослеДобавленияСтроки`**, **`ПриУдаленииСтроки`**, **`ПриИзмененииСтроки`** и
**`ПриАктивизацииСтроки`** — последнее при переходе на другую строку.

**Как настроить.** Объявите обработчики в блоке `events` (уровень формы — для записи;
на элементе `ТабличнаяЧасть` — для строк) и реализуйте процедуры в `.form.os`.

Обработчик записи объявляется без выходного параметра, например
`Процедура ПередЗаписьюФормы()`. Совместимость с 1С-параметром `Отказ` пока не
поддерживается: `Отказ = Истина` запись не отменяет. Используйте
`ВызватьИсключение` прямо в обработчике или в вызываемом общем модуле —
необработанное исключение прерывает сохранение.

**Как попробовать.** В `examples/crm` форма «Сделка» в `ПередЗаписьюФормы` проверяет
заголовок и клиента и пересчитывает суммы — попробуйте сохранить сделку без заголовка
и увидите отказ записи.

## Визуальный конструктор управляемых форм
<!-- status: testing -->
<!-- date: 2026-06-23 -->
<!-- issue: 164 -->

WYSIWYG-конструктор управляемой формы прямо в редакторе: правая панель стала
интерактивным **холстом**. Реквизит из палитры перетаскивается **на форму** (а не в
текст YAML) и появляется полем в нужном месте; клик по элементу открывает **панель
свойств** (заголовок, имя, `data_path`, подсказка, обязательное/только-чтение). Холст и
YAML синхронизированы двусторонне: правка на холсте переписывает YAML в Monaco, правка
YAML перерисовывает холст. Ручные комментарии и порядок ключей в `.form.yaml` при этом
сохраняются (round-trip через дерево YAML), поэтому конструктор не затирает правки,
сделанные руками. Невалидный YAML временно блокирует визуальные правки с баннером.

Кроме реквизитов на холст переносятся **структурные элементы** (группа, набор
страниц-закладок, страница, надпись) и **табличные части** объекта — отдельными
палитрами. Брошенная на холст **страница сразу становится закладкой** (оборачивается в
набор вкладок), а «Страницы (набор)» создаётся уже с одной готовой вкладкой. Выбранный
элемент можно **удалить** или **переставить** среди соседей кнопками ↑/↓ в панели
свойств, либо перетащить мышью в другой контейнер. У табличной части в панели свойств —
**состав колонок** галочками: отметка добавляет колонку, снятие убирает; порядок колонок
— теми же ↑/↓ на выбранной колонке. Возврат **← В конфигуратор** ведёт обратно к объекту,
из которого открыли форму. Редактор кода (YAML + модуль) слева можно **свернуть** кнопкой
«⮜ Свернуть код», отдав всё место холсту.

**События — формы «оживают».** Панель свойств разделена на закладки **«Элемент | Форма»**.
На закладке «Элемент» у выбранного элемента есть секция **События**: у кнопки — `Нажатие`,
у поля/флажка/даты/таблицы/переключателя — `ПриИзменении`. Выпадающим списком выбирается
процедура из модуля `.form.os` или пункт **«Создать обработчик…»** — конструктор сам
допишет пустую процедуру в модуль. Закладка **«Форма»** (или клик по пустому месту холста)
открывает свойства самой формы: заголовок, вид (object/list/choice/…), события формы
(`ПриОткрытии`, `ПередЗаписью`, `ПриЗаписи`, …) и галочку видимости штатной кнопки
**«Удалить»**.

**Переключатель и список значений.** Новый элемент **Переключатель** в палитре: поле с
набором значений в виде радио-кнопок или выпадающего списка (переключается свойством
**Представление**). Для поля-перечисления значения подставляются автоматически; для
произвольных (в т.ч. числовых) наборов значение→представление задаётся в свойствах.

**Как настроить.** Конфигуратор → **Управляемые формы** → откройте/создайте форму. Справа
переключатель **Конструктор | Просмотр** (по умолчанию — Конструктор).

**Как попробовать.** Перетащите реквизит-чип из палитры над редактором на drop-зону
холста — добавится поле. Перетащите чип **Группа**, **Переключатель** или **табличную
часть** — добавится контейнер/элемент; в табличной части отметьте нужные колонки.
Кликните элемент, поменяйте свойства, привяжите событие к процедуре, переставьте
кнопками ↑/↓ или удалите. Нажмите **Сохранить** — изменения уйдут в
`forms/<сущность>/<форма>.form.yaml`.

## SlickGrid для табличных частей
<!-- status: stable -->
<!-- date: 2026-06-06 -->

Табличные части в управляемых формах рендерятся через SlickGrid — виртуализация для
больших объёмов строк, как в ERP. Используется по умолчанию.

**Как попробовать.** Откройте документ с табличной частью (например реализацию товаров) и
отредактируйте строки прямо в гриде.

## Команды табличных частей и подбор с мультивыбором
<!-- status: stable -->
<!-- date: 2026-06-06 -->

Обобщённый механизм команд формы/табличной части и диалога подбора с мультивыбором:
событие `Нажатие` открывает подбор (`ПоказатьПодбор`), результат возвращается событием
`Выбор`. Подходит для подбора номенклатуры и любых других операций над выделенными строками.

**Как попробовать.** На форме документа нажмите кнопку команды (например «Подбор» в
табличной части) и отметьте несколько позиций.

## Диалог подбора ищет на сервере

<!-- status: testing -->
<!-- date: 2026-09-13 -->

Строка поиска в диалоге `ПоказатьПодбор` умеет спрашивать сервер, а не фильтровать
уже приехавшие строки. Нужно там, где клиентский фильтр бессилен: выдача обрезана
пределом и искомое осталось за ним, либо колонка показана маской ПДн (план 88) —
в тексте строки `••••2233`, и искать по нему нечего.

**Как настроить.** В конфиге диалога — `Конфиг.Вставить("ПоискНаСервере", Истина)`.
У той же кнопки появляется событие `Поиск`: набранное приходит в переменной
`ПодборЗапрос`, обработчик отбирает по нему и снова зовёт `ПоказатьПодбор`.

**Как попробовать.** Откройте подбор, наберите часть значения — строки в открытом
окне заменятся ответом сервера, закрывать диалог не нужно. Подробности и правила
(пустой запрос, пустая выдача, задержка 250 мс) — в `docs/forms.md`, раздел
«Диалог подбора ищет на сервере».

## Условное оформление табличных частей форм
<!-- status: testing -->
<!-- date: 2026-07-05 -->
<!-- issue: 236 -->

Managed-формы раскрашивают строки и ячейки табличных частей по условиям — двумя
способами. Декларативно в `.form.yaml`: блок `conditional_formatting` с правилами
`target` (имя ТЧ, ValueTable или элемента формы), `when` (DSL-выражение в контексте
строки; доступны поля строки, значения шапки и `НомерСтроки`), `field` (пусто = вся
строка) и `then` (`color`, `background`, `bold`, `italic`). И программно из
обработчиков `.form.os`: `ДобавитьПравилоОформления(Цель, Условие, ...)` и
`ОчиститьОформление([Цель])` меняют оформление на время ответа текущего события.
Работает и в SlickGrid, и в простых таблицах; цвета проходят CSS-фильтр
безопасности, а цели, поля и выражения правил проверяет `onebase check`.

**Как настроить.** `docs/forms.md`, раздел «Условное оформление».

**Как попробовать.** Добавьте в `.form.yaml` документа правило `target: Товары`,
`when: Количество < 0`, `then: {background: "#fee2e2"}` и откройте форму — либо
вызовите `ДобавитьПравилоОформления` в обработчике события.

## Дашборды и виджеты главной страницы
<!-- status: stable -->
<!-- date: 2026-05-31 -->

Главная страница из виджетов: KPI (со сравнением с прошлым периодом), списки, графики
(bar/line/pie), кнопки быстрых действий, последние документы. У раздела может быть своя
главная.

**Как настроить.** `widgets/<имя>.yaml` и `config/home_page.yaml` (или `home_page` в
подсистеме); редактор виджетов — в конфигураторе.

**Как попробовать.** Откройте главную (примеры — `examples/trade/widgets/`).

## Условная доступность элементов формы
<!-- status: testing -->
<!-- date: 2026-08-06 -->

Элемент управляемой формы принимает `readonly_when` и `hidden_when` — условия по
полям записи. Пока условие истинно, элемент нередактируем либо не показывается
вовсе. Запрет, который живёт в бизнес-логике, перестаёт быть невидимым: раньше
поле выглядело активным, а отказ прилетал исключением уже при записи.

```yaml
- kind: ПолеВвода
  name: ПолеУлица
  data_path: Объект.Улица
  readonly_when: 'СтадияОформления = "Принята"'

- kind: Кнопка
  name: КнопкаПринять
  hidden_when: 'СтадияОформления = "Принята"'
```

`readonly_when` действует только на указанный элемент и не распространяется на
его детей. `onebase check` поэтому не разрешает его у `ГруппаФормы`,
`СтраницыФормы` и `Страница`: задайте условие на конкретных полях или кнопках.
`hidden_when` у этих контейнеров поддерживается и скрывает всё содержимое.
Статический `readonly: true` отличается намеренно — дочерние элементы наследуют
такой запрет.

**Как попробовать.** Добавьте условие в `forms/<сущность>/<форма>.form.yaml`,
откройте карточку в двух состояниях и сравните. Язык выражений — тот же, что у
`when` условного оформления.

Условия пересчитываются после каждого события формы: `readonly_when` меняется на
экране сразу, в обе стороны. Скрытый элемент можно так же сразу спрятать, а вот
показать обратно — только перезагрузкой страницы: его не было в разметке.
Подробности и границы — в `docs/forms.md`.

## Переход к данным из KPI-плитки
<!-- status: testing -->
<!-- date: 2026-08-06 -->

У KPI-виджета появился ключ `link:` — клик по значению открывает список или отчёт,
из которого счётчик и посчитан. «Передачи в карантине: 3» перестаёт быть просто
числом: с плитки видно, куда идти разбирать.

```yaml
name: ПередачиВКарантинеKPI
type: kpi
link: /ui/document/ПередачаЗаявки?f.СостояниеПередачи=ТребуетРазбора
query: |
  ВЫБРАТЬ КОЛИЧЕСТВО(*) КАК Значение ИЗ Документ.ПередачаЗаявки
  ГДЕ СостояниеПередачи = 'ТребуетРазбора'
```

Принимается только внутренний путь приложения (начинается с `/`); внешние адреса
игнорируются, плитка в этом случае остаётся некликабельной. Без `link:` поведение
прежнее.

## Подсистемы (разделы навигации)
<!-- status: stable -->
<!-- date: 2026-05-08 -->

Структурирование интерфейса и прав по разделам: в разделе перечисляются страницы, отчёты,
документы и справочники; у раздела может быть своя главная страница.

**Как настроить.** `subsystems/<раздел>.yaml`.

**Как попробовать.** Переключайте разделы в шапке приложения.

## Скрытие главной страницы (навигация только по разделам)
<!-- status: testing -->
<!-- date: 2026-07-09 -->
<!-- issue: 304 -->

Глобальную «Главную» можно спрятать целиком: ведущая ссылка «Главная» уходит из панели
разделов, а вход (`/ui/`) сразу открывает первый раздел. Полезно, когда интерфейс
организован подсистемами и общий стартовый дашборд со всеми объектами не нужен. Работает и
в режиме отдельных страниц, и во вкладках. Фейл-сейф: если подсистем нет, флаг игнорируется
(иначе навигация оказалась бы пустой).

**Как настроить.** В конфигураторе: узел **«Главная страница»** → галочка **«Скрыть главную
(навигация только по разделам)»**. Вручную — `hidden: true` в `config/home_page.yaml`.

**Как попробовать.** В базе с подсистемами включите флаг и откройте `/ui/` — вместо общей
главной откроется первый раздел, а «Главной» в шапке не будет.

## Иконки разделов навигации
<!-- status: testing -->
<!-- date: 2026-06-23 -->
<!-- issue: 72 -->

У раздела навигации (подсистемы) может быть иконка — она выводится в шапке приложения
перед названием раздела. Иконки берутся из набора [Lucide](https://lucide.dev/icons):
весь набор вшит в программу одним SVG-спрайтом, интернет для них не нужен.

**Как настроить.** Поле `icon:` в `subsystems/<раздел>.yaml` — имя иконки Lucide в
kebab-case, например `shopping-cart`, `layout-dashboard`, `russian-ruble`. В
конфигураторе рядом с полем «Иконка» есть живое превью, подсказка доступных имён и
ссылка на каталог lucide.dev; имя нормализуется автоматически (нижний регистр, дефисы).
Работает **любое** имя из каталога lucide.dev — подсказка перечисляет их все; неизвестное
имя показывается нейтральным квадратом, пустое — без иконки. У страницы поле `icon:`
тоже есть (с превью в конфигураторе).

**Как попробовать.** Задайте `icon` разделу и переключайте разделы в шапке — иконка
появится перед названием. В конфигураторе начните вводить имя в поле «Иконка» — превью
обновится сразу. Возьмите на lucide.dev что-нибудь неожиданное (`rocket`, `stethoscope`,
`graduation-cap`) — работает без обновления программы.

## Журнал документов
<!-- status: stable -->
<!-- date: 2026-05-20 -->

Объединение разнородных документов в общую ленту с настраиваемыми колонками,
пользовательскими настройками списка и условным оформлением строк/ячеек.

**Как настроить.** `journals/<имя>.yaml` (`documents`, `columns`,
`conditional`). В правилах условного оформления `field` можно оставить пустым
для всей строки или указать колонку журнала; `Документ` красит колонку вида
документа.

**Как попробовать.** Откройте журнал из навигации.

## Условное оформление журнала документов
<!-- status: stable -->
<!-- date: 2026-07-05 -->

Строки и ячейки журнала раскрашиваются по условиям — DSL-выражениям в контексте
строки журнала. Правило задаёт цвет текста и фона, жирный и курсив; `field` пусто —
красится вся строка, имя колонки — только её ячейка, `Документ` — колонка вида
документа. Ошибки выражений показываются предупреждением над списком, а
`onebase check` ловит их ещё до запуска.

**Как настроить.** `journals/<имя>.yaml`, блок `conditional`: `when`, `field`,
`then` (`color`, `background`, `bold`, `italic`). Правила проверяются по
порядку: действует первое совпавшее для строки и первое — для каждой ячейки.

**Как попробовать.** Добавьте правило `when: Сумма < 0`, `then: {background:
"#fee2e2", color: "#991b1b"}` и откройте журнал.

## Настройка колонок журнала пользователем
<!-- status: stable -->
<!-- date: 2026-07-05 -->

Каждый пользователь настраивает журнал под себя: панель **«Настройка списка»** на
форме журнала управляет видимостью и порядком колонок (стрелки ↑/↓). Настройки
персональны, хранятся в базе (конфигурация не меняется) и применяются также к
выгрузке журнала в Excel; **«Стандартные настройки»** возвращают вид из YAML.

**Как попробовать.** Откройте журнал → **«Настройка списка»**, скройте колонку или
поменяйте порядок и нажмите **«Сохранить»**.

## Автонумерация документов
<!-- status: stable -->
<!-- date: 2026-05-10 -->

Документы получают номера автоматически по настраиваемому формату, длине и периодичности
сброса. Нумерация атомарна при параллельной записи, поэтому два пользователя не получают
один и тот же номер.

**Как настроить.** В метаданных документа задайте блок нумерации: префикс/формат, длину
и период сброса, если он нужен.

**Как попробовать.** Создайте несколько документов подряд и проверьте, что номер
заполняется автоматически.

## Пометка на удаление и проведение документов
<!-- status: stable -->
<!-- date: 2026-06-09 -->
<!-- issue: 36 -->

Проведённый документ нельзя провести повторно, если он помечен на удаление; пометка
проведённого документа автоматически отменяет проведение, а снятие пометки возвращает
документ в обычное состояние. В списках доступны действия отмены проведения, пометки,
снятия пометки и удаления помеченных.

**Как попробовать.** Проведите документ, пометьте его на удаление и убедитесь, что движения
сняты, а кнопки проведения скрыты до снятия пометки.

## Иерархические справочники
<!-- status: stable -->

Группы и элементы с деревом в списке — для номенклатуры, статей, подразделений.

**Как настроить.** `hierarchical: true` (вид иерархии — `hierarchy_kind`) в метаданных
справочника.

**Как попробовать.** Откройте иерархический справочник и разверните группы.

## Активность элементов справочника
<!-- status: stable -->
<!-- date: 2026-06-25 -->

Для сезонных или временно неиспользуемых элементов справочника можно включить opt-in
механизм активности. В списке появятся переключатели «Активные / Скрытые / Все», а
неактивные элементы будут скрываться из выбора ссылок в рабочих формах. Отчёты и отборы
по ссылочным полям видят все элементы, чтобы старые документы и аналитика не теряли данные.

**Как настроить.** Добавьте булевый реквизит и блок `activity` в YAML справочника или
включите «Активность» в конфигураторе:

```yaml
fields:
  - name: Наименование
    type: string
  - name: Активный
    type: bool
activity:
  field: Активный
  default_scope: active
  hide_from_choice: true
```

Если блок `activity` не задан, справочник работает как раньше, без дополнительных фильтров.

**Как попробовать.** В `examples/trade` откройте «Номенклатура», через действие строки
скройте товар из выбора, затем переключитесь на вкладку «Скрытые» и верните его обратно.

## Предопределённые элементы справочников
<!-- status: stable -->
<!-- date: 2026-05-10 -->

Справочник может содержать предопределённые элементы с фиксированными именами и UUID:
например базовые склады, системные категории или служебные контрагенты. Они синхронизируются
при старте и доступны в DSL как стабильные ссылки.

**Как настроить.** Добавьте блок `predefined` в YAML справочника с именем, UUID и
начальными значениями реквизитов.

**Как попробовать.** Опишите предопределённый элемент, запустите базу и проверьте, что
элемент создан и не теряется при повторном старте.

## Ввод на основании
<!-- status: stable -->
<!-- date: 2026-05-27 -->

Создание документа на основании другого с автозаполнением (например счёт → реализация).

**Как настроить.** Поле `based_on` в метаданных сущности + `ОбработкаЗаполнения`
(заполнение через выражение DSL).

**Как попробовать.** В форме документа-источника нажмите «Создать на основании».

## Оптимистичные блокировки
<!-- status: stable -->

При записи проверяется версия объекта; при конфликте интерфейс предлагает выбрать, какие
данные сохранить.

**Как попробовать.** Откройте один документ в двух вкладках, измените и запишите оба.

## Итоги регистров (быстрые остатки)
<!-- status: stable -->
<!-- date: 2026-07-08 -->

Для регистров накопления с большой историей движений можно включить предрасчёт
итогов: платформа держит помесячные обороты по каждому набору измерений в
отдельной таблице и поддерживает их прямо при проведении/отмене документов (в той
же транзакции, поэтому итоги всегда согласованы). Из них считаются `Остатки()`,
`Остатки(&Момент)` / `Остатки(&Дата)` и `ОстаткиИОбороты(&Начало, &Конец)` —
вместо суммирования всей истории читаются обороты прошлых месяцев плюс небольшой
«хвост». Быстрее на больших объёмах и корректно при проведении задним числом.

**Как настроить.** В YAML регистра добавьте блок `totals: { enabled: true }`,
затем `onebase migrate`. Для существующей базы итоги наполнятся при миграции.

**Как попробовать.** Включите `totals` у регистра остатков, проведите документы,
постройте отчёт/запрос с `.Остатки()` или `.Остатки(&Дата)` — результат тот же,
но без полного скана движений. Полный пересчёт вручную: `onebase recalc-totals
--project <dir> --sqlite <файл>`.

## Итоги регистра бухгалтерии
<!-- status: testing -->
<!-- since: v0.9.8 -->
<!-- date: 2026-07-31 -->

Тот же предрасчёт итогов, что у регистров накопления, теперь работает и для
**регистра бухгалтерии**. Помесячные обороты по счетам и субконто живут в
отдельной таблице и сдвигаются инкрементально прямо в транзакции проведения,
поэтому остаются согласованными с проводками. Из них считаются `Остатки()`,
`Остатки(&Момент)` и обороты — вместо суммирования всей истории `акк_*` читается
ближайшая граница периода плюс небольшой «хвост». На больших базах это разница
между «оборотка строится секундами» и «оборотка сканирует всё».

**Как настроить.** В YAML регистра бухгалтерии добавьте блок
`totals: { enabled: true }`, затем `onebase migrate` — для существующей базы
итоги наполнятся при миграции.

**Как попробовать.** Включите `totals` у регистра, проведите документы и
постройте оборотно-сальдовую ведомость или запрос с `.Остатки(&Дата)`. Полный
пересчёт вручную (после массовой правки или сбоя): `onebase recalc-totals
--project <dir> --sqlite <файл>`. Свёртка базы пересчитывает итоги сама.

## Настройка пула соединений PostgreSQL
<!-- status: testing -->
<!-- since: v0.9.8 -->
<!-- date: 2026-07-31 -->

Размер пула соединений к PostgreSQL настраивается в `config/app.yaml`. Дефолт
платформы — **20** соединений на процесс базы (раньше действовал автодефолт
драйвера pgx `max(4, NumCPU)`, то есть 4–8 — этого не хватало десяткам активных
пользователей: авторизация делает запросы на каждый запрос, а проведение держит
соединение всю транзакцию).

```yaml
db:
  pool_max_conns: 20   # максимум соединений (0 или отсутствие = дефолт 20)
  pool_min_conns: 2    # тёплый минимум (0 или отсутствие = дефолт 2)
```

Приоритет: `app.yaml` → явные `pool_max_conns`/`pool_min_conns` в строке
подключения → дефолт платформы. То есть настройка в DSN, если она уже была,
продолжает работать. К SQLite не относится — там всегда одно соединение.

**Когда трогать.** Если один PostgreSQL обслуживает много баз OneBase, посчитайте
суммарный лимит: `число баз × pool_max_conns` должно быть меньше
`max_connections` сервера. Для одной базы со 100+ активными пользователями
ориентир — 20–40 соединений; больше обычно не помогает, узким местом становится
сам PostgreSQL.

## Мобильный доступ (адаптив + PWA)
<!-- status: stable -->

Адаптивная вёрстка интерфейса и PWA: установка на телефон, офлайн-оболочка.

**Как попробовать.** Откройте базу на телефоне и выберите «Добавить на главный экран».

## Локализация интерфейса (i18n)
<!-- status: stable -->

Переводы синонимов объектов, полей и подписей по языкам (ru/en/de/sr); в DSL — `НСтр(...)`.

**Как настроить.** `titles:`/`labels:` в YAML и файлы `<проект>/locales/*.json`.

**Как попробовать.** Переключите язык интерфейса и проверьте переводы.

## Переводы синонимов в конфигураторе
<!-- status: testing -->
<!-- date: 2026-06-23 -->
<!-- issue: 31 -->

Синонимы объектов и их полей/параметров можно переводить на языки интерфейса
прямо в формах конфигуратора, без правки YAML. Поддержаны справочники, документы
(с полями и табличными частями), отчёты и обработки (с параметрами), константы,
подсистемы, регистры накопления/сведений/бухгалтерии (с измерениями и ресурсами),
страницы и главная страница.

Поля переводов по умолчанию скрыты, чтобы не загромождать форму (их правят
редко). Включаются разом одной кнопкой **«🌐 Переводы»** в шапке конфигуратора
(вкладка «Дерево»): тогда у заголовка объекта и каждого реквизита появляются
поля ввода синонима на каждом языке интерфейса. Режим запоминается между
сессиями. (Раньше свёрнутый блок «Переводы» висел отдельно у каждого поля и
занимал место, даже когда переводы не редактировали.)

**Как попробовать.** В конфигураторе на вкладке «Дерево» нажмите **«🌐 Переводы»**
в шапке, откройте любой справочник или документ — у объекта и его полей появятся
поля перевода; впишите синонимы и нажмите **«Сохранить»**.

## Переводы значений перечислений
<!-- status: testing -->
<!-- date: 2026-06-20 -->
<!-- issue: 31 -->

Значения перечислений теперь можно перевести на языки интерфейса: в разделе
конфигуратора «Перечисления» у каждого значения enum-а есть блок «🌐 Переводы».
Переведённые подписи отображаются в выпадающих списках форм, в автогенерируемых
списках документов/справочников (включая режим плиток), а также в управляемых
табличных частях (SlickGrid и DOM-режим).

**Формат YAML** поддерживает обратную совместимость: старый формат (`values: [A, B]`)
и новый с переводами (`values: [{name: A, titles: {en: ...}}]`) работают одновременно.

## Уведомления в реальном времени (push сервер → браузер)
<!-- status: stable -->
<!-- date: 2026-06-24 -->

Конфигурация может из встроенного языка отправить уведомление — и оно мгновенно
появится в браузере у нужного пользователя, без перезагрузки страницы. Под
капотом — SSE-канал `/ui/events` и внутрипроцессная шина. Это основа для живых
дашбордов, оповещений о задачах/событиях и «всплывашки входящего звонка»
(телефония, план 75).

**Как использовать (DSL).** Из любой обработки, HTTP-сервиса, проведения или
регламентного задания:

```
ОтправитьУведомление("ivan", "уведомление", "Заявка №42 назначена на вас");
ОтправитьУведомление("роль:Оператор", "очередь.переполнена", Новый Структура("ждут", 7));
ОтправитьУведомление("*", "уведомление", "Сервер перезагрузится в 18:00");
```

Первый аргумент — адрес: логин пользователя, `"роль:Имя"` или `"*"` (всем
онлайн). Событие `"уведомление"` со строкой показывается готовым тостом; для
своих событий страница слушает `window.addEventListener('onebase:<имя>', …)`.

**Как попробовать.** Запустите пример `examples/crm`, откройте интерфейс в двух
окнах/вкладках и выполните обработку **«Тест real-time уведомления»** — тост
всплывёт в подписанных окнах/вкладках. (HTTP-сервис `POST /hs/notify/broadcast`
делает то же, но, как все
`/hs/*`, требует включить «Разрешить сетевые операции».)

## Телефония / CTI
<!-- status: testing -->
<!-- date: 2026-06-24 -->
<!-- issue: 75 -->

OneBase может выступать CRM-бэкендом для колл-центра: принимать события звонков через
HTTP-сервис, находить клиента по номеру, создавать/обновлять документ `Звонок`, показывать
оператору real-time скрин-поп и запускать click-to-call через Asterisk ARI. Эталонная
конфигурация `examples/callcenter` содержит справочники клиентов/операторов/очередей,
документ звонка, панель оператора, общий модуль `Телефония` и демо-обработки заполнения
данных и теста входящего звонка. Реализован режим «монитор»: аудио остаётся на стороне
АТС/телефона, OneBase ведёт события и интерфейс оператора.

**Как настроить.** В конфигурации опишите HTTP-сервис `services/telephony.yaml`,
секрет webhook-а (`TELEPHONY_SECRET`) и константы подключения к Asterisk ARI. Для
операторов задайте роль и логины OneBase, на которые отправляются уведомления.

**Как попробовать.** Запустите `examples/callcenter`, выполните обработку тестового
входящего звонка — у оператора появится всплывашка, а в панели телефонии обновится журнал.

## Создать копированием (F9)
<!-- status: testing -->
<!-- date: 2026-08-12 -->
<!-- issue: 762 -->

Новая запись, похожая на существующую, больше не набирается заново: выберите
строку списка и нажмите **F9** (или «⚙ Действия ▾ → Скопировать», или кнопку
**«Скопировать»** на карточке) — откроется форма создания, заполненная
значениями выбранной записи вместе со строками табличных частей. Правите что
нужно и записываете; до нажатия «Записать» копии в базе нет.

Копия получает заново то, что платформа выдаёт сама: системные **Номер**
(его присваивает запись) и **Дата** документа — новый документ оформляется
текущим числом, как и при обычном создании. Остальные реквизиты типа `date`
(срок оплаты, дедлайн, дата отгрузки и т. п.) переносятся из источника.
Реквизиты справочника, включая `Код`, переносятся как есть: их платформа не
генерирует. Копия группы остаётся группой и в той же родительской группе.

В управляемой форме размещённые редактируемые поля и табличные части можно
изменить перед записью. Неразмещённые и доступные только для чтения значения
не доверяются скрытым данным браузера: при записи сервер повторно проверяет
доступ к источнику и переносит их из канонического состояния записи. Поэтому
неполная компоновка формы не обнуляет реквизиты или служебные табличные части.

Поле типа `image` переносит ссылку на тот же неизменяемый blob (UUID), не
создавая второй бинарный файл; сборщик мусора учитывает ссылки обеих записей.
**Вложения не копируются**: они остаются у исходной записи, поскольку являются
отдельными файлами-владениями, а не реквизитами шапки или табличной части.

Что вам не видно, то не копируется: реквизиты, закрытые **маской
персональных данных**, в форму копии не попадают (иначе в новую запись
уехала бы строка-маска), а копировать строку, скрытую **строковыми
ограничениями доступа** или серверным `ПриЧтенииНаСервере`, нельзя вовсе.
Доступ повторно проверяется при сохранении управляемой формы, поэтому скрытый
идентификатор источника нельзя использовать как обход RLS. Пункт меню и F9
показываются только при праве записи на объект.

**Как попробовать.** Откройте любой список, встаньте на строку и нажмите F9.

## Левая панель меню скрывается на широком экране
<!-- status: testing -->
<!-- date: 2026-08-25 -->
<!-- issue: 1122 -->

Кнопка «☰» слева в топбаре теперь видна не только на телефоне. На широком
экране она схлопывает левую панель объектов, отдавая её 210 пикселей списку или
форме, — удобно, когда нужные объекты уже открыты и навигация идёт по вкладкам.
Полоса подсистем остаётся на месте: свёрнутое меню не запирает вас в текущем
разделе.

Выбор запоминается браузером и переживает переходы между страницами, в том
числе в режиме открытия форм «Страницы», где каждый переход — полная
перезагрузка. На узком экране кнопка работает как раньше: выдвигает меню
шторкой поверх содержимого.

**Как попробовать.** Нажмите «☰» слева в шапке и походите по разделам.
Повторное нажатие возвращает панель.

<!-- ═══════════════════════ Торговое оборудование ═══════════════════════ -->

## Подключаемое торговое оборудование
<!-- status: stable -->
<!-- date: 2026-06-20 -->

Из DSL можно подключать торговое оборудование через единый объект
`ПодключитьОборудование`: чековый принтер, дисплей покупателя, весы, эквайринг, сканер
штрихкода и фискальный регистратор. Драйверы работают через TCP/serial или через
локальный `device-agent`; для тестов есть эмуляторы без физического железа.

**Как настроить.** В конфигурации заведите справочник подключаемого оборудования
(пример — `examples/trade/catalogs/подключаемоеоборудование.yaml`) с типом устройства,
драйвером и параметрами порта/протокола. В DSL вызовите
`ПодключитьОборудование("escpos_tcp", Новый Структура("Порт", "..."))` или другой драйвер.

**Как попробовать.** В `examples/trade` запустите обработки печати чека, взвешивания
товара или оплаты картой на TCP-эмуляторе устройства.

## Фискальная ККТ 54-ФЗ
<!-- status: stable -->
<!-- date: 2026-06-20 -->

Драйвер `atol_kkt` отправляет фискальный чек в локальный сервис «Драйвер ККТ АТОЛ v10»,
ожидает результат и возвращает фискальные реквизиты: ФН, ФД, ФП и QR. Чек собирается из
структуры DSL с позициями, оплатами, СНО, НДС, признаком способа и предмета расчёта.
Физическая регистрация ККТ, ФН и договор с ОФД остаются внешними юридическими шагами.

**Как настроить.** Запустите сервис АТОЛ на кассовом месте, добавьте оборудование с
драйвером `atol_kkt` и портом/URL сервиса. В товарных справочниках заполните НДС и
признаки ФФД.

**Как попробовать.** В `examples/trade` выполните обработку **«Пробить чек»** или нажмите
кнопку фискализации в РМК (`/ui/pos`) — на эмуляторе АТОЛ вернутся ФД/ФП/ФН.

## РМК и локальный device-agent
<!-- status: stable -->
<!-- date: 2026-06-20 -->

Рабочее место кассира в основном UI умеет обращаться из браузера к локальному
`onebase device-agent` на машине кассира: печать, открытие денежного ящика, дисплей,
весы, эквайринг, фискализация и поток событий сканера по SSE. Адрес и токен агента
хранятся в `localStorage`, поэтому у каждого рабочего места свои настройки, а сервер
OneBase не должен иметь сетевой доступ к кассовому железу.

**Как настроить.** Запустите `onebase device-agent` на кассовой машине, в пользовательском
режиме откройте настройки агента и укажите URL/token локального агента.

**Как попробовать.** Откройте `/ui/pos`, проверьте связь с агентом, взвесьте товар,
подключите сканер или пробейте тестовый фискальный чек.

## Декларативные драйверы оборудования
<!-- status: stable -->
<!-- date: 2026-06-20 -->

Простые устройства можно подключать без нового Go-драйвера: `scripted` описывает весы
через hex-запрос и regexp ответа, `scripted_pay` — терминал эквайринга через текстовый
протокол, `scripted_display` — дисплей покупателя через hex-шаблоны строк и кодировку
`cp866`/`utf8`. CP866 также доступна для ESC/POS-принтера и стандартного дисплея.

**Как настроить.** В карточке оборудования укажите драйвер `scripted`, `scripted_pay`
или `scripted_display` и параметры протокола: порт, запрос, шаблон разбора, шаблоны строк,
ширину и кодировку.

**Как попробовать.** В `examples/trade` настройте весы или дисплей через справочник
оборудования и вызовите существующие обработки без пересборки платформы.

<!-- ═══════════════════════ Данные и интеграции ═══════════════════════ -->

## Обмен данными между базами (планы обмена)
<!-- status: testing -->
<!-- date: 2026-07-14 -->
<!-- issue: 86 -->

Синхронизация данных между двумя базами OneBase файлами-пакетами: центр ↔ филиал,
периодическая консолидация. План обмена описывает состав (какие справочники и
документы синхронизируются) и узлы-участники. Изменения объектов из состава
регистрируются автоматически при записи; выгрузка собирает их в файл, загрузка на
приёмнике идемпотентна (повторная доставка не создаёт дублей). Встречная правка
одного объекта разрешается правилом: по времени, по приоритету узла или хуком.

Обмен доступен файлами (`.obx`) и **онлайн по сети** (HTTP с Bearer-токеном).
В интерфейсе администратора есть **монитор обмена** (Система → Обмен данными):
очередь и счётчики по узлам, кнопка «Синхронизировать» для онлайн-узлов.

**Как настроить.** Создайте `exchange/<имя>.yaml` с полями `content` (записи вида
`Справочник.X` / `Документ.Y` / `Константа.Z` / `РегистрСведений.W` — синхронизируются
и справочники/документы, и константы, и непериодические регистры сведений), `nodes`
(коды и названия узлов; для онлайна — `url`) и `conflict`
(`by_time` | `by_node_priority` | `hook`). На каждой базе задайте её узел (и, для
онлайна, общий токен из файла или `ONEBASE_EXCHANGE_TOKEN`):
`onebase exchange init --plan <имя> --node <код> --token-file <файл>`.

**Топология «звезда» (хаб-транзит).** Пара узлов по умолчанию равноправна. Для плана
с тремя и более узлами обязателен ровно один `role: hub`; остальные узлы становятся
спицами (`role: spoke`). Спицы (филиалы) обмениваются только с хабом (центром), а
хаб автоматически ретранслирует изменения между ними — не нужно настраивать обмен
каждого филиала с каждым.

**Перепроведение документов.** С `repost: true` в плане онлайн-приёмник (запущенный
`onebase run`) перепроводит пришедшие проведёнными документы — движения ложатся в
регистры приёмника. Без флага (и при файловой CLI-загрузке) документы приходят
непроведёнными.

**Как попробовать (файлами).** На базе-источнике измените объект из состава,
затем выгрузите: `onebase exchange dump --plan <имя> --to <узел> --out pkg.obx`;
на приёмнике загрузите: `onebase exchange load --in pkg.obx`. Повторная загрузка
безопасна (идемпотентно).

**Как попробовать (по сети).** Задайте узлам `url` и одинаковый токен,
поднимите обе базы `onebase run` и синхронизируйтесь одной командой:
`onebase exchange sync --plan <имя> --with <узел>` (отправит свои изменения и
заберёт чужие). Состояние очереди — `onebase exchange status --plan <имя>`.

Из встроенного языка — `ПланыОбмена.<План>.ВыгрузитьИзменения("узел")` и
`.ЗагрузитьПакет(Пакет)`.

## Перечисления и константы
<!-- status: stable -->
<!-- date: 2026-05-04 -->

Конфигурация поддерживает перечисления с фиксированными значениями и константы для
глобальных настроек базы. Значения перечислений используются в реквизитах, формах,
табличных частях, отчётах и DSL; константы доступны как централизованное место хранения
настроек организации, оборудования или интеграций.

**Как настроить.** Создайте `enums/<имя>.yaml` или `constants/<имя>.yaml`. Для enum-ов
можно задавать переводы значений через новый формат `values: [{name, titles}]`.

**Как попробовать.** Добавьте реквизит типа `enum:<Имя>` в документ и откройте форму —
поле отобразится выпадающим списком.

## Регистры сведений
<!-- status: stable -->
<!-- date: 2026-05-06 -->

Регистры сведений хранят периодические и непериодические факты: цены, курсы валют,
статусы, настройки по измерениям. Для периодических регистров доступен срез последних,
а списки регистров поддерживают отборы по измерениям и периоду.

**Как настроить.** Создайте `inforegs/<имя>.yaml`, опишите измерения, ресурсы,
реквизиты и периодичность.

**Как попробовать.** Откройте список регистра сведений, заполните несколько записей и
постройте запрос к виртуальной таблице `.СрезПоследних()`.

## Регистры накопления
<!-- status: stable -->
<!-- date: 2026-05-08 -->

Регистры накопления принимают движения документов и дают остатки/обороты по измерениям:
складские остатки, резервы, взаиморасчёты, денежные движения. Встроенный язык запросов
умеет обращаться к виртуальным таблицам `.Остатки()` и `.Обороты()`.

**Как настроить.** Создайте `registers/<имя>.yaml`, задайте измерения, ресурсы и движения
в модуле проведения документа (`.posting.os`).

**Как попробовать.** Проведите документ, который пишет движения, и откройте отчёт по
остаткам регистра.

## Регистр бухгалтерии и субконто
<!-- status: stable -->
<!-- date: 2026-06-08 -->

План счетов и бухгалтерский регистр позволяют писать проводки с дебетом/кредитом,
суммой и аналитиками-субконто. Виртуальные таблицы бухгалтерского регистра поддерживают
остатки и обороты с разворотом по субконто, что покрывает управленческий и бухгалтерский
учёт в типовых конфигурациях.

**Как настроить.** Опишите план счетов в `accounts/<имя>.yaml`, бухгалтерский регистр в
`accountregs/<имя>.yaml`, а проводки формируйте в модуле проведения документа.

**Как попробовать.** В `examples/accounting` проведите кассовый или товарный документ и
откройте отчёт по оборотно-сальдовой ведомости.

## Точные десятичные числа и деньги
<!-- status: stable -->
<!-- date: 2026-06-08 -->

Числа для учёта и денег хранятся и считаются как decimal, а не как двоичный `float64`.
Это убирает накопление копеечных ошибок в суммах документов, регистрах, отчётах, KPI и
печатных формах.

**Как попробовать.** Создайте документ с дробной ценой/количеством и сравните сумму в
документе, движениях регистра и отчёте — итог должен сходиться до копейки.

## HTTP-сервисы (публикация REST на DSL)
<!-- status: stable -->
<!-- date: 2026-06-18 -->

Аналог 1С «HTTP-сервис»: DSL-обработчик отдаёт произвольный ответ (`ОтветТекст`/`ОтветJSON`)
по пути `/hs/<корень>/...` — чтобы публиковать собственные REST-эндпоинты для внешних
систем. Есть авто-документация (OpenAPI/RapiDoc).

**Как настроить.** Опишите HTTP-сервис в конфигурации (корень пути + обработчик).

**Как попробовать.** Дёрните свой `/hs/<корень>/...` из браузера или `curl` и проверьте
ответ.

## Выполнение команд ОС (ВыполнитьКоманду)
<!-- status: stable -->
<!-- date: 2026-06-17 -->

Запуск внешнего процесса из DSL с ожиданием результата: `ВыполнитьКоманду(Команда,
Аргументы, Таймаут)` → объект с полями `КодВозврата`, `СтандартныйВывод`, `ОшибочныйВывод`,
`Завершилась`. Без shell, с обязательным таймаутом, обращения пишутся в аудит.

> ⚠️ Исполнение произвольного кода на сервере. Включайте только на доверенной/локальной
> базе.

**Как настроить.** Конфигуратор → **Меню ▾ → «Параметры базы»** → галочка **«Разрешить
выполнение команд ОС»**. По умолчанию и после восстановления из бэкапа — **выключено**.

**Как попробовать.** Включите флаг и вызовите из обработки простую команду
(например вывод версии стороннего CLI), затем выведите `СтандартныйВывод`.

## Объект Запрос и язык запросов
<!-- status: stable -->
<!-- date: 2026-07-08 -->

Встроенный русскоязычный язык запросов в стиле 1С: `ВЫБРАТЬ … ИЗ … ГДЕ … СГРУППИРОВАТЬ`,
виртуальные таблицы регистров `.Остатки()` / `.Обороты()` / `.СрезПоследних()` (для
бухрегистра — с разворотом по субконто). Ссылки в запросах без уточнения возвращаются
представлением: для справочников — `Наименование`, для документов — `Номер`.

Если нужна сама ссылка, выбирайте виртуальное поле `.Ссылка` и задавайте псевдоним:
`ВЫБРАТЬ Проект.Ссылка КАК ПроектСсылка ИЗ Документ.Приход`. Такая колонка приходит
DSL-ссылкой: `ТипЗнч()` называет её квалифицированно (`СправочникСсылка.Проект`),
работает `.УникальныйИдентификатор()`, её принимают методы менеджера
(`Удалить`, `ОтменитьПроведение`, `ПометитьНаУдаление`) и присваивание в ссылочный
реквизит. Печать и сравнение со строкой UUID при этом прежние. Менеджера у неё нет,
поэтому `ПолучитьОбъект()` вызывают у ссылки, полученной от менеджера:
`Справочники.Проект.НайтиПоИдентификатору(Стр.ПроектСсылка)` или
`Документы.Приход.НайтиПоИдентификатору(Стр.Ссылка)`.

Ссылкой колонка приходит, **пока в запросе один `ВЫБРАТЬ`**. Второй — `ОБЪЕДИНИТЬ`
или подзапрос, хотя бы в `ГДЕ`, — отключает приведение колонок результата целиком:
ссылка снова приходит строкой (`ТипЗнч()` отвечает `Строка`), и там же перестают
приводиться булево и дата. Соответствие «элемент выборки → колонка результата» в
таком запросе неоднозначно — имя колонки может прийти из другой ветки. Нужен тип —
разбейте на два запроса.

**Как попробовать.** Откройте конструктор запросов или консоль запросов (меню Система,
только admin) и соберите запрос к справочнику или регистру.

## JOIN в языке запросов
<!-- status: stable -->
<!-- date: 2026-05-20 -->

В запросах работают `ВНУТРЕННЕЕ` / `ЛЕВОЕ` / `ПРАВОЕ` / `ПОЛНОЕ СОЕДИНЕНИЕ … ПО` — для
нетривиальных отчётов (продажи по контрагентам с группировкой по менеджерам).

**Как попробовать.** Соберите отчёт с соединением справочника и регистра.

## HTTP-клиент в DSL
<!-- status: stable -->
<!-- date: 2026-05-22 -->

Объекты `HTTPСоединение` / `HTTPЗапрос` / `HTTPОтвет` с GET/POST/PUT/DELETE, заголовками,
телом и таймаутами — для интеграций с внешними API (курсы валют, маркетплейсы, банк-клиент,
Telegram).

**Как настроить.** Сетевые операции должны быть разрешены (см. «Сетевой предохранитель»).

**Как попробовать.** Запросите данные стороннего API и разберите JSON-ответ.

## REST API v2 (CRUD, отчёты, OpenAPI)
<!-- status: stable -->

Доступ к данным сущностей по REST — для доверенных интеграций и внешних клиентов внутри
контролируемого контура. `/api/v2` возвращает JSON envelope `{data, meta}`, поддерживает
CRUD справочников и документов, фильтры `filter[Поле]`, поиск `q`, `page/limit`,
проведение/отмену проведения документов, запуск отчётов в плоский JSON или
структурированную YAML-композицию (`composition=1`) и OpenAPI 3.0. Поверхность
использует session-аутентификацию или Bearer API-токены и RBAC
(`read/write/delete/post/unpost`, для отчётов — `run`).

**Как попробовать.** Откройте [`docs/rest-api-v2.md`](rest-api-v2.md), выполните curl-пример
списка справочника или отчёта и проверьте `/api/v2/openapi.json`.

## REST API v2: вложения (attachments)
<!-- status: testing -->
<!-- date: 2026-07-13 -->
<!-- issue: 315 -->

Загрузка, список, скачивание и удаление вложений к документам и справочникам теперь
доступны в `/api/v2` под Bearer-токеном, а не только по сессионной куке UI. Интеграции
работают с файлами по публичному контракту с теми же проверками владельца (RBAC +
построчный доступ), что и в интерфейсе. Эндпоинты:
`GET/POST /api/v2/{catalog|document}/{Имя}/{id}/attachments` (список и multipart-загрузка,
поле `file`), `GET /api/v2/attachments/{aid}` (скачать), `DELETE /api/v2/attachments/{aid}`
(удалить) — описаны в `openapi.json`. Серверная проверка `attachments.allowed_types`
(по расширению файла) теперь применяется и к REST-, и к UI-загрузке.

**Как настроить.** Блок `attachments:` в `config/app.yaml` (`max_file_size_mb`,
`allowed_types`); доступ — по Bearer API-токену с правами на сущность-владельца.

**Как попробовать.** Bearer-токеном загрузите файл
`POST /api/v2/catalog/<Справочник>/<id>/attachments` (multipart, поле `file`), затем
получите список тем же URL и скачайте бинарник по `GET /api/v2/attachments/<aid>`.

## Webhook-уведомления
<!-- status: stable -->

Отправка webhook-ов на события записи и проведения объектов.

**Как настроить.** Описать webhook на событие сущности; сетевые операции должны быть
разрешены (см. «Сетевой предохранитель»). Доставка идёт через ограниченную очередь
(256 вызовов, 4 worker-а): переполнение фиксируется как ошибка, а при штатной
остановке очередь дренируется. Полные endpoint URL не сохраняются в журнале,
чтобы токены из пути или query-параметров не попадали в БД.

**Как попробовать.** Настройте webhook на тестовый приёмник и проведите документ.

## Email-уведомления (SMTP)
<!-- status: stable -->
<!-- date: 2026-05-25 -->

Отправка писем из DSL через SMTP — уведомления, отчёты на почту, рассылки.

**Как настроить.** Блок `email:` в `config/app.yaml` (хост/порт/логин, пароль через
`${env:…}`); сетевые операции должны быть разрешены.

**Как попробовать.** Отправьте письмо из обработки и проверьте доставку.

## JSON в DSL
<!-- status: stable -->
<!-- date: 2026-05-05 -->

Чтение и запись JSON (`ПрочитатьJSON` / `ЗаписатьJSON`), работа с вложенными структурами —
для интеграций и хранения настроек.

**Как попробовать.** Разберите JSON-строку и обратитесь к её полям.

## Коллекции и ТаблицаЗначений в DSL
<!-- status: stable -->
<!-- date: 2026-05-05 -->

Встроенный язык поддерживает 1С-подобные коллекции: `Массив`, `Структура`,
`Соответствие` и `ТаблицаЗначений`. Они работают в обработках, формах, HTTP-сервисах,
отчётах и интеграциях; таблицу значений можно заполнять, выгружать по колонкам и передавать
между процедурами.

У `Массива` есть `Сортировать()` — по возрастанию, `Сортировать("Убыв")` — по убыванию;
направление задаётся тем же словом, что у `ТаблицаЗначений.Сортировать`. Числа
сравниваются как числа, даты как даты, остальное — строкой.

**Как попробовать.** В обработке создайте `Новый Массив`, `Новый Структура` или
`Новый ТаблицаЗначений`, заполните данные и выведите результат через `Сообщить`.

## Исключения и транзакции в DSL
<!-- status: stable -->
<!-- date: 2026-05-10 -->

Прикладный код может явно обрабатывать ошибки через `Попытка / Исключение /
КонецПопытки`, вызывать пользовательские ошибки через `ВызватьИсключение` и выполнять
несколько операций в транзакции с откатом при ошибке. Это нужно для безопасных импортов,
обработок проведения и пакетных операций.

**Как попробовать.** В обработке откройте транзакцию, создайте несколько записей и
вызовите `ВызватьИсключение` внутри `Попытка` — изменения должны откатиться.

## Индексный доступ к реквизитам объектов
<!-- status: testing -->
<!-- date: 2026-09-03 -->
<!-- issue: 1275 -->

Реквизит, имя которого вычисляется во время выполнения, можно читать и записывать через
`Объект[ИмяРеквизита]`. Индексный доступ работает у записываемых объектов справочников и
документов, у объекта управляемой формы, а также у `this` / `ЭтотОбъект` в обработчиках
`ПриЗаписи` и `ОбработкаПроведения`. Имя сопоставляется без учёта регистра, но должно быть
строкой и обозначать объявленный реквизит; иначе выполнение завершается понятной ошибкой.
Обычный доступ через точку (`Объект.Реквизит`) работает как прежде.

**Как попробовать.** В обработке создайте объект, сохраните имя поля в переменной и
обратитесь к нему по индексу: `Имя = "Организация"; Клиент[Имя] = "ООО Ромашка";
Сообщить(Клиент[Имя]);`, затем запишите объект.

## Расширения DSL для реальных конфигураций
<!-- status: stable -->
<!-- date: 2026-06-25 -->

Язык стал ближе к привычной 1С-модели: `ИначеЕсли`, тернарный оператор `?(...)`,
compound assignment (`+=`, `-=`, `*=`, `/=`), расширенные функции строк и дат,
`Пустая`/`ЗначениеЗаполнено`, `Формат`, `ТипЗнч`/`Тип`, общие модули
`src/*.module.os`, создание документов из DSL (`Документы.X.Создать()`) и write-hooks.
Главный оставшийся зазор этой группы — автоматический доступ к полям ссылок через
`X.Y` без ручного `ЗначениеРеквизитаОбъекта(...)`.

**Как попробовать.** Создайте общий модуль `src/Расчёты.module.os` с экспортной функцией
и вызовите её из модуля проведения документа или обработки.

## Поиск совпадения по реквизиту (safe-match 0/1/несколько)
<!-- status: testing -->
<!-- date: 2026-06-23 -->
<!-- issue: 178 -->

Штатный метод для импорта, синхронизации и дедупликации, где важно различать
«создать / обновить / конфликт»: `Справочники.Имя.ПроверитьСовпадениеПоРеквизиту(Реквизит, Значение)`
(и так же `Документы.Имя.…`). Возвращает Структуру с полями **Статус**
(`"НеНайдено"` / `"НайденаОдна"` / `"НайденоНесколько"`), **Ссылка** (заполнена
только при ровно одном совпадении — сразу `.ПолучитьОбъект()` для обновления) и
**Количество** (точное число совпадений). Снимает с прикладного кода ручной
подсчёт совпадений и обработку случая дублей.

**Как попробовать.** В trade есть демо-обработка **«Синхронизация контрагентов по
ИНН»**: `onebase procrun --project examples/trade --sqlite base.db --proc СинхронизацияКонтрагентов --set 'ФайлJSON=[{"ИНН":"7701234567","Наименование":"ООО Ромашка"}]'`.
Повторный запуск с тем же ИНН обновит запись, а при дубле ИНН в базе — сообщит о конфликте.

## Вложения к документам
<!-- status: stable -->
<!-- date: 2026-05-28 -->

К документам и элементам справочников можно прикреплять файлы (скан договора, фото товара)
перетаскиванием на форму.

**Как настроить.** Блок `attachments:` в `config/app.yaml` (максимальный размер,
допустимые типы, место хранения).

**Как попробовать.** Перетащите файл на форму документа и сохраните.

## Импорт конфигурации из 1С
<!-- status: stable -->

Импорт XML-выгрузки конфигурации 1С:Предприятие в проект OneBase (через собственный парсер),
с отчётом о конвертации.

**Как попробовать.** `onebase convert --from 1c-xml --dir ./export --out ./my-project`.

## Обмен управляемыми формами 1С
<!-- status: stable -->
<!-- date: 2026-05-24 -->

Конвертация управляемых форм 1С ↔ OneBase через промежуточное представление.

**Как попробовать.** `onebase forms convert-from … / convert-to … / validate …`.

## ВызватьИсключение без скобок
<!-- status: stable -->
<!-- date: 2026-08-11 -->
<!-- issue: 697 -->

`ВызватьИсключение` теперь пишется как оператор, а не только как вызов функции:

```
Попытка
    ВызватьИсключение "Сначала отмените проведение";
Исключение
    Сообщить(ОписаниеОшибки());
КонецПопытки;
```

Прежняя форма со скобками — `ВызватьИсключение("текст")` — продолжает работать,
переписывать модули не нужно. Смысл один и тот же: исключение возбуждается и
ловится `Попыткой`, а в хуках записи и проведения отменяет операцию.

## Представление объекта своим реквизитом
<!-- status: testing -->
<!-- date: 2026-08-15 -->
<!-- issue: 846 -->

Подпись объекта — то, что видно вместо UUID в списках, в подборе ссылок, в
глобальном поиске, в REST и в DSL — по умолчанию берётся по имени реквизита:
сначала «Наименование», а «Код» — последним. Теперь её можно назначить явно: в
справочниках, где позицию узнают по артикулу, сотрудника — по табельному
номеру, а договор — по шифру, показывать наименование бесполезно.

**Как настроить.** В YAML справочника или документа добавьте ключ
`presentation`:

```yaml
name: Номенклатура
presentation: Артикул                    # одно поле
# presentation: [Артикул, Наименование]  # или список: запасной вариант
```

Список задаёт порядок предпочтения: подписью станет первый непустой реквизит,
поэтому позиция без артикула покажет наименование. Реквизит должен быть
строковым и существовать в шапке — опечатку отклонит `onebase check`. Без
ключа поведение прежнее, ничего перенастраивать не нужно.

**Как попробовать.** Допишите `presentation` в справочник, выполните
`onebase check --project <dir>` и откройте список — подпись сменится сразу,
в том числе в пикере ссылок у документов. В глобальном поиске заголовки
хранятся в индексе, поэтому для уже наполненной базы выполните
`onebase reindex --entity <Имя>`.

## Маска ввода в управляемой форме
<!-- status: testing -->
<!-- date: 2026-08-16 -->
<!-- issue: 763 -->

Поле формы теперь умеет **шаблон ввода**: набираете `123456` — в поле появляется
`12.34.56`, разделители ставятся сами. Раньше ключ `mask` был регулярным
выражением и только проверял значение, а написанный в нём шаблон `00.00.00`
отвергал ровно тот формат, ради которого его писали.

**Как настроить.** На элементе `kind: ПолеВвода` управляемой формы:

```yaml
- kind: ПолеВвода
  name: ПолеШифр
  data_path: Объект.Шифр
  input_mask: "00.00.00"                # шаблон набора
  mask: '\d{2}\.\d{2}\.\d{2}'         # проверка при отправке (необязательно)
  hint: "6 цифр в формате 12.34.56"
```

Заполнители: `0` — цифра, `X` — буква, `*` — цифра или буква; остальные символы
литеральные и подставляются сами. Работают и телефонные шаблоны:
`"+7 (000) 000-00-00"`. Для цифровых шаблонов поле само просит у мобильной
клавиатуры цифровой режим.

Ключи независимы: `input_mask` помогает набрать, `mask` не пускает неверное.
Оба проверяет `onebase check` — он же ловит старую ошибку «шаблон записан в
`mask`» и обратную «регулярное выражение записано в `input_mask`».

**Как попробовать.** Допишите `input_mask` в форму, откройте карточку и
наберите цифры подряд — разделители появятся сами.

<!-- ═══════════════════ Конфигуратор и разработка ═══════════════════ -->

## Кэш ответов HTTP-сервисов
<!-- status: testing -->
<!-- date: 2026-08-17 -->

Публичный сервис может отдавать готовый ответ вместо повторной сборки:

```yaml
name: Сайт
root_url: site
auth: none
cache:
  ttl: 60             # секунды; без блока или 0 — кэш выключен
  vary: [query, host] # что входит в ключ помимо пути (ещё бывает lang)
  public: true        # отдавать наружу Cache-Control: public, max-age=60
  max_body: 1048576   # ответы крупнее не кэшируются
```

Повторный запрос не запускает обработчик и не ходит в базу. Ответ получает
`ETag`, поэтому браузер и поисковый робот при следующем заходе получат короткий
`304 Not Modified` вместо тела. Параллельные запросы на «остывшую» страницу
собирают её один раз, а не всей толпой.

При изменении данных кэш сбрасывается из модуля — обычно в `ПриЗаписи`
справочника, чьи данные попали на страницу:

```
СброситьКэшСервисов("Сайт");   // или без аргумента — весь кэш
```

Горячая перезагрузка конфигурации (`--watch`) сбрасывает кэш сама.

Ограничения намеренные: кэшируются только `GET`/`HEAD`, только успешные ответы
без `Set-Cookie` и только у сервисов с `auth: none` — ответ, собранный под
правами одного пользователя, не должен достаться другому. `onebase check`
сообщит об ошибке, если задать `cache` сервису с авторизацией. Кэш живёт в
памяти процесса.

Работу видно в `/metrics`: `onebase_http_cache_hits_total`,
`..._misses_total`, `..._evictions_total`, `onebase_http_cache_bytes`.

**Как попробовать.** Добавьте блок `cache` в сервис, отдающий текущее время,
и вызовите его дважды — время не изменится до истечения `ttl`.

## Публичные ссылки на файлы
<!-- status: testing -->
<!-- date: 2026-08-17 -->

Вложение из базы можно отдать наружу — контрагенту без учётной записи, в письмо
или на страницу сайта:

```
Ссылка = ОпубликоватьФайл(ИдВложения);          // "/pub/nR3s…"

Опции = Новый Структура;
Опции.Вставить("ДействуетДо", '20260901');
Опции.Вставить("Имя", "прайс-август.pdf");
Ссылка = ОпубликоватьФайл(ИдВложения, Опции);

Ссылка = СсылкаНаФайл(ИдВложения);              // Неопределено, если не опубликован
СнятьПубликациюФайла(ИдВложения);               // адрес сразу перестаёт работать
```

Картинки из полей карточки (тип `image`) публикуются своей парой функций —
`ОпубликоватьКартинку`, `СсылкаНаКартинку`, `СнятьПубликациюКартинки`:

```
Ссылка = ОпубликоватьКартинку(Товар.Фото);      // "/pub/…", тот же адрес и правила
```

Функции разные, потому что вложения и картинки платформа хранит по-разному, а
идентификаторы из разных хранилищ путать нельзя — перепутанный молча дал бы
«файл не найден».

Публикация — **явное действие**, а не флаг у файла: адрес содержит случайный
токен, который невозможно подобрать, и знание адреса и есть право на файл.
Отзыв ломает ссылку немедленно, срок действия можно ограничить датой. Каждая
публикация и снятие попадают в журнал аудита — видно, кто и когда открыл файл
наружу.

Отдача поддерживает докачку (`Range`), условные запросы и кэш браузера. Тело
может храниться локально, но перед каждым повторным использованием браузер
ревалидирует ETag: это позволяет вернуть 304 без передачи файла и одновременно
не даёт fresh-копии пережить отзыв capability-ссылки. Старая опция `КэшСекунд`
принимается для совместимости, но больше не создаёт окно без ревалидации.
Картинки, PDF и видео открываются прямо в браузере; HTML и SVG принудительно
скачиваются файлом — иначе они исполнились бы как страница на домене платформы.

**Как попробовать.** Присоедините файл к элементу справочника, в обработке
вызовите `ОпубликоватьФайл` и откройте полученный путь в браузере в режиме
инкогнито.

## Рабочий стол «Управление сайтом» (examples/cms)

<!-- status: testing -->
<!-- date: 2026-08-21 -->

Главная страница базы в примере «Сайт» стала рабочим столом: кнопки на живой
сайт (главная, каталог, новости, `sitemap.xml`, `robots.txt`), быстрое
добавление материала, счётчик необработанных заявок и списки свежих заявок и
публикаций. Сайт и управление живут в одной базе и на одном порту, поэтому
посмотреть результат правки можно в один клик.

Ссылки наружу открываются в новой вкладке — у виджетов `type: actions` появился
признак `new_tab: true`. Он нужен любой ссылке за пределы интерфейса: открытая
в текущей вкладке, она оставляет пользователя без пути назад.

**Как попробовать.**

```bash
onebase migrate --project examples/cms --sqlite cms.db
onebase run --project examples/cms --sqlite cms.db --port 8080
```

Откройте `http://localhost:8080/ui`. База без демо-контента при этом
показывает на сайте страницу «Сайт готов к наполнению» с подсказкой, а не 404:
пустая база — не ошибка.

Сайт отвечает на тот адрес, который записан в поле «Домен» его карточки. Там
можно написать `*` — «любой адрес»: у одной базы их обычно несколько и все
верные (лаунчер открывает её по `127.0.0.1`, руками набирают `localhost`, с
соседнего ноутбука заходят по имени машины). `localhost`, `127.0.0.1` и `::1`
считаются одним адресом. Конкретный домен приоритетнее `*`, поэтому база с
несколькими сайтами продолжает разделять их по адресам, а адрес, не привязанный
ни к одному сайту, получает 404 «Здесь пока нет сайта» — не приглашение
наполнить базу.

## Корзина и заказ с сайта (examples/cms)

<!-- status: testing -->
<!-- date: 2026-08-25 -->

Посетитель собирает корзину и оформляет заказ без регистрации, а заказ приезжает
в базу обычным документом «Заказ с сайта» — с нумерацией, списком, правами и
печатью. Кнопка «В корзину» стоит в карточке товара, ссылка на корзину — в шапке
сайта, на рабочем столе появились счётчик новых заказов и список последних.

Корзина — свойство сайта, а не сборки: реквизит «Принимать заказы с сайта»
включает её там, где она нужна, и по умолчанию снят. У сайта без приёма заказов
кнопки и ссылки на корзину нет, а адреса `/hs/shop` отвечают 404 — выключено
по-настоящему, а не на вид.

Оплаты, фискализации и личного кабинета с паролем здесь нет намеренно: это
витрина плюс заказ, а не интернет-магазин из коробки. Оплата — счёт или ссылка
от банка, которую выставляет менеджер.

Цену определяет база: из браузера принимаются только товар и количество, иначе
«купить за 1 ₽» делается правкой скрытого поля. Страница заказа открывается по
неподбираемой ссылке с UUID документа (по номеру — не открывается), повторная
отправка формы ведёт на уже созданный заказ, а не создаёт второй. Письмо о
заказе отправляется после записи документа и не может её отменить: база без
настроенного SMTP — обычное состояние стенда.

**Как попробовать.** Заполните демо-базу (демо-сайту приём заказов включается
сразу), загрузите каталог выгрузкой и откройте карточку любого товара: «В корзину» → «Корзина» → «Оформить заказ».
После оформления адрес страницы заказа можно сохранить — по нему виден статус.
Сам заказ появится в разделе «Сайт» → «Заказы с сайта».

## Главная демо-сайта: витрина разделов и новости (examples/cms)

<!-- status: testing -->
<!-- date: 2026-08-24 -->

Корень сайта примера показывает текст страницы «Главная», под ним восемь
разделов каталога и три свежие новости. Раньше там были заголовок и один абзац:
посетителю некуда идти, кроме меню, — а на демонстрации это первое, что видит
заказчик.

У раздела каталога обложка берётся из первой позиции внутри: разделы приходят
из выгрузки пустыми, и витрина иначе встречает сеткой серых плашек. Вместо
подписи «Раздел каталога» показывается число позиций — «6 позиций», у пустого
«Пока пусто».

Демо-тексты страниц («Главная», «О компании», «Контакты») переписаны так, чтобы
на них можно было показывать: чем занимается компания, как работает, куда
писать. Повторное «Заполнить демо-контент сайта» обновит их и в уже созданных
базах — но только если текст не правили руками.

## Оформление демо-сайта: витрина карточками (examples/cms)

<!-- status: testing -->
<!-- date: 2026-08-23 -->

Сайт примера выглядит магазином, а не отладочной страницей: белая шапка с меню и
поиском, витрина карточками (фото, название, крупная цена), карточка товара в
две колонки, подвал и адаптив под телефон. Цена показывается как `187 ₽` —
ровные копейки не пишутся, потому что `187.00 RUB` читается как строка выгрузки,
а не ценник.

Оформление остаётся **данными**: разметка лежит в справочнике «Шаблоны сайта»,
палитра и дополнительный CSS — в «Стилях сайта», и правятся они полем кода в
карточке. Внешних зависимостей нет ни одной: политика страницы разрешает стили
только со своего адреса, поэтому ни CDN, ни шрифтов со стороны, ни JS.

Поставляемый макет обновляется по номеру версии: демо-базы прежних сборок
получают новую вёрстку при повторном запуске «Заполнить демо-контент сайта»,
а макет, у которого версию подняли руками, обработка не трогает.

## Витрина каталога в примере «Сайт» (examples/cms)

<!-- status: testing -->
<!-- date: 2026-08-19 -->
<!-- issue: 1014 -->

Каталог, перенесённый YML-выгрузкой, теперь виден посетителю, а не только в
интерфейсе платформы:

| адрес | что показывает |
|---|---|
| `/catalog` | разделы верхнего уровня |
| `/catalog/<слаг раздела>` | подразделы и товары раздела |
| `/product/<слаг товара>` | карточку: артикул, цена, картинка, описание |
| `/catalog?group=<код>` | `301` на новый адрес раздела — старые ссылки не теряются |

**Как попробовать.** Заполните демо-базу и загрузите выгрузку:

```bash
onebase migrate --project examples/cms --sqlite cms.db
onebase procrun --project examples/cms --sqlite cms.db --proc ЗаполнитьТестовуюБазу
onebase query --project examples/cms --sqlite cms.db --json \
  'ВЫБРАТЬ Ссылка, Наименование ИЗ Справочник.Сайты'
onebase procrun --project examples/cms --sqlite cms.db --proc ИмпортИзYML \
  --set Сайт=<UUID из предыдущей команды> \
  --file Файл=examples/cms/demo/catalog.yml
onebase run --project examples/cms --sqlite cms.db --port 8080
```

Дальше откройте `http://localhost:8080/hs/site/catalog` — раздел «Каталог» есть
и в меню сайта. Повторный импорт обновляет позиции по внешнему коду и не плодит
дубли, поэтому выгрузку можно перезапускать сколько угодно.

Картинки выгрузки при импорте скачиваются в медиатеку и отдаются с адреса
самого сайта (`/pub/<токен>`): страница отдаётся с политикой «изображения только
со своего адреса», поэтому чужой адрес из выгрузки браузер не показал бы, а
каталог заодно перестаёт зависеть от сервера, с которого переехал. Нужен
включённый предохранитель сети (`onebase settings set net.enabled вкл`) и
заполненный параметр обработки **«Домен, с которого качать картинки»**: адреса
берутся из чужого файла, поэтому домен называет оператор, а не выгрузка —
у URL должен точно совпасть разрешённый `authority` (хост и эффективный порт).
После DNS отвергается весь ответ, если хотя бы один IPv4/IPv6 относится к
special-use диапазону (в том числе private, loopback, link-local и CGNAT),
соединение открывается с проверенным числовым адресом без повторного DNS, а
каждый redirect снова проверяется по той же границе. Поэтому подмена DNS между
проверкой и соединением и перенаправление во внутреннюю сеть закрыты.

Формат проверяется по сигнатуре файла (PNG, JPEG, GIF, WEBP), а не по заголовку
ответа. Составной `UNIQUE (Сайт, ВнешнийКлюч)` не даёт параллельным импортам
создать две записи медиатеки: уникальный claim, метаданные принадлежащего ему
blob, активация и публикация фиксируются одной DB-транзакцией. В top-level режиме
helper работает как create-or-read: precheck идёт до `Begin`, а проигравший INSERT
делает до 200 проверок с паузами 50 мс в пределах execution deadline (DB-вызовы
могут добавить время). Внутри уже открытой транзакции helper не делает lookup и
работает только на создание: existing/conflict возвращается caller-у. Caller
должен искать до `Begin` либо повторить весь workflow с новым snapshot, потому
что SQLite не обновляет snapshot после rollback savepoint. Второй claim/blob не
создаётся. Для содержимого в disk/S3 rollback использует best-effort компенсацию,
а crash или недоступность могут потребовать операторской cleanup/reconciliation.
Отчёт импорта
называет, сколько позиций оказалось в базе, сколько картинок загружено, сколько
пропущено по границе источника и сколько не удалось — с первой причиной.

Макет страницы, CSS и разметка блоков правятся полем кода с подсветкой прямо в
карточке справочника: у «Шаблонов сайта», «Стилей сайта» и «Блоков» появились
управляемые формы. Демо-база заводит шаблон «Базовый» с макетом по умолчанию —
вид сайта тот же, но макет теперь редактируется в интерфейсе.

Куда ведёт адрес, решает справочник «Маршруты URL» (шаблон → тип контента), а не
код модуля: чтобы новости открывались по `/stati/<слаг>`, достаточно завести
правило. Пустой справочник сайт не ломает — действуют правила по умолчанию.

Адреса витрины, канонические ссылки, карта сайта и меню собираются из реквизита
сайта «Базовый префикс адреса»: за reverse proxy, отдающим сайт в корне домена,
его очищают — и ссылки становятся такими, какими их видит посетитель.

## Приём HTML-форм в HTTP-сервисах
<!-- status: testing -->
<!-- date: 2026-08-18 -->

Обработчик сервиса теперь умеет разбирать тело обычной формы:

```
Данные = Запрос.ФормаДанные();
Имя = Данные.Получить("имя");
Контакт = Данные.Получить("контакт");
```

Раньше это было невозможно: значения приходят в процентном кодировании
(`%D0%98%D0%B2%D0%B0%D0%BD`, `+` вместо пробела), а раскодировать их в модуле
было нечем. Метод возвращает `Соответствие` с уже нормальными значениями.

Пригодится для форм обратной связи на сайте, приёма вебхуков от платёжных
систем (многие шлют именно форму, а не JSON) и любых интеграций, где на другой
стороне обычный `<form method="post">`.

Понимаются оба вида формы — `application/x-www-form-urlencoded` и
`multipart/form-data`. Тело другого типа методом не разбирается: придёт ошибка
с указанием фактического типа, а для JSON есть `Запрос.ТелоJSON()`. Поле с
файлом в multipart-форме тоже даёт ошибку с именем поля — приём файлов через
`ФормаДанные` пока не поддерживается.

**Как попробовать.** В сервисе объявите POST-обработчик и выведите
`Данные.Получить("поле")` через `Сообщить`.

## Сжатие ответов и заголовки безопасности HTTP-сервисов
<!-- status: testing -->
<!-- date: 2026-08-17 -->

Ответы HTTP-сервисов теперь сжимаются gzip, а каждая публичная точка входа может
объявить собственные заголовки безопасности:

```yaml
name: Обмен
root_url: exchange
auth: none
compress: true            # по умолчанию: включено для auth: none, выключено для остальных
security_headers:
  csp: "default-src 'self'; img-src 'self' data:"
  frame_options: DENY
  referrer_policy: no-referrer
  hsts: 15552000
  extra:
    Permissions-Policy: "geolocation=(), camera=()"
```

`extra` — только для заголовков без выделенного поля. Заголовок, у которого поле
есть (`csp`, `frame_options`, `referrer_policy`, `hsts`), а также
`X-Content-Type-Options` и `Access-Control-*`, там задавать нельзя, и `onebase
check` на это ругается с указанием нужного поля: через `extra` проверки поля не
выполняются — `X-Frame-Options` уехал бы с любым значением, а
`Strict-Transport-Security` ушёл бы и по обычному HTTP, где браузер запомнит
домен как HTTPS-only.

Сжатие включается для текста, JSON и XML начиная с 1 КБ — выгрузка обмена едет
в несколько раз быстрее. Картинки, PDF и архивы не трогаются, клиент без
поддержки gzip получает обычный ответ.

Умолчание намеренно зависит от аутентификации: у анонимного сервиса секретов
нет, а вот сжатый ответ, содержащий одновременно секрет и подставленный извне
текст, теоретически позволяет угадать секрет по длине (атака BREACH). Поэтому
для сервисов с `auth`, отличным от `none`, сжатие включается только явным
`compress: true`.

Заголовок `X-Content-Type-Options: nosniff` ставится всегда. `hsts` применяется
только к запросам по HTTPS. Ошибочные ответы сервиса (404, 403) получают ту же
политику, что и успешные.

**Как попробовать.** Добавьте блок в `services/<имя>.yaml`, перезапустите сервер
и вызовите `curl -s -D - -H "Accept-Encoding: gzip" http://localhost:8080/hs/<root>/<путь> -o nul`.

## HTML-шаблоны в модулях
<!-- status: testing -->
<!-- date: 2026-08-17 -->

Письма, тела ответов HTTP-сервисов и любую другую разметку теперь можно собирать
шаблоном, а не склейкой строк:

```
Шаблон = Новый ШаблонHTML(
    "<h3>{{.Наименование}}</h3>
     <p>{{число .Цена 2}} ₽ · до {{дата .ДатаПоставки ""02.01.2006""}}</p>
     {{if .Активен}}<span>в наличии</span>{{end}}
     <ul>{{range .Позиции}}<li>{{.Имя}}</li>{{end}}</ul>
     <div>{{.Описание}}</div>");

Данные = Новый Структура;
Данные.Вставить("Наименование", Товар.Наименование);
Данные.Вставить("Цена", Товар.Цена);
Данные.Вставить("Описание", БезопасныйHTML(Товар.Описание));
HTML = Шаблон.Заполнить(Данные);
```

Главное отличие от конкатенации: значения экранируются **автоматически и по
месту вставки** — в тексте, в атрибуте и в адресе ссылки по-разному. Товар с
именем `<script>` больше не ломает письмо и не исполняется у получателя.
Готовую разметку (описание из richtext-поля, содержимое блока) вставляйте через
`БезопасныйHTML(...)`: функция чистит HTML тем же санитайзером, что и
richtext-поля, — теги остаются, `<script>`, `onerror=` и `javascript:` исчезают.

Синтаксис шаблона — как в Go (`{{.Поле}}`, `{{if}}`, `{{range}}`, `{{end}}`);
имена полей пишутся по-русски и регистр не важен. Отсутствующее поле даёт
пустую строку, а не ошибку. В шаблоне доступны `дата` и `число`.

**Как попробовать.** Любая обработка: соберите шаблон из примера и выведите
результат через `Сообщить`.

## Регулярные выражения в модулях
<!-- status: testing -->
<!-- date: 2026-08-17 -->

В модулях появился объект **Регекс** — разбор и проверка строк по шаблону вместо
цепочек `СтрНайти`/`Сред`. Пригодится для валидации телефона, ИНН и e-mail при
записи, разбора банковской выписки, выделения номера из текста письма.

```
Рег = Новый Регекс("^\+7\d{10}$");
Если Не Рег.Совпадает(Телефон) Тогда
    ВызватьИсключение "Телефон должен быть в формате +7XXXXXXXXXX";
КонецЕсли;

М = Новый Регекс("(?P<код>[A-Z]{2})-(\d+)").Найти("Заказ AB-1024 принят");
Сообщить(М.Значение);                        // AB-1024
Сообщить(М.Позиция);                         // 7 — как у СтрНайти, с единицы
Сообщить(М.ГруппыПоИмени.Получить("код"));   // AB

Телефон = Новый Регекс("^8(\d{10})$").Заменить("89161234567", "+7$1");
Части   = Новый Регекс("\s*[;,]\s*").Разделить("а, б;  в");
```

Методы: `Совпадает`, `Найти`, `НайтиВсе`, `Заменить`, `Разделить`; вторым
аргументом конструктора — флаги `i` (без учёта регистра), `m` (`^`/`$` на каждой
строке), `s` (точка захватывает перенос). Имена групп можно писать по-русски.
Функция `РегексЭкранировать(Строка)` собирает шаблон из пользовательского ввода
безопасно.

Синтаксис — RE2: обратных ссылок (`\1`) и опережающих проверок (`(?=...)`) нет.
Взамен поиск всегда работает за линейное время, поэтому подобранная строка не
может «подвесить» сервер — это важно там, где шаблон применяется к данным из
интернета.

**Как попробовать.** Любая обработка: вставьте пример выше и запустите.

## Веб-конфигуратор
<!-- status: stable -->
<!-- date: 2026-05-12 -->

Браузерный конфигуратор: дерево объектов конфигурации (справочники, документы, регистры,
отчёты, формы, обработки, страницы) с раскраской в стиле 1С, правка YAML-метаданных и
DSL-модулей в нескольких вкладках.

**Как настроить.** Вход под ролью **admin** (отдельная сессия конфигуратора, пароли bcrypt).

**Как попробовать.** Откройте конфигуратор и создайте/измените объект через дерево.

## Светлая тема редактора модулей
<!-- status: testing -->
<!-- date: 2026-08-07 -->

Редактор кода в конфигураторе умеет работать в светлой теме — как остальной
конфигуратор, конструктор форм и инструменты разработчика. Переключение затрагивает
все редакторы сразу: модули объектов и общие модули, модули проведения и менеджера,
запросы отчётов, обработки, страницы, YAML печатных форм и журналов, — а также блоки
кода, которые видны до открытия редактора, и просмотр содержимого файла в истории
конфигурации. Выбор запоминается в браузере, переживает перезагрузку и действует
заодно на редактор управляемых форм. Пока тема не выбрана явно, каждый экран
выглядит как раньше: конфигуратор тёмный, конструктор форм светлый. Панель отладки
и вывод команд остаются тёмными: это консоли, а не редакторы.

Попутно в конструкторе управляемых форм заработала подсветка синтаксиса: там не
подключался загрузчик редактора, и YAML с модулем правились в простом текстовом
поле — без подсветки, сворачивания блоков и направляющих отступов.

**Как попробовать.** Откройте конфигуратор, вкладка **«Дерево»** — в шапке появилась
кнопка **☀️ «Светлая тема»**. Нажмите и откройте любой модуль; чтобы вернуться,
нажмите **🌙 «Тёмная тема»**. Та же кнопка есть в шапке редактора управляемых форм.

## Дерево файлов конфигурации
<!-- status: testing -->
<!-- date: 2026-06-21 -->
<!-- issue: 132 -->

В конфигураторе есть файловое дерево проекта: YAML, DSL-модули, формы, макеты, локали и
служебные файлы можно открывать прямо в редакторе. Это помогает работать не только через
предметные формы объектов, но и с реальной структурой конфигурации на диске или в БД.

**Как попробовать.** В конфигураторе откройте вкладку дерева файлов, выберите `.os` или
`.yaml` файл и отредактируйте его в Monaco.

## История версий конфигурации
<!-- status: testing -->
<!-- date: 2026-06-24 -->
<!-- issue: 60 -->

Для конфигураций, хранящихся в БД, платформа создаёт снимки в `_config_versions`: при
сохранениях/удалениях из конфигуратора, batch-операциях и `onebase deploy --message`.
В UI истории можно посмотреть список версий, сравнить любые две версии, экспортировать
снимок в ZIP или config-only `.obz` и выполнить rollback — он создаёт новую версию, не
стирая историю.

**Как настроить.** Используйте базу с `--config-source database` или импортируйте файловую
конфигурацию в БД. Для релизного снимка передайте `--message` в `onebase deploy`.

**Как попробовать.** Измените объект в конфигураторе, откройте **Историю конфигурации**,
сравните новую версию с предыдущей и выполните rollback на тестовой базе.

## Проверка конфигурации
<!-- status: stable -->

Валидатор, который не только парсит, но **компилирует и исполняет** запросы модулей — ловит
`no such column`/`no such table` ещё до запуска. Основной инструмент отладки конфигураций.

**Как попробовать.** `onebase check --project <dir>` или кнопка «Проверить» в конфигураторе.

## OneBase lint
<!-- status: stable -->
<!-- date: 2026-06-25 -->

Поверх строгой проверки конфигурации есть lint-режим с предупреждениями, которые не ломают
обычный `check`: неизвестные YAML-ключи, неиспользуемые `Перем`, недостижимые процедуры и
объекты без прав в ролях. Это инструмент для гигиены поставляемых конфигураций и будущего
CI-gate.

`dsl.unknown-global-member` помечает обращение к полю имени, которого нет нигде: ни переменная,
ни `Перем` модуля, ни процедура, ни общий модуль, ни доступный в этом контексте глобал или
builtin. Такое
выражение вычисляется в `Неопределено` молча, а при конкатенации в строку попадает литерал
`<nil>` — ошибку видно уже по испорченным данным. Обычная проверка сюда не достаёт:
`dsl.unknown-function` смотрит на имя вызываемой функции, а здесь чтение поля. Голое чтение
неизвестного имени не помечается: набор контекстных переменных зависит от вида исполнения.

**Как попробовать.**

```bash
onebase lint --project examples/trade
onebase check --project examples/trade --lint
```

## Строгая область видимости DSL
<!-- status: stable -->
<!-- date: 2026-07-08 -->

По умолчанию OneBase сохраняет legacy-поведение DSL: вызванная процедура может
увидеть локальные переменные caller-процедуры через цепочку runtime scope. Это
совместимо со старыми конфигурациями, но легко создаёт скрытые зависимости между
helper-процедурами.

Новый opt-in режим разрывает эту цепочку: процедура видит свои параметры,
локальные переменные, `ЭтотОбъект` и root-переменные запуска (`extraVars`,
платформенные объекты, factories), а также `Перем` своего модуля через отдельный
module-scope одного запуска, но не локальные переменные caller-а. Блочные
конструкции (`Если`, циклы, `Попытка`) по-прежнему не создают отдельный scope:
область видимости остаётся процедурной.

```yaml
dsl:
  strict_lexical_scope: true
```

Перед включением прогоните `onebase check --lint`: предупреждение
`dsl.cross-scope-read` показывает чтения, которые в строгом режиме станут
`Неопределено`. Исправление обычно простое: передать значение параметром или
вынести его в явный объект/структуру.

Когда `dsl.strict_lexical_scope: true` уже включён, `onebase check` повышает
`dsl.cross-scope-read` до блокирующей ошибки. В legacy-режиме это остаётся только
lint-предупреждением при `onebase check --lint`.

## Веб-отладчик DSL
<!-- status: stable -->
<!-- date: 2026-05-11 -->

Глобальный отладчик встроенного языка: брейкпоинты в редакторе Monaco, пошаговое исполнение,
просмотр переменных. Для разработчиков, открывается из конфигуратора.

**Как попробовать.** Откройте консоль отладчика из конфигуратора, поставьте брейкпоинт и
запустите сессию.

## Условные точки останова
<!-- status: testing -->
<!-- date: 2026-08-12 -->

Точка останова может срабатывать не всегда, а только когда истинно ваше выражение.
Отладка цикла на тысячу строк больше не требует тысячу раз нажать «Продолжить»:
пишете `Сч = 743` или `Стр.Количество > 100` — и останов происходит ровно там.

Условие — обычное выражение DSL, вычисляется в окружении той самой строки: видны
локальные переменные, реквизиты объекта, функции модуля. Истинность понимается так
же, как в `Если`.

**Как попробовать.** В конфигураторе включите отладку и задайте условие любым из
трёх способов:

- **Alt+клик** по колонке слева от нужной строки в редакторе;
- карандаш ✎ в списке точек останова на вкладке **Точки ост.**;
- поля «Файл / Стр / Условие» там же — для точки, заданной вручную.

Условная точка отличается цветом маркера, а под ней в списке видно само условие и
сколько раз оно оказалось ложным. Пустое условие возвращает точку в обычную.

Ошибку в условии платформа не прячет: неразбираемое выражение отклоняется сразу
при установке, а если условие не вычислилось уже во время прогона (скажем,
деление на ноль), отладчик **останавливается** и пишет причину под точкой — иначе
точка молча не срабатывала бы, и ошибку вы искали бы в своём коде.

## Синтакс-помощник DSL
<!-- status: stable -->
<!-- date: 2026-06-10 -->

Единый справочник встроенного языка (`internal/dsl/langref`) питает автодополнение,
hover, подсказки параметров Monaco, окно синтакс-помощника в конфигураторе и `onebase
ai-guide`. В справке есть глобальные функции, методы объектов, конструкции языка и язык
запросов; тест полноты не даёт забыть описать новую встроенную функцию.

**Как попробовать.** В конфигураторе откройте редактор `.os`, нажмите **F1** или пункт
«Синтакс-помощник», начните вводить имя функции и посмотрите подсказку сигнатуры.

## Инструменты разработчика
<!-- status: stable -->
<!-- date: 2026-05-13 -->

Консоль кода (выполнить произвольный DSL), консоль запросов и визуальный конструктор
запросов (выделить текст → ПКМ → получить запрос).

**Как попробовать.** Меню **Система → Инструменты разработчика** (только admin).

## CLI и MCP-инструменты для ИИ-разработки
<!-- status: stable -->
<!-- date: 2026-06-24 -->

CLI отдаёт машинный контракт конфигурации для внешних ИИ-агентов и разработчиков.
`onebase describe` возвращает JSON `schemaVersion: 2`: объекты, формы, отчёты, виджеты,
журналы, подсистемы, страницы, HTTP-сервисы, регламентные задания, RBAC-роли, модули,
процедуры с `export`/параметрами/source location и справочник DSL из `langref`. Команда
`onebase examples [kind]` печатает короткие canonical YAML/DSL-фрагменты для типовых
объектов, чтобы агент не угадывал формат файлов. Дополнительно доступны headless-команды
`fmt`, `schema`, `query`, `eval`, `widget explain`, `report explain`, `impact`,
`refactor rename-object/rename-field`, а `onebase mcp` публикует их MCP-клиентам в
read-only режиме с отдельными флагами для write-инструментов.

**Как настроить.** Ничего включать не нужно; команды работают с теми же флагами базы,
что и остальные CLI-команды (`--project`, `--id`, `--sqlite`, `--db`).

**Как попробовать.**

```bash
onebase describe --project examples/minimal
onebase examples --list
onebase examples document
onebase examples query
onebase mcp --project /absolute/path/to/config
```

## Нагрузочное тестирование HTTP/API
<!-- status: stable -->
<!-- date: 2026-06-24 -->

В репозитории есть готовый стенд для проверки производительности onebase под
HTTP-нагрузкой: PostgreSQL + onebase + Prometheus + Grafana + k6-сценарии.
Сценарии покрывают чтение списков, CRUD справочника и создание/проведение документа
через REST. k6 умеет показывать live dashboard и сохранять HTML-отчёт с графиками,
чтобы удобно сравнивать p95/p99, ошибки, RPS и деградацию при росте данных.

**Как настроить.** Стенд лежит в `loadtest/`; эталонная конфигурация —
`examples/minimal`. По умолчанию тест проще запускать на базе без пользователей.
Если пользователи есть, передайте k6 cookie `onebase_session` через
`OB_SESSION_COOKIE`. Подробности — [`loadtest/README.md`](../loadtest/README.md).

**Как попробовать.**

```bash
docker compose -f loadtest/docker-compose.yml up -d --build
go run ./loadtest/seed -url http://localhost:8080 -out loadtest/seed/counterparties.json
mkdir -p loadtest/reports
docker compose -f loadtest/docker-compose.yml run --rm --service-ports \
  -e K6_WEB_DASHBOARD=true \
  -e K6_WEB_DASHBOARD_HOST=0.0.0.0 \
  -e K6_WEB_DASHBOARD_EXPORT=/reports/post_document.html \
  k6 run /scripts/scenarios/post_document.js
```

Во время прогона откройте `http://localhost:5665`; после завершения смотрите
`loadtest/reports/post_document.html`.

## Hot-reload конфигурации
<!-- status: stable -->

Сервер перечитывает изменённые метаданные, модули и регламентные задания без
перезапуска — быстрый цикл разработки. Настройки процесса из `app.yaml`, роли и
локали требуют рестарта.

**Как попробовать.** `onebase run --project <dir> --watch` (или `onebase dev`).

## Шаблоны проектов
<!-- status: stable -->
<!-- date: 2026-05-30 -->

Новая конфигурация может стартовать не с пустой папки, а с готового шаблона:
минимальная база, CRM, склад, финансы, задачи и другие поставляемые примеры. Шаблон
создаёт структуру проекта с YAML-метаданными, DSL-модулями, ролями, виджетами и демо-логикой.

**Как попробовать.**

```bash
onebase init --template warehouse my-base
onebase run --project my-base --sqlite my-base.db
```

## Авторство и лицензия конфигурации
<!-- status: stable -->
<!-- date: 2026-06-18 -->

У конфигурации есть поля «Автор», «Правообладатель» и «Лицензия» — чтобы форк или
поставка конфигурации клиенту имели юридически определённого правообладателя
поверх MIT-лицензии платформы. Поля едут вместе с конфигурацией (выгрузка/загрузка,
`.obz`) и показываются пользователю на экране «О программе» рядом с правообладателем
платформы.

**Как настроить.** Конфигуратор → панель **«Конфигурация»** → поля «Автор»,
«Правообладатель», «Лицензия конфигурации». В файловом режиме — ключи `author` /
`copyright` / `license` в `config/app.yaml`.

**Как попробовать.** Заполните поля в конфигураторе, сохраните и откройте в
пользовательском режиме **Меню ▾ → «О программе»**.

## Версия сборки платформы (дата и коммит)
<!-- status: stable -->
<!-- date: 2026-07-01 -->

Рядом с номером сборки платформы (`build-NNN`) теперь показываются **дата коммита**
(`дд.мм.гг`) и **короткий SHA** — по ним сборку легко найти в git. Само число
`build-NNN` — это лишь порядковый номер CI-прогона, а дата и хеш берутся из
информации о ревизии, которую `go build` автоматически зашивает в бинарь, без
каких-либо ручных меток при сборке.

**Как попробовать.** Откройте **Меню ▾ → «О программе»** — в строке «Версия
платформы» будет `onebase build-NNN · дд.мм.гг · <sha>`. В конфигураторе дата видна
и в шапке рядом с `платформа build-NNN`.

## Тело модуля в обработках
<!-- status: testing -->
<!-- date: 2026-06-22 -->
<!-- issue: 171 -->

Обработку можно писать как скрипт: операторы прямо в теле модуля (вне процедур),
как «исполняемый раздел» в 1С. При запуске обработки тело исполняется как точка
входа `Выполнить` — оборачивать код в отдельную `Процедуру Выполнить()` не нужно.
Объявления `Перем …` в начале модуля доступны телу. У остальных модулей (общий,
менеджера, проведения, формы) тело по-прежнему недопустимо — код помещается в
процедуры/обработчики (как в соответствующих модулях 1С).

**Как попробовать.** В `src/<имя>.proc.os` обработки напишите код без обёртки в
процедуру (например `Сообщить("Привет");`) и запустите её — `onebase procrun
--proc <Имя>` или из пользовательского режима.

## Ошибка при вызове метода у Неопределено
<!-- status: testing -->
<!-- date: 2026-07-01 -->
<!-- issue: 226 -->

Вызов метода у значения `Неопределено` (например `Макет.Область(...)`, когда
макет не найден) теперь честно останавливает модуль с ошибкой «Метод X вызван у
Неопределено» — раньше такой вызов тихо возвращал `Неопределено`, и код
продолжал работать с пустым результатом, маскируя опечатки и незаполненные
данные. Это ужесточение поведения: конфигурации, полагавшиеся на тихий no-op,
получат ошибку в месте настоящей проблемы. Перехват — обычной конструкцией
`Попытка … Исключение`.

**Как попробовать.** Вызовите метод у незаполненной переменной — модуль
остановится с понятной ошибкой вместо молчаливого «ничего не произошло».

## Dev-цикл: страница обновляется сама, браузер открывается сам
<!-- status: testing -->
<!-- date: 2026-08-12 -->

`onebase dev` держит сервер с перечитыванием конфигурации на лету, и теперь
результат правки виден без ручного F5: после перезагрузки метаданных, модулей и
расписаний открытая страница перечитывает себя сама. Правите форму или `.os`,
сохраняете — в браузере уже новая версия. Автообновление работает только в
`onebase dev`: обычный `onebase run` страницу пользователя не трогает.

Флаг `--open` открывает базу в браузере, когда сервер действительно готов
отвечать (а не в момент запуска, когда первая миграция схемы ещё идёт). Он есть
и у `onebase run`. `onebase dev` научился работать с SQLite — `--sqlite dev.db`,
как у `run`; раньше dev-режиму была нужна база PostgreSQL.

**Как попробовать.**

```bash
onebase dev --project examples/trade --sqlite dev.db --open
```

Откроется браузер; поправьте форму или модуль в конфигурации, сохраните файл —
страница обновится сама.

## Пересборка платформы на лету (`--reload-binary`)
<!-- status: testing -->
<!-- date: 2026-08-12 -->

Возможность для тех, кто дорабатывает саму платформу (не конфигурацию):
`onebase dev --reload-binary` следит за деревом Go-исходников и на каждое
сохранение `.go`, `go.mod` или `go.sum` пересобирает платформу и перезапускает
сервер. Страница в браузере после перезапуска обновляется сама — весь цикл
«поправил код → увидел результат» проходит без единой команды руками.

Если код не компилируется, прежний сервер продолжает работать, а в консоль
выводится ошибка компиляции: в браузере остаётся живая база, а не «страница
недоступна» до ближайшей удачной сборки. Конфигурацию `--reload-binary` не
пересобирает — её по-прежнему перечитывает сам dev-сервер.

**Как попробовать.** Из каталога с исходниками платформы:

```bash
onebase dev --project examples/trade --sqlite dev.db --reload-binary --open
```

Поправьте любой `.go` файл и сохраните — в консоли появится `[dev] изменился
Go-код — пересобираю...`, а страница в браузере перечитается уже с новой сборкой.
Каталог исходников можно задать явно: `--source <путь>` (ищется `go.mod` вверх
по дереву).

## Предохранители базы из командной строки (onebase settings)
<!-- status: stable -->
<!-- date: 2026-08-10 -->
<!-- issue: 722 -->

Опасные возможности конфигурации — сетевые операции (`net.enabled`: HTTP- и
WebSocket-клиенты, веб-хуки, почта и входящие HTTP-сервисы) и запуск команд ОС
(`exec.enabled`) — закрыты флагами в самой базе. Раньше переключить их можно
было только из конфигуратора; теперь есть команда, и это важно там, где UI
недоступен: CI, автопрогон на свежей базе, разбор восстановленной копии.

**Как попробовать.**

```bash
onebase settings list --sqlite base.db                 # что есть и что включено
onebase settings set net.enabled вкл --sqlite base.db  # вкл|выкл, on|off, true|false, 1|0
```

Команда переключает **только** предохранители: произвольную запись в служебные
настройки она не делает намеренно — там же лежат политика аутентификации и
ссылки на секреты, и правит их код, который знает про их инварианты.

Флаг — свойство конкретной базы, а не конфигурации: чужая конфигурация не может
включить его себе сама, а при загрузке `.obz` он сбрасывается в «выключено».

## Подпись обновлений платформы
<!-- status: testing -->
<!-- date: 2026-08-16 -->
<!-- issue: 783 -->

Обновление платформы теперь проверяет **криптографическую подпись** релиза, а не
только контрольную сумму. Разница принципиальная: сумма лежит на том же сервере,
что и архив, и защищает от битой закачки, но не от подмены — тот, кто получил
доступ к релизам, меняет обе. Подпись эту связь разрывает.

Что именно она даёт:

- **нельзя подменить файлы уже опубликованного релиза** — это главное:
  скомпрометированный доступ не перевыпустит вчерашний архив с закладкой;
- **нельзя опубликовать релиз в обход конвейера** — публикуется только то, что
  прошло через `main` и зелёный CI.

Чего она не даёт: защиты от того, кто владеет самим конвейером. Приватный ключ
живёт секретом сборки, и релиз подписывается автоматически — значит изменение,
проведённое в `main`, будет подписано. Подпись защищает артефакты, а не заменяет
контроль над тем, что попадает в ветку.

Пользователю делать ничего не нужно: проверка встроена в кнопку «Обновить» и в
`onebase update`. Неверная подпись останавливает обновление до закачки архива.

Проверка обязательна: релиз без подписи отвергается так же, как релиз с
неверной подписью. До этого действовал мягкий переход — отсутствие подписи лишь
писалось в журнал, — и польза от проверки была неполной: тому, кто получил
доступ к релизам, достаточно было не класть файл подписи рядом с архивом.
Мягкость нужна была, чтобы не оборвать автообновление стоящим на сборках без
подписи; таких сборок среди достижимых релизов не осталось, и режим переключён
на обязательный.

Режим определяется той копией, которая проверяет, а не релизом: установленная
раньше копия ещё обновится на неподписанный выпуск, но обновится она при этом
на сборку, которая так уже не сделает.

Сборка из исходников и форки не задеты: без вшитого открытого ключа проверки
нет, а форк со своим ключом подписывает свои сборки сам. Подробности —
`docs/release-signing.md`.

<!-- ═══════════════ Администрирование и безопасность ═══════════════ -->

## Команда `onebase version`

<!-- status: testing -->
<!-- date: 2026-08-20 -->
<!-- issue: 1052 -->

Показывает не только версию, но и **путь исполняемого файла**, коммит сборки и
платформу. Нужно это в одном очень частом случае: скачали новую версию, а
`onebase -v` отвечает старой. Почти всегда это значит, что отвечает другой
бинарь — архив распаковывается в подкаталог и сам ничего в `PATH` не заменяет.
По строке «onebase version v0.9.3» понять это было нельзя, по пути — сразу
видно.

**Как попробовать.**

```bash
onebase version
# onebase version v0.9.9
# коммит:      a1b2c3d от 20.08.26
# платформа:   linux/amd64, Go go1.26.6
# исполняется: /usr/local/bin/onebase
```

Раньше `onebase version` отвечало «unknown command "version"» — команда была
только флагом `-v`.

## Отказ старого бинаря открыть базу от новой версии
<!-- status: testing -->
<!-- date: 2026-08-20 -->
<!-- issue: 1057 -->

Платформа хранит в базе **минимальную ревизию бинаря**, совместимую со схемой, —
монотонный счётчик — и сверяет её при открытии. Если базу обслуживала платформа
новее той, что её открывает, запуск прерывается с внятным текстом: названы
ревизия базы, ревизия бинаря, версия и путь исполняемого файла. При переходе
ревизии новый бинарь сначала получает эксклюзивный lifetime-lock и атомарно
публикует барьер, а уже затем открывает обычный storage и выполняет DDL. Поэтому
работающий старый процесс не позволяет начать несовместимое обновление, а сбой
посреди миграции не оставляет старому бинарю ложного разрешения. Понизить
ревизию нельзя.

Раньше старый бинарь такую базу открывал молча и падал позже, на первой же
операции, которой нужна незнакомая колонка, — причём в произвольном месте: у
одного пользователя это был вход (500 «internal error»), но с тем же успехом мог
быть провод документа посреди рабочего дня. Числом, а не версией, ревизия
сделана намеренно: в обращении две схемы именования (`vX.Y.Z` и `build-NNN`), и
сравнивать их между собой некорректно.

Проверка защищает будущие пары версий: выпущенные ранее бинари её не содержат,
и ретроактивно лечить нечего.

**Как попробовать.** Откройте базу текущей версией (`onebase run`, launcher или
обычной CLI-командой) — она проштампуется до первой схемной операции. Запуск бинарём, который знает более
раннюю ревизию, теперь прервётся с объяснением вместо непонятной ошибки позже.
Осознанно открыть такую базу можно флагом `--allow-newer-schema` (в лаунчере и
GUI — переменной `ONEBASE_ALLOW_NEWER_SCHEMA=1`); ревизия базы при этом не
понижается, и следующий запуск снова будет проверен.

## Видимость разделов и виджетов по правам
<!-- status: testing -->
<!-- date: 2026-07-19 -->

Интерфейс подстраивается под роль пользователя, как в 1С. Панель разделов
показывает только те подсистемы, где пользователю доступен хотя бы один объект
(`read` для справочников/документов/регистров, `run` для отчётов/обработок);
вход при скрытой глобальной «Главной» уводит в первый видимый раздел, а прямая
ссылка на чужой раздел отвечает «нет доступа». Виджеты рабочего стола, чей
источник данных пользователю недоступен, больше не показывают ошибку «нет
доступа к объекту» — карточка (и опустевший ряд) просто исчезает; кнопки
создания в виджете «Быстрые действия» фильтруются по праву записи. Ошибки
конфигурации (битый запрос) по-прежнему видны.

Дополнительно у подсистемы появился необязательный whitelist ролей — как у
страниц и HTTP-сервисов: `roles: [Кладовщик, Управленец]` в
`subsystems/<имя>.yaml` показывает раздел только этим ролям (админ видит всё).
Так в демо-базе кладовщик видит только «Склад», снабженец — «Закупки», а
управленец — все разделы в режиме чтения. Опечатку в имени роли подсветит
`onebase check --lint` (предупреждение `rbac.unknown-role`).

**Как попробовать.** Создайте пользователей с разными ролями (Администрирование
→ Пользователи), задайте ролям права на объекты своих разделов и войдите под
каждым: панель разделов и рабочие столы у всех будут разными. Для жёсткого
разведения разделов добавьте `roles:` в `subsystems/*.yaml`.

## Параллельные сессии и изолированные окна Предприятия
<!-- status: stable -->
<!-- date: 2026-07-07 -->

Один пользователь теперь может работать в нескольких окнах одновременно: новый вход
не выбивает прежние сессии (в том числе вход в конфигуратор больше не завершает
сессию Предприятия того же администратора). В лаунчере рядом с «Предприятие»
появилась кнопка **«Новое окно»** — она открывает базу в отдельном окне с
изолированным профилем: у окна свой cookie-jar, поэтому в нём можно войти под
другим пользователем, не задев остальные окна. В GUI-сборке под Windows это
нативное окно (WebView2), в остальных случаях — внешний Chromium-браузер
(Edge/Chrome/Chromium); стрелка «▾» рядом с кнопкой позволяет выбрать способ явно.
Профиль работает как «рабочее место» и помнит вход — повторное открытие того же
окна не спросит пароль. Ссылка «Очистить изолированные профили» удаляет незанятые
профили с диска.

Админку активных сессий научили различать окна: по строке на сессию с видом
(Предприятие/Конфигуратор), временем входа, последней активностью, IP и браузером;
завершить можно одну сессию или все сессии пользователя. Смена пароля администратором
завершает все сессии пользователя, самостоятельная — все, кроме текущей; в профиле
появилась кнопка «Выйти со всех устройств». События завершения сессий пишутся в аудит.

Для борьбы с шарингом учёток есть опциональная политика «Максимум сессий на
пользователя» (поле на экране активных сессий): 0 — без ограничения (по умолчанию),
1 — как в прежних версиях. При превышении лимита новый вход вытесняет самую давнюю
по активности сессию Предприятия; окна конфигуратора не учитываются.

Жизненный цикл стал 1С-подобным: лаунчер позволяет оставить запущенные базы после
своего закрытия. В фоновом режиме открытые окна продолжают работать, а следующий
запуск лаунчера подхватывает живые базы (в том числе оставшиеся после
пересборки/перезапуска) и показывает их как работающие. Явная остановка —
«Остановить» у базы или «Стоп всё»; по умолчанию при закрытии окна с работающими
базами лаунчер спрашивает, оставить их в фоне или остановить (см. «Вопрос при
закрытии окна информационных баз»).

**Как попробовать.** Запустите базу кнопкой «Предприятие», войдите под первым
пользователем; затем «Новое окно» → войдите под вторым. Оба окна остаются
авторизованными; в **Администрирование → Активные пользователи** видны обе сессии,
каждую можно завершить отдельно.

## Вопрос при закрытии окна информационных баз
<!-- status: testing -->
<!-- date: 2026-08-11 -->

Базы можно оставить работать в фоне при закрытии лаунчера. Раньше это происходило
молча, и было легко решить, что «всё закрыто», пока серверы баз держат порт, файл
базы и сеансы пользователей. Теперь, если на момент закрытия базы работают,
лаунчер по умолчанию спрашивает:

- **Да / «Продолжить в фоне»** — базы продолжают работать: регламентные задания, обмены
  и подключённые пользователи не пострадают. Окно запуска закрывается; чтобы остановить
  базы, откройте его снова — работающие базы видны в списке.
- **Нет / «Остановить все»** — то же, что «Стоп всё»: базы останавливаются, и только
  потом закрывается окно. Открытые окна Предприятия при этом теряют связь.
- **Отмена** — окно не закрывается.

Вопрос задаётся только когда есть что оставлять в фоне: если ни одна база не работает,
окно закрывается сразу. Ответ можно запомнить галочкой **«Больше не спрашивать»** — он
сохраняется в реестре баз (`~/.onebase/ibases.yaml`, ключ `settings.on_close`:
`ask` — спрашивать, по умолчанию; `background` — всегда оставлять работать; `stop` —
всегда останавливать). Эту же политику можно в любой момент выбрать в списке
**«При закрытии»** на панели лаунчера. Кнопка **«Стоп всё»** теперь тоже
переспрашивает: одно нажатие мимо обрывало сеансы всех подключённых пользователей.

При выборе остановки лаунчер завершает только процессы, принадлежность которых
надёжно подтвердил; совпадения номера порта недостаточно. Неподтверждённые
процессы он не трогает, предупреждает о них и закрывается. Если управляемую базу
не удалось остановить или подтвердить её остановку, окно остаётся открытым и
предлагает повторить действие или отменить его.

**Как попробовать.** Запустите базу, закройте окно лаунчера системным крестиком —
появится вопрос. Выберите «Да», снова откройте лаунчер: база показана работающей, её
можно остановить. В сборке без нативного окна (лаунчер открыт в браузере) тот же вопрос
задаёт кнопка ✕ на панели лаунчера.

## Бэкап и восстановление
<!-- status: stable -->
<!-- date: 2026-06-01 -->

Резервное копирование базы и восстановление из бэкапа — через CLI и интерфейс. Переносите
базу между PostgreSQL и SQLite одним `.obz`-файлом без потери нумерации, пользователей и
вложений. Universal-архив также содержит очередь, счётчики доставки и watermarks планов
обмена. При импорте интерфейс предлагает выбрать disaster recovery того же узла
(вернуть очередь и watermarks) либо безопасный clone (сбросить identity и очередь).
Bearer-токен в обоих режимах нужно задать заново. Программный
`ImportUniversal` по умолчанию создаёт безопасный clone — очищает node identity, token и
очереди; для DR используется `ImportUniversalWithOptions`. Полное восстановление требует
остановленной базы, а сеть и выполнение команд после него остаются выключенными.

**Как попробовать.** Сделайте бэкап рабочей базы и восстановите её в чистую.

## Автобэкап по расписанию
<!-- status: stable -->
<!-- date: 2026-06-24 -->

База может сама создавать резервные копии по cron-расписанию и удалять старые файлы
сверх лимита `keep_last`. Автобэкап регистрируется как системное задание `AutoBackup`,
поэтому успехи и ошибки видны в журнале регламентных заданий. PostgreSQL-дампы пишутся
как `.sql.gz`, SQLite — как `.db`; файл сначала создаётся во временном имени и только
после успешного завершения переименовывается в финальный бэкап.

**Как настроить.** Конфигуратор → вкладка **«Бэкапы»** → **«Настройки автобэкапа»**:
включите автобэкап, задайте расписание (`0 2 * * *` — каждую ночь в 02:00), лимит
хранения и папку. То же можно задать в `config/app.yaml`:

```yaml
backup:
  enabled: true
  schedule: "0 2 * * *"
  keep_last: 7
  directory: ""
```

**Как попробовать.** Поставьте временное расписание на ближайшую минуту или создайте
бэкап вручную на вкладке «Бэкапы», затем проверьте, что старые `backup_*` файлы сверх
лимита удаляются.

## Бэкап в S3 (off-site)
<!-- status: testing -->
<!-- date: 2026-07-30 -->
<!-- issue: 393 -->

Автобэкап дополнительно отправляет копию в S3-совместимое хранилище (AWS S3, MinIO,
Ceph RGW, Backblaze B2, Wasabi и т.п.) — off-site-копия на случай потери самого
сервера. Локальная копия при этом остаётся. Клиент S3 встроен и **не тянет внешних
зависимостей** (подпись AWS SigV4 на стандартной библиотеке), офлайн-работа без S3
не меняется. Если задан `s3.keep_last`, лишние объекты в бакете ротируются так же,
как локальные файлы.

**Как настроить.** Секция `s3` внутри `backup` в `config/app.yaml`. Ключи доступа
задавайте через `${env:VAR}` — секрет живёт в окружении, а не в YAML/git/дампе:

```yaml
backup:
  enabled: true
  schedule: "0 2 * * *"
  keep_last: 7
  directory: ""            # локальная копия остаётся
  s3:
    endpoint: s3.amazonaws.com     # или minio.local:9000
    region: us-east-1
    bucket: my-onebase-backups
    prefix: prod/                  # ключ-префикс в бакете
    access_key: ${env:S3_ACCESS_KEY}
    secret_key: ${env:S3_SECRET_KEY}
    use_ssl: true                  # для локального MinIO по http — false
    path_style: true               # совместимо с MinIO/кастомными эндпойнтами
    keep_last: 30                  # ротация в бакете (0 = не ротировать)
```

**Как попробовать.** Проще всего локальный MinIO: создайте бакет, задайте
`endpoint: 127.0.0.1:9000`, `use_ssl: false`, ключи через env, поставьте расписание на
ближайшую минуту. После срабатывания `AutoBackup` объект `prod/backup_*.db` появится в
бакете; ошибки выгрузки видны в журнале регламентных заданий (локальная копия при этом
создаётся в любом случае).

## Бэкап и восстановление SQLite из CLI
<!-- status: testing -->
<!-- date: 2026-07-09 -->
<!-- issue: 299 -->

Команды `onebase backup` и `onebase restore` теперь умеют SQLite напрямую, а не только
PostgreSQL. `onebase backup --sqlite ./base.db --out ./backups/` делает атомарный снимок
через `VACUUM INTO` в обычный `.db` — восстановление простым копированием файла, без
установленного `pg_dump`. Обратно: `onebase restore --sqlite ./base.db --file
./backups/backup_base_….db --force` (база должна быть остановлена; резервная копия
проверяется через `integrity_check`, прежняя БД сохраняется рядом как `.old`).

**Как попробовать.** `onebase backup --sqlite <ваша-база>.db --out ./backups/` — в папке
появится `backup_<имя>_<дата>.db`.

## Проба готовности `/healthz`
<!-- status: testing -->
<!-- date: 2026-07-09 -->
<!-- issue: 299 -->

У сервера появился публичный readiness-эндпоинт `GET /healthz`: `200`, только если база
данных отвечает, и `503`, если нет. В отличие от liveness-`/health` (всегда `200` —
«процесс жив»), `/healthz` годится для health-check reverse-proxy, systemd `WatchdogSec`
и проверки после обновления. Токен не требуется.

**Как попробовать.** При запущенном сервере откройте `http://<host>:<port>/healthz` —
вернётся `ok`.

## Обновление платформы: кнопка в лаунчере и каналы build/stable
<!-- status: testing -->
<!-- date: 2026-08-06 -->

Платформа сама узнаёт о новых сборках и умеет обновляться. Каналов два:

- **build** (по умолчанию) — сборки из `main`, выходят по нескольку раз в день;
- **stable** — только выпуски `vX.Y.Z`.

В шапке лаунчера появляется отметка с номером доступной сборки — тихая, только когда
есть что предложить. По ней открывается страница обновления: установленная версия,
переключатель канала, список изменений выпуска и кнопки «Проверить», «Скачать»,
«Перезапустить и обновить», «Вернуть версию».

Загрузка и применение разделены: скачивание идёт фоном и ничего не останавливает,
контрольная сумма сверяется обязательно. Применение останавливает запущенные базы,
меняет бинарь и перезапускает платформу — после чего те же базы поднимаются обратно.
Прежняя версия сохраняется, вернуться на неё можно одной кнопкой (но базы, уже
открытые новой версией, обратно не мигрируются — об этом предупреждает сама страница).

Обновляться кнопкой может только **личная установка**: на Windows каталог платформы
обязан лежать внутри профиля пользователя (`C:\Users\<имя>`), на Unix — за приватной
границей каталога владельца. Общая установка (`C:\Program Files`, папка терминального
сервера, каталог вроде `C:\Projects\onebase` у корня диска) обновляется вручную —
распаковкой архива выпуска поверх. Запуск от администратора правила не меняет: оно
смотрит на расположение каталога, а не на права, и личный каталог у администратора
другой. Настоящее отсутствие прав на запись платформа называет отдельно: это разные
отказы, и лечатся они разным.

Версия сравнивается по схеме `build-<число>` либо `vX.Y.Z`. Бинарь с нестандартным
ярлыком (например `build-793fix`) с выпусками не сопоставляется: обновление ему не
предложат никогда, и страница пишет об этом прямо, а не «установлена актуальная
версия». У сборки разработчика (собрана без штампа версии) вдобавок не идёт фоновая
проверка — там строка «Проверено» относится к результату другого бинаря, о чём
страница тоже предупреждает.

Администратор может дополнительно положить рядом с бинарём `onebase.policy.yaml`:

```yaml
updates:
  ui: false                    # спрятать средства обновления
  check: off                   # запретить обращения к сети (офлайн-контур)
  channel: stable              # зафиксировать канал
  repo: myorg/onebase-dist     # свой источник сборок
```

Источник сборок — всегда репозиторий GitHub в формате `владелец/имя`: произвольный
сервер обновлений платформа не поддерживает, меняется только имя репозитория —
флагом `--repo` на одну команду или `updates.repo` в политике на всю установку
(политика перекрывает флаг). Как устроено обновление целиком, как поднять
зеркало предприятия и офлайн-контур — в [`docs/self-update.md`](self-update.md).

Экран «О программе» в Предприятии только сообщает о новой версии: рабочий процесс
базы может быть службой и своим бинарём не распоряжается. В сеть он не ходит —
читает результат проверки, сделанной лаунчером.

**Как попробовать.** В лаунчере нажмите значок обновления в шапке (или откройте
`/updates`), выберите канал и нажмите «Проверить». Из командной строки то же самое:
`onebase update --check`, `onebase update --channel stable`, `onebase update`
(скачает и применит), `onebase update --rollback`. Для мониторинга —
`onebase update --check --json`.

## Offline-обновление одной командой
<!-- status: testing -->
<!-- date: 2026-07-09 -->
<!-- issue: 299 -->

`onebase update --from <файл>` обновляет установленный бинарь из локального архива
(`.zip` с `onebase.exe` внутри) или самого `.exe` — для серверов без интернета, куда
обновление приносят на флешке. Команда останавливает сервис, подменяет бинарь (сохраняя
старый рядом как `.old`), запускает сервис и опрашивает `/healthz`. Если новый бинарь не
ответит `200` с версией нового бинаря за `--timeout` (по умолчанию 30 с) — автоматически
откатывает старый бинарь и перезапускает сервис. SHA256 артефакта обязательна.

**Как попробовать.** `onebase update --from D:\flash\onebase-v0.9.1.zip --sha256 <hex>
--id <база>` (имя сервиса и порт берутся из реестра) либо `--service <имя> --port
<порт>`. Сервис должен быть установлен через `onebase service install`.

## Горячая перезагрузка конфигурации из БД
<!-- status: testing -->
<!-- date: 2026-07-09 -->
<!-- issue: 299 -->

В режиме `--config-source database` сервер, запущенный с `--watch`, сам подхватывает
новую конфигурацию после `onebase deploy` (или отката версии) — без перезапуска и
разрыва активных сеансов. Раз в 5 секунд сервер сверяет историю версий конфигурации и
при появлении новой перечитывает метаданные. Схему БД деплой мигрирует сам (до создания
версии), поэтому перезагрузка безопасна. Для файловой конфигурации `--watch` работал и
раньше (fsnotify по `.yaml`/`.os`) — теперь тот же флаг покрывает и режим БД.

Без рестарта атомарно обновляется runtime-реестр: сущности, регистры, модули,
формы, отчёты, сервисы, страницы, планы обмена, подсистемы, журналы и виджеты.
Регламентные задания из `scheduled/` предварительно валидируются и заменяются
полным набором; нативные задания `demo/backup` сохраняются. Настройки процесса
из `app.yaml` (порт, SMTP, webhooks, лимиты, demo/backup), роли и локали остаются
прежними до перезапуска сервиса.

**Как попробовать.** Запустите сервер с `onebase run --config-source database --db <DSN>
--watch`, из другого окна задеплойте изменение (`onebase deploy --project <dir> --db
<DSN>`) — через несколько секунд оно применится без рестарта (в логе
«metadata/DSL/scheduled перезагружены из БД»). Для постоянной службы установите тот же режим командой
`onebase service install --id <ID-базы> --watch`; уже установленную без `--watch`
службу сначала удалите и установите заново.

## Свёртка базы
<!-- status: stable -->
<!-- date: 2026-06-24 -->

Свёртка информационной базы на дату: остатки балансовых регистров накопления сворачиваются
в опорные записи (синтетический регистратор `_СвёрткаБазы`), а движения и — по выбору —
документы до даты удаляются. Уменьшает размер базы и ускоряет виртуальные таблицы
`.Остатки()`. Оборотные регистры (`kind: обороты` в YAML) свёртка не трогает. Регистры
бухгалтерии сворачиваются опорными проводками через вспомогательный счёт («000» или
настройка `rollup.aux_account`). Перед запуском
показывается предпросмотр (что свернётся, сколько документов удалится, сколько ссылок
повиснет); операция идёт в одной транзакции с пост-чеком «остатки до == после» и
выставляет дату запрета проведения свёрнутого периода. Операция **необратима** — сначала
сделайте бэкап.

**Защита ссылок (жёсткий гейт).** Если включено удаление документов и на удаляемый документ
ссылается *сохраняемая* запись (другой документ после даты, элемент справочника), свёртка
отменяется с пояснением — чтобы не оставить повисших ссылок. Безопасный выход — снять
«Удалить документы» (режим keep: документы остаются без проведения). Ссылки *между самими
удаляемыми* документами гейту не мешают.

**Обрезка регистров сведений (опционально, нестандартно для 1С).** По желанию можно обрезать
периодические регистры сведений: на каждую комбинацию измерений остаётся последний срез до
даты свёртки, более ранняя история удаляется. `СрезПоследних` на даты от свёртки и позже не
меняется; `СрезПервых` и история срезов до даты теряются, поэтому включается осознанно
(чек-лист в мастере / флаг `--trim-info-registers`). По умолчанию выключено.

**Как настроить.** В **Конфигураторе**: **Меню ▾ → «Свёртка базы»** (под ролью admin).
Либо CLI: `onebase rollup --project <dir> --sqlite <f> --date ГГГГ-ММ-ДД [--dry-run]
[--keep-documents] [--registers a,b] [--trim-info-registers]`.

**Как попробовать.** На копии рабочей базы откройте «Свёртка базы», выберите дату, нажмите
«Предпросмотр», затем — после резервной копии — «Выполнить свёртку». Проверьте, что остатки
на дату не изменились.

## RBAC-роли
<!-- status: stable -->
<!-- date: 2026-05-04 -->

Роли с правами на сущности, обработки и формы; есть роль «демо» с запретом создания объектов.

**Как настроить.** `roles/<роль>.yaml` (примеры — `examples/trade/roles/`).

**Как попробовать.** Войдите под ограниченной ролью и проверьте доступ.

## Строковые политики доступа
<!-- status: testing -->
<!-- since: build-273 -->
<!-- date: 2026-07-07 -->
<!-- issue: 273 -->

Роли могут ограничивать не только объект целиком, но и отдельные строки справочников,
документов, регистров накопления и регистров сведений. Политики применяются одинаково в
UI, REST, списках, карточках, picker-ссылках, журналах, печати, экспорте, вложениях,
картинках, отчётах, AI-запросах и виджетах. В запросах OneBase предикаты
встраиваются alias-aware в SQL для справочников, документов, регистров накопления и
регистров сведений, включая JOIN, виртуальные таблицы и `FROM`-подзапросы;
`UNION` с RLS пока отклоняется безопасной ошибкой.

Пользовательский DSL-код в обработках, страницах, отчётах и HTTP-сервисах
использует тот же контекст доступа: `Справочники`, `Документы`, `Запрос`,
`РегистрыНакопления` и чтение реквизитов по ссылке не обходят RLS. Платформенные
server hooks (`OnWrite`, `OnPost`, серверные события форм) считаются trusted
server-code: они видят `ТекущийПользователь()`, но не ограничиваются строковыми
policy при выполнении внутренних правил конфигурации.

Для условий по реквизитам ссылок используйте поле вида `Ссылка.Реквизит`, например
`Клиент.Owner`: списки и запросы получают SQL `EXISTS`, а прямое открытие/запись
проверяет referenced row перед доступом. При создании запись может получить
auto-owner: простые политики `field eq user/login|id|user_attr|literal` для
`write`/`post` заполняют пустое поле перед row-check; явно переданное другое
значение не перезаписывается и будет отклонено обычной проверкой.

`onebase check --lint` дополнительно проверяет политики и выдаёт warnings
`rls.*`: неизвестный объект в `row_access`, невалидные `field/op/value/user_attr/same_as`
и policy, которая не применяется из-за отсутствующего object-level права.
Администраторский экран `/ui/admin/rls` показывает активные policies, ошибки
компиляции, auto-fill поля и матрицу trusted/user DSL paths.


**Как настроить.** В `roles/<роль>.yaml` добавьте `permissions.row_access` рядом с
обычными правами:

```yaml
permissions:
  documents:
    Сделка: [read, write, post]
  row_access:
    documents:
      Сделка:
        read:
          field: Ответственный
          op: eq
          value: { user: login }
        write:
          same_as: read
        post:
          same_as: read
```

Поддерживаются `all`, `any`, `not`, `same_as`, операции `eq`, `ne`, `in`, `not_in`,
`empty`, `not_empty`, значения `{ user: id|login }`, `{ user_attr: full_name|lang|... }`,
`{ literal: ... }`, `{ list: [...] }`. `user_attr` читает встроенные атрибуты пользователя
(`id`, `login`, `full_name`, `lang`, `is_admin`, `deny_passwd_change`, `show_in_list`,
`ai_data_access`) и host-provided `User.Attrs`; отсутствующий атрибут закрывает доступ
и виден как lint warning.

**Как попробовать.** Создайте две записи с разными значениями поля-владельца, войдите под
ролью с `row_access` и проверьте, что список, прямое открытие, REST и экспорт показывают
только разрешённые строки.

## Журнал регистрации / аудит
<!-- status: stable -->
<!-- date: 2026-05-21 -->

Трёхуровневый аудит (critical/important/normal): ошибки, удаление, создание и изменение
данных с контекстом пользователя.

**Как настроить.** Уровень журнала — в конфигураторе (применяется без перезапуска).

**Как попробовать.** Откройте журнал регистрации и отфильтруйте события.

## Наблюдаемость: metrics и структурные логи
<!-- status: stable -->
<!-- date: 2026-06-25 -->

Сервер отдаёт Prometheus-метрики на `/metrics`, а runtime, launcher, device-agent,
devserver, scheduler и CLI diagnostics пишут структурные `slog`-логи. Логи могут быть
text/json, поддерживают уровни, компоненты и редактирование секретов/токенов/DSN в
атрибутах, URI и startup-аргументах.

**Как настроить.** Используйте `ONEBASE_LOG_LEVEL`, `ONEBASE_LOG_FORMAT=json` или
`ONEBASE_ENV=production` для JSON-логов. Метрики доступны под административным доступом.

**Как попробовать.** Запустите базу, выполните несколько HTTP-действий и посмотрите
структурные записи в логах; затем откройте `/metrics` под admin-сессией.

## Регламентные (фоновые) задания
<!-- status: stable -->
<!-- date: 2026-05-05 -->

Задания по расписанию (cron) с DSL-обработчиком; параметры-шаблоны
(`{{today|minus_days:7}}`); история запусков.

**Как настроить.** `scheduled/<имя>.yaml` + обработчик; управление — в админке.

**Как попробовать.** Создайте задание и запустите его вручную.

## Сетевой предохранитель
<!-- status: stable -->
<!-- date: 2026-06-18 -->

Один переключатель блокирует все сетевые операции конфигурации (HTTP-клиент, webhook,
HTTP-сервисы, email) — защита от случайного выхода в сеть из недоверенной или демо-базы.
Сбрасывается при восстановлении из бэкапа.

**Как настроить.** Конфигуратор → **Меню ▾ → «Параметры базы»** → галочка **«Разрешить
сетевые операции конфигурации»**.

**Как попробовать.** Снимите галочку и убедитесь, что HTTP-запрос из обработки запрещён.

## Песочница DSL
<!-- status: stable -->
<!-- date: 2026-06-16 -->

Профиль ограничений для запуска недоверенного кода (запрет сети/файлов/команд ОС, лимит
времени и итераций цикла). Применяется к недоверенному DSL-коду и дополнительно запрещает
вызов команд ОС внутри песочницы.

**Как попробовать.** Запустите недоверенный DSL через sandbox-профиль и проверьте, что
сетевые, файловые операции и команды ОС запрещены.

## Демо-режим
<!-- status: stable -->

Жёлтый баннер и автосброс данных по расписанию из эталонного `.obz` — для публичных
демо-баз с полным функционалом.

**Как настроить.** Блок `demo:` в `config/app.yaml` (`enabled`, `reset_backup`,
`reset_schedule`, `message`).

## Табличные части справочников в DSL
<!-- status: testing -->
<!-- date: 2026-07-18 -->

Объекты справочников, создаваемые из DSL-кода (`Справочники.X.Создать()`,
`Ссылка.ПолучитьОбъект()`), теперь поддерживают табличные части — как у
документов: `Кл.Контакты.Добавить()`, `Кл.Контакты.Очистить()`, перебор
`Для Каждого Стр Из Кл.Контакты`. Запись идёт тем же путём, что и из
веб-формы: срабатывает хук `ПриЗаписи` (`<справочник>.os`), веб-хуки и
регистрация в планах обмена.

**Как попробовать.** В обработке: создайте элемент справочника с ТЧ,
добавьте строки через `.Добавить()`, вызовите `Записать()`; откройте
карточку — строки ТЧ на месте, хук ПриЗаписи выполнен.

## Вложения из DSL
<!-- status: testing -->
<!-- date: 2026-07-18 -->

Обработки и регламентные задания могут работать с вложениями записей:
`ПрисоединитьФайл(Ссылка, Путь[, Имя])` прикрепляет файл с диска к элементу
справочника или документу, `СписокВложений(Ссылка)` возвращает массив структур
`{ИД, ИмяФайла, Размер, ТипMIME, Загружен}`, `ПутьКВложению(ИД)` отдаёт путь к
файлу хранилища, `УдалитьВложение(ИД)` удаляет. Лимиты размера и типов — те же,
что у загрузки через веб-форму.

**Как попробовать.** В обработке прикрепите файл к записи и откройте её
карточку — файл виден в секции «Вложения».

## Вложения в письмах
<!-- status: testing -->
<!-- date: 2026-07-18 -->

`ПисьмоEmail.ПрисоединитьФайл(Путь[, ИмяВПисьме])` прикладывает файл к письму;
отправка собирает корректный `multipart/mixed` (тело + файлы в base64). В связке
с `ПутьКВложению` позволяет рассылать файлы, прикреплённые к записям.

**Как попробовать.** `Письмо = Новый ПисьмоEmail; … Письмо.ПрисоединитьФайл("/путь/файл.zip"); Письмо.Отправить();`

## Полноценные регламентные задания
<!-- status: testing -->
<!-- date: 2026-07-18 -->

Регламентные задания теперь исполняются с полным DSL-окружением — как обработки:
доступны `Справочники`, `Документы` (создание/запись/проведение), вложения,
транзакции, `ЗначениеРеквизитаОбъекта`. Фоновые сценарии вида «сканировать
папку и регистрировать записи» или «создавать документы по расписанию» работают
без обходных путей.

**Как попробовать.** Создайте обработку, создающую элемент справочника, и
задание `scheduled/*.yaml` на неё; после срабатывания запись появится в базе.

## Маскирование персональных данных по ролям
<!-- status: testing -->
<!-- date: 2026-07-21 -->
<!-- issue: 88 -->

Роль может маскировать чувствительные поля (телефон, адрес, паспорт, e-mail) —
единообразно в списках, карточках, REST v1/v2, picker'е, печати и экспорте
(маска считается на сервере, полное значение в HTML/JSON не уходит). В
`permissions.field_access` для поля задаётся стратегия: `mask_tail` (оставить
последние N символов, `keep: N` → `•••••1122`), `mask_city` (для адреса —
оставить только город), `mask_all` (фиксированная маска), `hide` (поле не
отдаётся вовсе) или `full`. Семантика — как у строкового доступа (план 79):
наименее ограничивающая роль побеждает. Полное значение раскрывается точечно —
операцией «Раскрыть» (`POST /ui/{kind}/{entity}/{id}/disclose` или REST
`GET /api/v2/{kind}/{name}/{id}/field/{Поле}?disclose=1&reason=…`) по праву
`disclose` и с обязательным основанием; раскрытие фиксируется в журнале
регистрации **без самого значения**.

Отчёты, виджеты, ИИ-запросы и консоль запросов **строятся с маской**: колонка,
которая просто выводит защищённое поле (в том числе под алиасом `КАК` и в
проекции `*`), приходит замаскированной, а не закрывает отчёт целиком. Запрос
отклоняется, только если защищённое поле участвует в отборе `ГДЕ`, группировке,
сортировке или агрегате: там маска на выходе не защищает — условием значение
восстанавливается перебором. Так же ведут себя модули: `Новый Запрос`,
`Ссылка.ПолучитьОбъект()`, `НайтиПоНомеру()` и разыменование `Клиент.Телефон`
отдают защищённый реквизит по политике роли, а `НайтиПоРеквизиту` по такому
реквизиту запрещён. Прикладной код исполняется в правах пользователя —
не используйте защищённые реквизиты в расчётах проведения. Реквизит, видимый
только под маской, нельзя перезаписать ни из формы, ни из REST, ни из модуля.

**Признак ПДн у реквизита (`pii: true`).** Маскирование объявляет роль, и у
этого есть обратная сторона: новый реквизит с телефоном открыт всем, пока про
него не вспомнили в каждой роли по отдельности, а роль вовсе без `field_access`
видит значения целиком. Признак `pii: true` у поля сущности переворачивает
умолчание:

```yaml
fields:
  - name: Телефон
    type: string
    pii: true
```

Роль, которая читает объект и **не высказалась** о таком поле, получает маску
(`mask_all`), а не полное значение. Выход из fail-closed есть, но он явный: роль
пишет `read: full` (или свою стратегию — `mask_tail` и т.п.) и тем самым
принимает решение о раскрытии на себя. Семантика прежняя: наименее
ограничивающая роль побеждает, поэтому одна роль с явным `full` открывает поле.

Признак ставит конфигуратор, поэтому конфигурации без `pii:` не меняются вовсе.

**Где признак работает.** Там же, куда дотягивается маскирование: реквизиты
шапки объекта (`fields` справочника или документа) и измерения, ресурсы и
реквизиты регистров **накопления** и **сведений** — у них закрыты обе границы
чтения, и запросы, и собственные списки регистра.

Два места признак не исполняют, и в обоих он не принимается молча, а
отвергается — `onebase check` откажет ещё до запуска:

- **поля табличных частей**: политику `field_access` на них не написать, и
  признак там нечему исполнять («признак pii не поддерживается в табличных
  частях»);
- **ресурсы и субконто регистра бухгалтерии**: списки проводок и остатков
  (`/ui/accountreg/…`) не маскируют полей ни по умолчанию, ни по явному правилу
  роли, поэтому закрытое в отчёте значение осталось бы открытым на соседней
  странице («списки проводок и остатков бухрегистра поля не маскируют»).

Приняв признак там, платформа обещала бы защиту, которой нет, — а именно от
таких обещаний он и заведён. Нужен защищённый телефон построчно — держите его в
шапке отдельного объекта (обращение, попытка связи) и ссылайтесь на него из
табличной части.

**Как попробовать.** В роль добавьте
`field_access: { catalogs: { Клиент: { Телефон: { read: mask_tail, keep: 4 } } } }`
и право `disclose` к `Клиент` (готовый пример — `examples/callcenter`, роль
«Оператор»). Под этой ролью телефон в списке и карточке клиента виден как
`••••••••1122`; отчёт `ВЫБРАТЬ Наименование, Телефон ИЗ Справочник.Клиент`
построится с маской в колонке телефона, а тот же отчёт с `ГДЕ Телефон = …`
будет отклонён. Запрос `/field/Телефон?disclose=1&reason=звонок` вернёт полный
номер и запишет действие `disclose` в `/ui/admin/audit`.

## Версионируемые правила валидации
<!-- status: testing -->
<!-- date: 2026-07-22 -->
<!-- issue: 89 -->

Прикладная конфигурация может хранить правила проверки как данные и включать
их с заданной бизнес-даты. Эталонный паттерн в `examples/callcenter` использует
справочник правил, периодический регистр версий и документ публикации. Уровень
`Блокирующее` отменяет запись с кодом правила, `Предупреждение` и `Информация`
показывают сообщение без отмены. Опубликованные версии нельзя изменить,
перепровести или удалить отменой проведения; откат оформляется новой
публикацией с прежними значениями и более поздней датой.

**Как попробовать.** Запустите обработку «Заполнить правила валидации (демо)»
под ролью «Супервизор», затем создайте заявку без улицы: правило `REQ-STREET`
заблокирует запись. Создайте новую публикацию правила с будущей датой — до неё
будет действовать прежняя версия, после неё новая. Обычная роль «Оператор» не
может публиковать правила.

## Установка как службы Windows и лаунчер баз
<!-- status: stable -->

Установка сервера службой Windows и лаунчер баз (список, добавление/удаление баз),
portable-дистрибутив без установки в реестр.

**Как попробовать.** `onebase service install`; управление базами — `onebase ibases
list/add/remove` или через лаунчер.

## Лаунчер: открыть базу в локальную сеть
<!-- status: testing -->
<!-- date: 2026-08-05 -->
<!-- issue: 590 -->

Базы, запущенные из лаунчера, раньше всегда слушали только `127.0.0.1` — зайти на
свою базу с другой машины по `http://<ip>:<порт>/ui` было нельзя (флаг `--host`
существовал только у `onebase run` из командной строки, но лаунчер его не
пробрасывал). Теперь в карточке базы есть выбор **«Доступ по сети»**: «Только этот
компьютер (127.0.0.1)» по умолчанию или «Все интерфейсы (0.0.0.0)» — база
становится доступна из локальной сети.

Умолчание безопасное (loopback): наружу база выставляется только явным выбором.
Предупреждение остаётся в силе — пока в базе нет пользователей, аутентификация
выключена целиком, поэтому «Все интерфейсы» без заведённого пользователя открывают
базу и консоль кода без пароля (сообщение об этом пишется в журнал базы).

**Как попробовать.** Карточка базы → «Доступ по сети» → «Все интерфейсы» →
запустить базу и открыть `http://<ip-этой-машины>:<порт>/ui` с другой машины сети.

## Ручная нумерация из DSL (Нумераторы.СледующийНомер)
<!-- status: testing -->
<!-- date: 2026-07-21 -->
<!-- issue: 358 -->

Объекты, создаваемые из обработок/заданий (`Справочники.X.Создать().Записать()`),
шли мимо автонумерации и не получали номер. Теперь глобал `Нумераторы` даёт
доступ к тому же атомарному счётчику, что и создание через форму/REST:
`Нумераторы.СледующийНомер("Сущность"[, Дата[, Область]])`. Можно также передать
вторым аргументом структуру/карту полей, из которой будут взяты дата и `scope`. Сущность с настройкой
`numerator:` получит номер по своему формату/периоду, без неё — простую сквозную
последовательность. Для scoped-нумератора область обязательна: пустой общий счётчик
отклоняется, чтобы номера разных организаций не смешивались. В явной DSL-транзакции
счётчик участвует в той же транзакции и откатывается вместе с ней.

**Как попробовать.** В обработке: `Новый = Справочники.Договоры.Создать();
Новый.Номер = Нумераторы.СледующийНомер("Договоры"); Новый.Записать();` — элемент
сохранится с уникальным последовательным номером.

## Входной шлюз приёмки: идемпотентность и карантин
<!-- status: testing -->
<!-- date: 2026-07-26 -->

Надёжный приём произвольных внешних событий (с сайта, из 1С, от партнёров) с
гарантиями на уровне движка. Шлюз описывается файлом `intake/<имя>.yaml`: адрес
(`endpoint`), проверка отправителя (`auth: none|token|hmac` + `secret`), ключ
идемпотентности (`idempotency.key`, обычно `event_id`) и обработчик — процедура
`Обработать(Конверт)` в одноимённом модуле. Событие приходит единым JSON-конвертом
`{event_id, source, aggregate, payload, …}`; `payload` несёт поля бизнес-объекта.

Что даёт движок (в отличие от сборки этого вручную на справочниках): повторная
доставка одного `event_id` НЕ создаёт второй объект даже под конкурентной нагрузкой
(атомарный insert-if-new); обработчик и отметка «обработано» идут в одной
транзакции — сбой обработчика откатывает бизнес-объект целиком; «битое» событие
(ошибка обработчика или тот же ключ с другим телом) уходит в карантин. Ответы
транспорту: 200 Принято/Дубль, 202 Карантин, 422 Отклонено.

**Как попробовать.** Пример — в `examples/callcenter` (`intake/sitelead.yaml` +
`src/SiteLead.module.os`, endpoint `/hs/intake/lead`). Включите «Разрешить сетевые
операции конфигурации» (конфигуратор: Меню → «Параметры базы») и дважды
отправьте POST с одинаковым `event_id` — второй ответ будет «Дубль», второй
Заявки не появится; тот же ключ с другим телом уйдёт в карантин.

## Живой список (автообновление без F5)
<!-- status: testing -->
<!-- date: 2026-07-27 -->

Списки перечитываются сами, когда данные меняются, — без ручного F5 и без шторма
запросов. У сущности включается двумя строками:

```yaml
notify_changes: true        # платформа публикует «данные.<сущность>» после записи
list_refresh_on:            # список слушает эти события шины
  - данные.заявка
```

После успешной записи/проведения платформа шлёт сигнал инвалидации **только тем
онлайн-пользователям, кому строка видна по правам** (RLS): прежний владелец убирает
её из своего списка, новый — видит. Клиент перечитывает тот же GET, что и по F5, но
с задержкой (склейка пачки изменений в одно обновление) и **только в видимой
вкладке** — фоновые не нагружают сервер. Можно слушать и собственные прикладные
события: `ОтправитьУведомление("*", "данные.заявка", …)` из обработки обновит все
открытые списки заявок.

**Как попробовать.** В `examples/callcenter` документ «Задача» уже помечен
(`notify_changes` + `list_refresh_on: [данные.задача]`). Откройте «Мои задачи» в двух
окнах под разными операторами; создание/переназначение задачи обновит список
адресата без перезагрузки, а чужой список не дёрнется.

## Оповещения и команды интерфейса (ui.*)
<!-- status: testing -->
<!-- date: 2026-07-27 -->

Сервер может из обработки послать пользователю **богатый тост** и **команды
интерфейсу** — аналог «УведомленийКлиента» 1С, но вызов серверный.

- **`ПоказатьОповещениеПользователя(Кому, Заголовок, Текст, Ссылка, Важность)`**
  (алиас `ShowUserNotification`) — тост у пользователя (логин / `"роль:Имя"` /
  `"*"`). `Важность = "важное"` — тост не исчезает сам (закрыть крестиком). `Ссылка`
  = `Структура{вид, сущность, id}` — клик по тосту откроет форму объекта во вкладке.
- **`ОтправитьУведомление(Кому, "ui.обновитьСписок", Структура("сущность","Заказ"))`**
  — императивно «обнови список сейчас» (те же контейнеры, что у живого списка,
  даже без `list_refresh_on`).
- **`ОтправитьУведомление(Кому, "ui.открытьФорму", Структура{вид, сущность, id})`**
  — открыть форму объекта во вкладке у пользователя.

**Как попробовать.** В `examples/callcenter` при отправке заявки на согласование
(`Согласование.ОтправитьНаСогласование`) адресат получает важный тост «Новая
задача согласования», а его список «Мои задачи» обновляется сам. Из любой
обработки: `ПоказатьОповещениеПользователя("ivan", "Готово", "Отчёт собран")`.

## Управление пользователями из CLI (onebase user)
<!-- status: testing -->
<!-- date: 2026-07-31 -->

Заводить пользователей и назначать роли теперь можно **без запущенного сервера** —
командой `onebase user`. Удобно для сгенерированных, демо- и тестовых баз, где
раньше учётки приходилось создавать только вручную через веб-админку.

Операция офлайновая, над файлом/DSN базы (как `migrate`), и проходит через
штатные инварианты платформы (bcrypt, парольная политика, «первый пользователь —
администратор», аудит). Пароль **не передаётся аргументом** (утёк бы в history):
`--generate` печатает случайный пароль, `--password-stdin` читает его из stdin.

- **`onebase user add <login> --name "Имя" [--admin] [--show-in-list] --generate`** —
  создать пользователя (первого — обязательно `--admin`).
- **`onebase user list`** — логин, признак админа, тег `[в списках]`, полное имя,
  назначенные роли.
- **`onebase user show-in-list <login> --on|--off`** — показать или скрыть
  пользователя в списках выбора (reference-пикерах). По умолчанию новый
  пользователь в них **не** показывается — так же, как у формы нового
  пользователя в веб-админке.
- **`onebase user passwd <login> --generate`** — сменить пароль (сбрасывает сессии).
- **`onebase user role assign <login> <Роль>`** / **`role revoke`** — назначить/снять
  роль (роли берутся из `roles/*.yaml` проекта).
- **`onebase user rm <login>`** — удалить (последнего админа/пользователя нельзя).

**Как попробовать.** `onebase migrate --project examples/trade --sqlite base.db`,
затем `onebase user add admin --name Админ --admin --generate --project examples/trade
--sqlite base.db` — в выводе будет сгенерированный пароль. Дальше
`onebase user role assign admin Бухгалтер …` и `onebase user list …`.

## Тесты уровня конфигурации (onebase test)
<!-- status: testing -->
<!-- since: v0.9.8 -->
<!-- date: 2026-07-30 -->

Тесты конфигурации без внешних харнессов: `onebase test` находит обработки, у
которых в YAML указано `kind: test`, выполняет у каждой процедуру `Выполнить()`
офлайн и собирает результаты встроенных проверок `Утверждать.*`. Печатает строку
на проверку и итог, при провале — ненулевой код возврата, поэтому годится в
pre-commit и в CI. Тест-обработки скрыты из пользовательского списка и не
требуют прав в ролях.

Что даёт платформа поверх «обработки с ассертами и `procrun`»:

- **Изоляция данных.** По умолчанию каждый тест идёт в своей транзакции с
  откатом — записи не протекают между тестами и не оседают в базе.
  `--isolation schema` поднимает эфемерную схему PostgreSQL на прогон и удаляет
  её `CASCADE` (для тестов, которые сами управляют транзакциями),
  `--isolation none` — без изоляции.
- **Моки внешних эффектов.** `Мок.Email`, `Мок.Http`, `Мок.ОС`, `Мок.ИИ` —
  рекордеры: письмо, запрос, команда ОС и ИИ-вызов записываются, но наружу не
  уходят. Можно проверить «письмо ушло бы на X с вложением Y», не поднимая SMTP.
- **Управление временем.** `Часы.Установить(Дата)` / `Часы.Сбросить()`
  замораживают `ТекущаяДата()` и `ТекущаяДатаВремя()` — тесты «за N дней до
  окончания» перестают зависеть от календаря. Профиль сбрасывается перед каждым
  тестом.
- **Отчёт для CI.** `--format pretty|tap|junit` и `--out <файл>` — TAP или
  JUnit XML подхватываются CI-системами как есть.
- **Встроенные ассерты.** `Утверждать.Равно/НеРавно/Истина/Ложь/Заполнено/
  Провалить` — не нужно переизобретать ассерт-модуль в каждом проекте. Проверки
  прав, маскирования и RLS — в двух секциях ниже.

**Как попробовать.** Демо лежит в `examples/minimal` (обработки `ТестыДемо` и
`ТестРассылки`):

```bash
onebase test --project examples/minimal --sqlite :memory:
onebase test --project examples/minimal --sqlite :memory: --run Рассылки
onebase test --project examples/minimal --sqlite :memory: --format junit --out report.xml
```

## Тесты прав ролей (Утверждать.РольМожет)
<!-- status: testing -->
<!-- date: 2026-07-31 -->

Харнесс `onebase test` научился проверять **матрицу прав ролей** декларативно —
поверх того же движка прав, что и боевой доступ. Тест на уровне конфигурации,
без запущенного сервера и без заведения пользователей: правишь `roles/*.yaml` —
тест ловит «забыл выдать право» или «случайно выдал лишнее».

```
Утверждать.РольМожет("Кладовщик", "документ", "РеализацияТоваров", "провести", "проводит реализацию");
Утверждать.РольНеМожет("Кладовщик", "справочник", "Организация", "изменять", "не редактирует орг.");
```

- **Вид:** `документ`/`справочник`/`регистр`/`регистрсведений`/`отчёт`/`обработка`
  (принимаются и англоязычные формы).
- **Операция:** `read/write/post/unpost/delete/run` и русские синонимы
  (`провести`→post, `изменять`→write, `удалять`→delete, …).
- Роли берутся из `roles/*.yaml` проекта; неизвестная роль или вид — провал
  проверки с внятным сообщением.

**Как попробовать.** В обработке с `kind: test` добавь пару строк выше про свою
роль и запусти `onebase test --project . --sqlite base.db`.

## Тесты полевого и строкового доступа
<!-- status: testing -->
<!-- date: 2026-07-31 -->

Дополнение к тестам прав: `onebase test` теперь проверяет **маскирование ПДн**
(`field_access`, план 88) и **строковый доступ RLS** (`row_access`, план 79) —
два самых чувствительных слоя, где ошибка означает утечку данных или показ чужих
строк. Ассерты гоняют те же `access.FieldDecisions` / `access.Decide`, что и
боевой enforcement, — не копию логики.

```
// Полевой доступ — маскирование:
Утверждать.ПолеМаскируется("Оператор", "справочник", "Клиент", "Телефон", "телефон под маской");
Утверждать.ПолеВидно("Оператор", "справочник", "Клиент", "Наименование", "имя открыто");
Утверждать.МаскаПоля("Оператор", "справочник", "Клиент", "Телефон", "9161234567", "••••••4567", "keep=4");

// Строковый доступ — RLS-фильтр:
Утверждать.СтрокиОграничены("Оператор", "документ", "Задача", "читать", "видит только свои");
Утверждать.СтрокиНеОграничены("Оператор", "справочник", "Клиент", "читать", "клиентов видит всех");
```

- **`ПолеМаскируется` / `ПолеВидно`** — маскируется/скрывается ли поле при чтении.
- **`МаскаПоля`** — точный результат маски на значении (проверяет сам алгоритм:
  `mask_tail`/`mask_all`/`mask_city`/`hide`).
- **`СтрокиОграничены` / `СтрокиНеОграничены`** — применяется ли RLS-фильтр (что он
  вообще есть; конкретную видимость строки против данных не проверяет).

**Как попробовать.** В `examples/callcenter` обработка `ТестДоступа` проверяет
маскирование телефона/почты и RLS задач у роли Оператор:
`onebase test --project examples/callcenter --sqlite :memory: --run Доступа`.

## Управление секретами (onebase secret)
<!-- status: testing -->
<!-- date: 2026-08-03 -->

Ключи ИИ-провайдеров, пароль SMTP, токены вебхуков, планов обмена и HTTP-сервисов,
креды S3 больше не обязаны лежать в конфигурации и в базе значением. Секрет
записывается **ссылкой**, а значение подставляется в момент использования:

- **`env:ИМЯ`** — переменная окружения процесса;
- **`file:/путь`** — файл (docker/k8s secrets, смонтированный том);
- **`enc:<base64>`** — само значение, зашифрованное AES-256-GCM на мастер-ключе,
  который живёт **вне базы** (`ONEBASE_MASTER_KEY` или `ONEBASE_MASTER_KEY_FILE`).

Те же три вида работают внутри строки — `${env:ИМЯ}`, `${file:/путь}`, `${enc:…}` —
когда секрет лишь часть значения: URL вебхука с токеном, заголовок `Authorization`.

**Зачем.** Обычная резервная копия снимается с базы целиком (`pg_dump` на PostgreSQL,
`VACUUM INTO` на SQLite) и уносит всё, что лежит в служебной таблице настроек. Раньше
ключ ИИ, аккуратно вынесенный в `${env:...}` в `app.yaml`, всё равно оказывался в базе
**значением** — и уезжал в копию открытым текстом. Теперь в базе остаётся ссылка;
копия без мастер-ключа бесполезна.

**Команды.**

- **`onebase secret keygen`** — сгенерировать мастер-ключ (печатает и его отпечаток).
- **`onebase secret encrypt --stdin`** — зашифровать значение и получить `enc:…`
  для вставки в YAML.
- **`onebase secret set <путь> --stdin|--generate`** — положить секрет в базу
  зашифрованным: `llm.<endpoint>.api_key`, `exchange.token.<план>`,
  `_settings:<ключ>`. Значение никогда не передаётся аргументом командной строки —
  оно осело бы в history и в выводе `ps`.
- **`onebase secret list`** — инвентаризация: что где лежит и в каком виде
  (окружение / файл / зашифровано / **ОТКРЫТЫЙ ТЕКСТ**), без самих значений.
- **`onebase secret rotate --new-key-stdin [--dry-run]`** — перешифровать
  `enc:`-значения базы на новый ключ. Идемпотентно: прерванный прогон можно повторить.

**Что ещё изменилось.** `onebase check` предупреждает о секретах, записанных открытым
текстом (код `secret.plaintext`), а `onebase backup` печатает, какие секреты уедут
в создаваемый файл. Если мастер-ключ не задан, подсистема с `enc:`-секретом
выключается с понятным сообщением, а сервер продолжает работать: ИИ-модель выбывает
из цепочки профиля, доставка вебхука отменяется, HTTP-сервис отвечает 401.

**Как попробовать.**

```bash
export ONEBASE_MASTER_KEY=$(onebase secret keygen | head -1)
onebase secret set llm.z_ai.api_key --stdin --project . --sqlite base.db < ключ.txt
onebase secret list --project . --sqlite base.db     # ключ показан как «зашифровано»
onebase backup --sqlite base.db --out ./backups      # предупреждений о нём больше нет
```

## Переименование реквизита без потери данных (реструктуризация)
<!-- status: testing -->
<!-- date: 2026-08-03 -->

Раньше миграция умела только **добавлять** колонки. Переименование реквизита в
конфигурации выглядело для неё как «одно поле исчезло, другое появилось»: заводилась
новая **пустая** колонка, а накопленные данные оставались в старой, невидимой
приложению. Смена типа реквизита не применялась вовсе — молча.

Теперь у реквизита есть необязательный **устойчивый `id`**, и по нему миграция
отличает переименование от «удалить и создать»:

```yaml
fields:
  - { id: f_price, name: ЦенаПродажи, type: number(15,2) }   # было: name: Цена
```

`onebase migrate` переименует колонку вместе с данными, преобразует значения при смене
типа и **не** удалит колонку выбывшего реквизита без явного разрешения.

**Как посмотреть, что будет.**

```bash
onebase migrate --project . --sqlite base.db --dry-run
```

```
План реструктуризации (2 изменен.):
  товары: переименовать колонку цена → ценапродажи (поле ЦенаПродажи, данные сохраняются)
  товары: удалить колонку комментарий вместе с данными (поле с id f_note убрано из конфигурации)
      данные колонки будут потеряны безвозвратно

Это пробный прогон — база не изменена.
Изменений с потерей данных: 1 — они выполнятся только с --allow-destructive.
```

**Что важно знать.**

- **Смена типа отменяется до потери данных.** Если в текстовой колонке есть значения,
  которые не станут числом, миграция откажется и покажет примеры — вместо того чтобы
  обнулить их (SQLite приводит `«abc»` к нулю молча) или упасть посреди работы.
- **Удаление — только по `--allow-destructive`.** Без флага колонка остаётся в базе, а
  миграция печатает `ПРОПУЩЕНО` и объясняет, как её всё-таки удалить.
- **Сужение точности числа — тоже «с потерей данных».** `number(15,2)` →
  `number(15,0)` округлит копейки, поэтому такое изменение требует того же
  `--allow-destructive`. Проверка значений тут не спасает: «10.55» — законное
  число, вопрос лишь в том, сколько знаков останется. Учтите, что разрядность в
  `number(L,S)` считает **все** знаки: перейти с `number(15,0)` на `number(15,2)`
  значит отдать копейкам два знака целой части, и значения от 10¹³ перестанут
  влезать.
- **Запуск сервера ничего разрушительного не применяет.** `onebase run` мигрирует
  базу при каждом старте, но изменения с потерей данных откладывает и печатает
  списком: колонки остаются как были, сервер работает на прежней схеме. Применить
  их можно только явной командой `onebase migrate --allow-destructive` — то есть
  правка типа реквизита в конфигураторе не изменит боевые данные сама собой на
  ближайшем перезапуске.
- **Ничего чужого миграция не трогает.** Реструктурируются только колонки, о которых
  платформа сама знает, что они принадлежали реквизиту с `id`.
- **Конфигурации без `id` работают как раньше** — аддитивно. Чтобы перевести
  существующий объект на устойчивые идентификаторы, достаточно один раз открыть и
  сохранить его в конфигураторе: он проставит `id` всем реквизитам и привяжет их к
  текущим колонкам, ничего не меняя в данных.
- `onebase refactor rename-field` теперь честно говорит, что будет с данными: сохранятся
  (у реквизита есть `id`) или останутся в осиротевшей колонке.

## Двухфакторная аутентификация и единый вход (2FA / SSO)
<!-- status: testing -->
<!-- date: 2026-08-05 -->
<!-- issue: 577 -->

Корпоративный минимум входа — **строго по желанию**: база, где ничего не включено,
работает ровно как раньше (логин + пароль).

**Второй фактор (TOTP).** «Профиль → Второй фактор» → «Включить» → QR-код для
приложения-аутентификатора (Google Authenticator, Aegis, 1Password и совместимые)
→ подтверждение кодом. После включения вход состоит из двух шагов: пароль, затем
шестизначный код; сессия не выдаётся, пока код не предъявлен. Вместе с включением
выпускаются **резервные коды** — по одному разу каждый, на случай недоступного
телефона (перевыпуск — там же в профиле). Код нельзя предъявить дважды: шаг
использованного кода запоминается. Секрет TOTP хранится в базе зашифрованным
мастер-ключом (см. «Управление секретами»); без мастер-ключа 2FA тоже работает,
но профиль об этом предупредит.

**Политики (Администрирование → Аутентификация).**

- «Требовать второй фактор от администраторов» и список ролей, которым он
  обязателен: такая учётная запись без 2FA не войдёт. Привязать фактор на входе по
  одному паролю **нельзя** — для утёкшего пароля это было бы закрепление доступа,
  после которого владелец не войдёт вовсе. Администратор выдаёт пользователю
  одноразовый **код привязки** в его карточке (Администрирование → Пользователи →
  «Выдать код привязки»; код действует сутки, новый заменяет прежний), пользователь
  вводит его после пароля — и только тогда открывается QR настройки. Флаг
  «Разрешить самостоятельную привязку второго фактора на входе» (там же, в политиках)
  возвращает прежнее поведение — QR сразу после пароля, без кода — для установок, где
  2FA раздают массово. **Вход через SSO** привязку разрешает сразу и без кода:
  личность подтвердил провайдер, а не один пароль;
- «Только единый вход» — локальные пароли не принимаются. Аварийный вход по паролю
  включается переменной окружения `ONEBASE_ALLOW_PASSWORD_LOGIN=1` у процесса базы:
  неработающий провайдер не должен запирать базу насовсем. Включить политику, пока
  нет ни одного работающего провайдера, платформа не даст. API-токены REST v2 —
  отдельный канал для сервисов, политикам входа они не подчиняются.

Политика действует и в конфигураторе: администратор с включённой 2FA вводит код и
там, а требование настроить 2FA отправит его сначала в Предприятие. Действует она и
на HTTP-сервисы с `auth: basic`: при «только едином входе» пароль там не принимается,
а учётной записи со вторым фактором одного пароля мало — второй фактор в этом потоке
предъявить негде, поэтому для интеграций остаётся API-токен REST v2.

**Потеря аутентификатора.** В карточке пользователя (Администрирование →
Пользователи) администратор видит состояние второго фактора и может его сбросить —
пользователь настроит фактор заново при следующем входе (тем же путём: по коду
привязки из карточки, либо сразу, если включена самопривязка). Без этого потеря
телефона при израсходованных резервных кодах означала бы потерю доступа, а при
«требовать второй фактор от администраторов» и единственном администраторе —
неадминистрируемую базу. Если под обязательной политикой без фактора остался
единственный администратор (кода привязки выдать некому), самопривязку можно
временно включить флагом политики.

**Резервные коды** — 16 символов (`abcd-efgh-ijkl-mnop`). Длина выбрана из расчёта на
оффлайн-перебор: тот, кто получил копию файла базы, перебирает хэши у себя, и прежних
8 символов для этого не хватало.

**Восстановление из `.obz`.** Резервные коды переносятся вместе с учётными записями.
Секрет TOTP зашифрован мастер-ключом исходной установки: если восстановление идёт под
другим ключом, разыменовать его нельзя ничем, поэтому второй фактор у таких учёток
гасится, а `restore` сообщает, у кого именно — иначе учётка оказалась бы заперта
(пароль принят, код предъявить невозможно).

**Единый вход (OIDC).** «Администрирование → Аутентификация → Добавить провайдера»:
issuer, `client_id`, `client_secret` (можно ссылкой `env:ИМЯ` — значение
шифруется мастер-ключом), scope'ы. Метаданные читаются из
`<issuer>/.well-known/openid-configuration`, поток — Authorization Code + PKCE с
проверкой подписи `id_token` по JWKS, `iss`/`aud`/`exp`/`nonce`. Работает с
Keycloak, Google Workspace, Microsoft Entra ID и любым совместимым провайдером.
На форме входа появляется кнопка «Войти через …».

- **Связывание с существующей учёткой** возможно только по **подтверждённой почте**:
  провайдер обязан прислать `email_verified: true`, и адрес должен совпадать с логином.
  Отсутствие claim подтверждением не считается — иначе каталог, который его не
  присылает (так делает Microsoft Entra ID), позволял бы представиться нашим
  администратором. Учётку с логином не из почты (`preferred_username` и подобные)
  связывает только администратор вручную; автосоздание новых записей это не
  ограничивает.
- **Учётные записи.** Пользователь находится по паре (провайдер, `sub`), затем по
  логину из claim'а (по умолчанию `email`, подтверждённая почта); при включённом
  «Создавать учётные записи при первом входе» заводится новая.
- **Роли по claim'ам.** Правила вида `groups = erp-buh -> Бухгалтерия` (справа
  можно указать `*admin` — выдать права администратора). Роли из правил
  синхронизируются при каждом входе: исчезла группа у провайдера — снимается и
  роль. Роли, назначенные администратором вручную, провайдер не трогает.
- **Второй фактор при SSO.** По умолчанию локальная политика 2FA действует и после
  входа через провайдера; если MFA обеспечивает сам провайдер — поставьте у него
  флаг «Провайдер сам обеспечивает второй фактор».

**Адрес возврата.** `redirect_uri` для регистрации у провайдера показан прямо в
карточке провайдера. За обратным прокси публичный адрес задаётся переменной
`ONEBASE_PUBLIC_URL` (например `https://erp.example.com`).

**Как попробовать.** Включите 2FA у своей учётной записи и войдите заново — пароль,
затем код; проверьте вход резервным кодом. Для SSO поднимите Keycloak (realm,
конфиденциальный клиент с адресом возврата из карточки), заведите провайдера и
нажмите «Войти через …».
## Тестирование и исправление базы (onebase doctor)
<!-- status: testing -->
<!-- date: 2026-08-03 -->

Одна команда проверяет информационную базу по шести осям и, по явному разрешению,
чинит найденное — аналог «Тестирования и исправления» в 1С. Часть проверок
существовала и раньше, но жила порознь: пересчёт итогов отдельной командой,
сборка бинарников другой, а удаление движений с удалённым регистратором вообще
только в веб-админке — то есть базу, которую не удаётся поднять, чинить было нечем.

| Проверка | Что находит | Чинит |
|---|---|---|
| `integrity` | повреждение файла базы (`PRAGMA integrity_check` на SQLite) | — восстановление из копии |
| `schema` | расхождение схемы таблиц с метаданными | — `onebase migrate` |
| `refs` | ссылки на несуществующие объекты («объект не найден») в реквизитах, табличных частях, измерениях регистров и субконто бухрегистра | очищает ссылку; субконто и ресурсы бухрегистра — только показывает |
| `codes` | пустые и повторяющиеся коды справочников и номера документов | — `onebase renumber` |
| `orphan-movements` | движения с удалённым документом-регистратором | удаляет и пересчитывает итоги |
| `totals` | итоги регистров разошлись с движениями | пересчитывает |
| `blobs` | бинарники, на которые никто не ссылается | удаляет |

**По умолчанию команда ничего не меняет.** Починка запускается только для проверок,
названных в `--fix` явно: каждая из них меняет данные, и делать это «заодно с
диагностикой» нельзя.

```bash
onebase doctor --project . --sqlite base.db          # отчёт
onebase doctor --check refs,totals --sqlite base.db  # только нужное
onebase doctor --fix refs --sqlite base.db           # починить ссылки
onebase doctor --json --sqlite base.db               # машинный отчёт
```

Отчёт называет объект, количество и примеры — по нему понятно, что чинить, даже
если чинить решено руками:

```
[ОШИБКА] Ссылочная целостность — битых ссылок: 3 в 1 поле(ях)
         Реализация.Контрагент: ссылка на несуществующий объект (3)
             например: 0f1e2d3c-4b5a-6978-8796-a5b4c3d2e1f0
         → onebase doctor --fix refs — очистить битые ссылки (значение станет пустым)
```

Не всё чинится автоматически, и это осознанно. Измерение регистра сведений
занулить нельзя — оно входит в первичный ключ. Субконто и ресурс регистра
бухгалтерии занулить технически можно, но это меняет аналитику уже проведённой
операции, поэтому `--fix refs` их не трогает, а называет вслух:

```
БухУчёт.Контрагент: 2 битых ссылок, субконто бухрегистра — очистка меняет
аналитику проводки, разберите вручную
```

Код возврата: `0` — чисто или только предупреждения, `1` — найдены ошибки; годится
для мониторинга и CI.

## Сжатие базы после массового удаления (onebase vacuum)
<!-- status: testing -->
<!-- date: 2026-08-04 -->

Свёртка регистров (`onebase rollup`) и сборка бинарников (`onebase gc-blobs --delete`)
удаляют помногу, а место после удаления само не возвращается:

- **SQLite** отдаёт освободившиеся страницы во внутренний свободный список, но файл
  на диске не укорачивает;
- **PostgreSQL** уберёт мёртвые строки автовакуумом, но статистика планировщика
  после удаления миллиона строк остаётся старой, и запросы какое-то время строятся
  по несуществующему распределению данных.

```bash
onebase vacuum --sqlite base.db
onebase vacuum --db postgres://localhost/onebase
```

На SQLite команда выполняет `VACUUM` и контрольную точку журнала (без неё файл не
укорачивается — новое содержимое остаётся в WAL) и печатает, сколько места
освободилось. На PostgreSQL — `VACUUM (ANALYZE)` без `FULL`: `FULL` переписывает
таблицы под эксклюзивной блокировкой, и решать за администратора, что базу можно
остановить, платформа не вправе.

Обслуживание отдельной командой, а не шагом внутри свёртки: на SQLite это
исключительная блокировка базы и вторая копия файла на диске. Свёртка и сборка
бинарников теперь сами напоминают о нём, когда действительно что-то удалили.

## Глобальный (полнотекстовый) поиск по базе
<!-- status: testing -->
<!-- date: 2026-08-04 -->

Поиск в OneBase был **пообъектным**: сначала откройте нужный справочник или журнал,
потом фильтруйте список. Сценарий «где-то был контрагент Ромашка / счёт на 15000 /
телефон +7999…, а в каком объекте — не помню» закрывать было нечем.

Теперь в шапке интерфейса есть строка поиска: один запрос проходит по всем
справочникам и документам сразу, результат ведёт прямо в карточку объекта.

**Что попадает в индекс.** По умолчанию — все строковые реквизиты шапки (у документа
и синтезированный `Номер`). Состав настраивается в метаданных объекта:

```yaml
name: Контрагент
fields:
  - { name: Наименование, type: string }
  - { name: Комментарий, type: string }
  - { name: Телефон, type: string }
fulltext: [Наименование, Телефон]   # искать только по этим реквизитам
```

`fulltext: []` убирает объект из глобального поиска целиком.

**Поиск с подстановкой.** Слова ищутся по началу: «рома» находит «Ромашку». Несколько
слов соединяются по И — объект должен содержать все. Знаки препинания внутри значения
разделяют слова, поэтому «РН-000012» находится и по «000012», а «+79990001122» — по
«79990001122». На PostgreSQL дополнительно
работает морфология (словарь `russian`): «договоры» находится по «договор». На SQLite
морфологии нет — там выручает поиск по началу слова.

**Права соблюдаются.** В выдачу не попадает то, чего пользователь не увидел бы в
списке: объекты без права чтения, строки, отсечённые строковыми политиками, и
совпадения по замаскированным реквизитам (иначе поиском можно было бы подбирать
скрытый телефон или ИНН).

**Из обработок:**

```
Найдено = ПолнотекстовыйПоиск("Ромашка договор", 20);
Для Каждого Стр Из Найдено Цикл
    // Объект — имя справочника/документа, Ссылка — обычная ссылка DSL,
    // с ней работают ПолучитьОбъект, запросы и ЗначениеРеквизитаОбъекта.
    Сообщить(Стр.Объект + ": " + Стр.Представление);
КонецЦикла;
```

Отбор по реквизиту и типам объектов можно включить в сам запрос индекса — до
ранжирования и лимита. Это важно для многосайтовости: постфильтр после top-N
позволил бы совпадениям соседнего сайта вытеснить всю локальную выдачу.

```
Найдено = ПолнотекстовыйПоиск("доставка", 30, "Сайт", Сайт.Ссылка,
    ["Страницы", "Новости"]);
```

**Из REST:** `GET /api/v2/search?q=ромашка&limit=20` — те же права. Следующая
страница берётся по `meta.next_cursor`: `…&cursor=<значение>`. Курсор непрозрачен
намеренно, числовое смещение снаружи не принимается — позиция чтения считается по
просмотренным строкам индекса, то есть ДО отсева правами, и в виде числа сообщала
бы, что совпадение было, хотя видеть его нельзя.

**Обслуживание.** Индекс наполняется сам при записи объектов. Пересобрать его нужно
после смены блока `fulltext:`, после заливки данных мимо платформы или для починки:

```bash
onebase reindex --project . --sqlite base.db
onebase reindex --project . --sqlite base.db --entity Контрагент
```

Существующая база получает схему поиска при обычном `onebase migrate` — он же один
раз наполняет индекс уже накопленными данными, поэтому поиск работает сразу после
обновления. Восстановление из `.obz` пересобирает индекс само.

## Сообщить об ошибке одним файлом
<!-- status: testing -->
<!-- date: 2026-08-07 -->

Чтобы пожаловаться на ошибку, до сих пор нужен был аккаунт на GitHub. У пользователя
прикладной конфигурации — бухгалтера, оператора, кладовщика — его обычно нет и не
будет, поэтому всё, что доходило до разработчика, — это пересказ «не работает» в чате.
Даже добросовестный пользователь не знал, что приложить: версия видна только на экране
«О программе», журнал лежит в `~/.onebase/logs` и из интерфейса не открывается ничем.

Теперь есть кнопка **«Сообщить об ошибке»**. Она собирает готовый отчёт: описание своими
словами, техническую расшифровку последней ошибки со стеком, версию платформы, ОС, СУБД,
имя и версию конфигурации, по желанию — хвост журналов. Дальше отчёт показывается целиком,
его можно поправить и сохранить файлом.

**Платформа ничего не отправляет.** Ни одного исходящего соединения не появляется: файл
отправляет пользователь — почтой, в чат, любым каналом, который у него есть. Поэтому
кнопка работает и в закрытом контуре.

**Код инцидента.** Каждая ошибка и каждая внутренняя авария получают короткий код вида
`E-3F7A2C`. Он показывается прямо в сообщении об ошибке — его можно продиктовать по
телефону, а в форме отчёта выбрать из списка: стек и место подставятся сами. Список
ведётся за текущий сеанс работы сервера, после перезапуска он пуст.

**Как попробовать.**

- В Предприятии: **Система → Сообщить об ошибке** (или кнопка на «О программе»).
  Заполните три поля, нажмите «Сформировать отчёт», проверьте текст и нажмите
  «Скачать файл» либо «Скопировать текст».
- В лаунчере: кнопка **«Сообщить об ошибке»** в панели инструментов. Здесь отчёт
  полнее — лаунчер приложит журнал выбранной базы, и он работает даже когда база не
  запустилась. «Сохранить пакет…» кладёт zip в `~/.onebase/reports` и открывает папку.
- Без интерфейса (база не поднялась, работа по RDP):

```bash
onebase support --message "не проводится реализация" --project .
onebase support --base <ID базы> --out C:\tmp\report.zip
onebase support --no-logs
```

**Проверьте текст перед отправкой.** В сообщениях об ошибках могут оказаться значения
из ваших документов — поэтому отчёт и показывается целиком в редактируемом поле. Пароли
и строки подключения вычищаются автоматически, но всё остальное решаете вы.

**Куда отправлять.** Если конфигурацию вам поставил внедренец, укажите его контакт в
`config/app.yaml` — он будет первым в списке (в конфигураторе это поле «Поддержка» в
свойствах конфигурации):

```yaml
name: Торговля
support: help@vasha-firma.ru, тел. 8-800-000-00-00
```

Журнал сервера и пути к файлам в Предприятии видит только администратор: обычный
пользователь отправляет отчёт с кодом инцидента, а журнал по этому коду достаёт
администратор.

## Состав колонок табличной части и события на колонке
<!-- status: testing -->
<!-- date: 2026-08-24 -->

Табличная часть показывала все свои реквизиты подряд, а событие было одно на всю
таблицу — колонку различали внутри обработчика по `ТекущаяКолонка`. Чекбоксы
«Колонки (показывать)» в конструкторе при этом уже были, но на форму не влияли:
рантайм их не читал. Теперь читает, и у колонки появилось своё событие.

**Как настроить.** Вложите в табличную часть элементы `kind: Колонка` — они
задают состав и порядок показа:

```yaml
- kind: ТабличнаяЧасть
  name: ТабТовары
  data_path: Объект.Товары
  auto_sum: true
  children:
    - kind: Колонка
      name: КолНоменклатура
      data_path: Объект.Товары.Номенклатура
    - kind: Колонка
      name: КолКоличество
      data_path: Объект.Товары.Количество
      events:
        ПриИзменении: КоличествоПриИзменении   # событие ЭТОЙ колонки
```

Обработчик получает тот же контекст, что и событие таблицы:

```
Процедура КоличествоПриИзменении()
    ТекущаяСтрока.Сумма = ТекущаяСтрока.Количество * ТекущаяСтрока.Цена;
КонецПроцедуры
```

Что важно знать:

- **Ни одной колонки-ребёнка = показать все реквизиты.** Существующие формы
  выглядят как раньше, ничего переписывать не нужно.
- **Невыбранный реквизит не исчезает из документа, он лишь перестаёт
  показываться.** Значение по-прежнему сохраняется и остаётся в базе — убрать
  служебную колонку с формы и продолжать заполнять её проведением можно
  спокойно.
- Обработчик колонки **не отменяет** обработчик таблицы: объявлены оба —
  выполняются последовательно, сначала колонка.
- События ячейки работают в гриде; при `no_grid: true` их нет, и `onebase check`
  об этом скажет, а не промолчит.

**Как попробовать.** Откройте конструктор форм (**◇ Управляемые формы**),
выберите табличную часть — в панели свойств появились галочка «Считать Сумма =
Количество × Цена», список «Колонки (показывать)» и все шесть событий таблицы.
Кликните колонку — у неё своё `ПриИзменении`. Живой пример —
`examples/trade/forms/заказпокупателя/`.

## Колонка табличной части с реквизитом по ссылке
<!-- status: testing -->
<!-- date: 2026-08-15 -->
<!-- issue: 845 -->

В строке табличной части лежит ссылка — на номенклатуру, клиента, договор, — а
рядом нужно видеть её реквизит: артикул, код, дату. Раньше для этого заводили
лишний реквизит в самой табличной части и заполняли его вручную из обработчика.
Теперь колонку можно объявить **виртуальной**: она показывается, но не хранится
и колонок в базе не создаёт.

**Как настроить.** На элементе табличной части управляемой формы:

```yaml
- kind: ТабличнаяЧасть
  name: Товары
  data_path: Объект.Товары
  virtual_columns:
    - name: АртикулНоменклатуры        # идентификатор колонки внутри формы
      data_path: Номенклатура.Артикул  # <ссылка в строке>.<реквизит цели>
      title:
        ru: "Артикул"
      width: 90
```

Путь состоит ровно из двух частей: ссылочный реквизит ЭТОЙ табличной части и
реквизит объекта, на который он ссылается. Опечатку в пути — несуществующий
реквизит, нессылочный первый сегмент, имя, занятое реквизитом табличной части, —
отклоняет `onebase check`, а не браузер.

Тип показываемого реквизита ограничений не накладывает: значение форматируется по
своему типу и выглядит одинаково на SQLite и на PostgreSQL. Дата показывается как
`14.03.1985`, флажок — галочкой, перечисление — подписью значения, размеченный
текст — без тегов. Если показываемый реквизит сам ссылочный, в колонке будет
представление объекта, а не его идентификатор: путь `Клиент.Город` покажет
«Москва».

Колонка только для чтения: редактора у неё нет, и при записи документа она не
отправляется. Значения читаются пачками — по одному запросу на колонку, а не на
строку, — и обновляются после серверных событий строки. Реквизиты, закрытые от
пользователя правами или маской персональных данных, в колонке не показываются.

**Как попробовать.** Допишите `virtual_columns` в форму документа, выполните
`onebase check --project <dir>` и откройте документ — колонка появится справа от
собственных реквизитов табличной части.

## Реакция на переход по строкам табличной части
<!-- status: testing -->
<!-- date: 2026-08-11 -->
<!-- issue: 670 -->

Событие **`ПриАктивизацииСтроки`** на элементе табличной части выполняется, когда
пользователь переходит на другую строку. Обработчик видит `НомерСтроки`,
`ИндексСтроки`, `ИмяТабличнойЧасти` и саму строку в `ТекущаяСтрока`, а изменённые
им реквизиты формы применяются к открытой форме без перезагрузки. Это то, чем
делается «карточка текущей строки»: надпись, картинка или расшифровка,
пересчитываемые при движении по табличной части.

```yaml
# forms/заказпокупателя/ФормаОбъекта.form.yaml
- kind: ТабличнаяЧасть
  name: Товары
  data_path: Объект.Товары
  events:
    ПриАктивизацииСтроки: ТоварыПриАктивизацииСтроки
```

```
// ФормаОбъекта.form.os
Процедура ТоварыПриАктивизацииСтроки()
    Объект.ПояснениеПоСтроке = "" + ТекущаяСтрока.Номенклатура
        + ": остаток " + ОстатокНаСкладе(ТекущаяСтрока.Номенклатура);
КонецПроцедуры
```

Событие объявлялось в метаданных и раньше, но никогда не вызывалось: обработчик
можно было описать, а сработать он не мог. Теперь клиент подписывается на смену
строки — но **только если обработчик объявлен**, и с задержкой в четверть
секунды: без этого прокрутка стрелкой через полсотни строк дала бы полсотни
запросов подряд. Переход между колонками внутри одной строки активизацией не
считается — как и в 1С.

## Обращение к полю через ссылку в запросе
<!-- status: testing -->
<!-- date: 2026-08-10 -->
<!-- issue: 705 -->

Взять наименование связанного объекта прямо в запросе можно и без ручного
соединения: `ВЫБРАТЬ Профиль.Наименование` разворачивается в `LEFT JOIN`
автоматически. Привычная по 1С запись с именем источника впереди раньше не
работала — запрос

```
ВЫБРАТЬ Сигналы.Профиль.Наименование ИЗ Справочник.Сигналы
```

падал с `no such column: сигналы.профиль_id.наименование`: путь уходил в SQL
дословно, и разработчик видел имя несуществующей колонки, из которого не
следует, что именно платформа не поняла. Теперь все три формы равнозначны:

```
ВЫБРАТЬ Профиль.Наименование          ИЗ Справочник.Сигналы
ВЫБРАТЬ Сигналы.Профиль.Наименование  ИЗ Справочник.Сигналы
ВЫБРАТЬ С.Профиль.Наименование        ИЗ Справочник.Сигналы КАК С
```

Работает во всех секциях: проекция, `ГДЕ`, `СГРУППИРОВАТЬ ПО`, `УПОРЯДОЧИТЬ ПО`.

**Второй переход по ссылке** (`Профиль.Владелец.Наименование`) не
поддерживается — но об этом сообщает ошибка компиляции, которая называет и
предел, и обход: выбрать ссылку целиком и дочитать поля вторым запросом либо
соединить таблицу явно через `СОЕДИНЕНИЕ`.

## Временные файлы, URL-кодирование и остаток от деления

<!-- status: testing -->
<!-- date: 2026-08-11 -->

Три небольшие вещи, которых не хватало обработкам, работающим с файлами и внешними сервисами.

**Временные файлы.** Файловые функции были, а временного пути для них — нет, и конфигурации
приходилось придумывать каталог самим:

```
Путь = ПолучитьИмяВременногоФайла("xml");   // имя уникально, файл не создаётся
ПутьTMP = ПолучитьИмяВременногоФайла();      // без аргумента расширение .tmp
Каталог = КаталогВременныхФайлов();          // всегда оканчивается разделителем ОС
Разделитель = ПолучитьРазделительПути();
```

Имя содержит 128 случайных бит. Без аргумента или с `Неопределено` используется расширение `.tmp`,
а явно переданная пустая строка оставляет имя без расширения. В заданном расширении разрешён только
безопасный суффикс до 64 байт;
путь, `..`, двоеточие и управляющие символы отклоняются. В файловой path-песочнице каталогом
служит приватный `<корень>/tmp`. И обычный, и sandbox-каталог оканчиваются нативным разделителем,
поэтому совместимый код может напрямую дописать имя файла. Профиль `DenyFile` вообще не выдаёт
временные пути.

`ПолучитьИмяВременногоФайла` возвращает только свободное имя: файл автоматически не создаётся и
не удаляется. Если обработка создала его сама, очистите путь явно через `УдалитьФайлы(Путь)`.

**URL-кодирование.** Параметр с пробелом или кириллицей ломал адрес — теперь его можно закодировать:

```
URL = "https://api.example.com/search?q=" + КодироватьСтроку(Запрос);
Адрес = КодироватьСтроку(ИсходныйURL, СпособКодированияСтроки.URLВКодировкеURL,
    КодировкаТекста.UTF8);
```

Реальный API 1С `СпособКодированияСтроки.URLВКодировкеURL` (а также короткое имя
`URLВКодировкеURL`) сохраняет структуру пути, если кодируется не значение, а адрес целиком.
Английские эквиваленты — `StringEncodingMethod.URLInURLCoding` и `URLInURLCoding`;
для совместимости также принимается вариант `URLInURLEncoding`.
Необязательная третья кодировка принимает `КодировкаТекста.UTF8`, `TextEncoding.UTF8` и строковые
варианты `UTF8`/`UTF-8`; другие кодировки отклоняются, а не молча игнорируются.
Поведение сверено с 1С:Предприятие прогоном на живой базе: пробел кодируется как `%20` (не «+»),
а при раскодировании «+» остаётся плюсом. Опечатка в способе не принимается; percent-строка,
дающая невалидный UTF-8, возвращается без декодирования. Ошибки аргументов и создания временного
каталога — обычные исключения DSL, которые можно обработать через `Попытка/Исключение`.

**Остаток от деления** — оператор `%` с тем же приоритетом, что умножение и деление:

```
Если Номер % 2 = 0 Тогда   // чётный
```

Операторы `%` и `/` до вычисления проверяют безопасный диапазон Decimal: экстремальная экспонента
возвращает обычную ловимую ошибку DSL вместо panic или неконтролируемого выделения памяти.

**Как попробовать.** Любая обработка: выгрузите данные во временный файл, соберите адрес с
параметром и проверьте чётность счётчика — раньше для каждой из трёх задач требовался обходной путь.

## Сдвиг даты по времени суток и `Число(Дата)`
<!-- status: testing -->
<!-- date: 2026-08-10 -->
<!-- issue: 707 -->

Сдвинуть дату по календарю можно было давно — `ДобавитьДень`, `ДобавитьМесяц`,
`ДобавитьГод`. Сдвиг внутри суток появился в `ДобавитьСекунд/ДобавитьМинут/
ДобавитьЧас`; теперь приняты `ДобавитьСекунду`, `ДобавитьМинуту` и
`ДобавитьЧасов`, чтобы форму имени не приходилось угадывать:

```
СледующаяПопытка = ДобавитьСекунду(ТекущаяДатаВремя(), 30);
Крайний          = ДобавитьЧас(ТекущаяДатаВремя(), 4);
Половина         = ДобавитьМинуту(Начало, 0.5);   // дробь учитывается — 30 секунд
```

Все секундные формы, включая `Дата + Число`, проверяют итоговый год: выход за
`0000`–`9999` возвращает обычную ловимую ошибку DSL до JSON-сериализации.

Календарные сдвиги (`ДобавитьДень/Месяц/Год`) целочисленные и усекают дробь к
нулю. Патологически большое значение либо результат за пределами годов
`0000`–`9999` возвращают обычную ловимую ошибку DSL, не повреждённую дату.

**`Число(Дата)` перестал молчать.** Раньше он возвращал 0 — и это читалось как
«дата пустая», из-за чего арифметику времени начинали строить на нуле. Теперь
он даёт `ГГГГММДДЧЧММСС`. `Дата(Число(Д))` восстанавливает календарные
компоненты непустой даты до секунд, в том числе для годов `0001`–`0999`.
Часовой пояс и доли секунды число не кодирует, пустая дата остаётся нулём.
Формат рассчитан на годы `0001`–`9999`; дата за этой границей также даёт 0,
чтобы не возвращать число без обратного преобразования. Это **не** секунды от эпохи: интервал между датами —
это `Д2 - Д1` или `РазностьДат(Д1, Д2, "секунда")`, а сдвиг —
`ДобавитьСекунд`.

Заодно уточнена справка по конструктору: компоненты сверх диапазона он
**нормализует** (`Дата(2026, 5, 11, 10, 30, 90)` → `10:31:30`), а месяц или
день меньше 1 прижимает к 1. Нормализация за пределы годов `0000`–`9999`
завершается ловимой ошибкой; нераспознанная строка возвращает пустую дату.

## XML в модулях: чтение, запись и примитивы

<!-- status: testing -->
<!-- date: 2026-08-10 -->

Обмен с внешними системами часто идёт в XML, а в модулях до сих пор был только JSON —
приходилось собирать документ конкатенацией строк и разбирать поиском подстрок.

Теперь XML устроен так же, как JSON: пара «прочитать/записать» работает со строкой и обычными
коллекциями, без объектов-потоков.

**Как попробовать.** В обработке:

```
Дерево = ПрочитатьXML("<Заказ Номер=""7""><Строка>Стол</Строка></Заказ>");
Сообщить(Дерево.Имя);                          // Заказ
Сообщить(Дерево.Атрибуты.Получить("Номер"));   // 7
Сообщить(Дерево.Элементы[0].Текст);            // Стол

Сообщить(ЗаписатьXML(Дерево));                 // структурно эквивалентный документ
```

Разобранный документ — Структура с полями `Имя`, `Атрибуты` (Соответствие), `Текст` и
`Элементы` (Массив таких же структур). `ЗаписатьXML` принимает и такое дерево, и произвольную
Структуру/Соответствие/Массив — тогда вложенные элементы называются по именам полей, а имя
корня задаётся вторым параметром.

Разбор fail-closed: неподдерживаемая конструкция не исчезает молча, а даёт понятную ошибку.
Сейчас принимается один корневой элемент без `DOCTYPE`, комментариев, инструкций обработки и
смешанного содержимого (`текст<Тег/>текст`). Пробельное
форматирование между дочерними элементами отбрасывается, пробелы текстового элемента сохраняются.

Пространства имён сохраняются лексически: в `Имя` попадает квалифицированное имя как в документе
(`v8msg:Body`), а `xmlns`/`xmlns:префикс` остаются обычными атрибутами — префиксы в URI не
разрешаются. Объявление XML в начале документа допускается (только `version="1.0"` и `UTF-8`),
отбрасывается и при записи не восстанавливается.
Вне корня допускаются только буквальные XML-пробелы (не ссылки и не CDATA); UTF-8 BOM в начале
разрешён.
Глубина ограничена 256 уровнями, количество элементов — 100 000, атрибутов элемента — 10 000,
атрибутов всего — 100 000.
Исходный и готовый документы, а также суммарные имена/текст/атрибуты ограничены 16 МиБ каждый.

При записи имена проверяются по XML 1.0 `NameStartChar`/`NameChar`; одно двоеточие допускается как
разделитель префикса и локального имени, обе части проверяются как `NCName`. Символы проверяются,
а текст и атрибуты экранируются XML-кодировщиком.
Структура с полем `Имя` всегда считается XML-деревом: его `Атрибуты` должны быть Соответствием
строк, `Текст` — строкой, `Элементы` — Массивом узлов; неизвестные поля дают ошибку. Записанная
строка может отличаться форматированием (`<Пустой/>` → `<Пустой></Пустой>`), сохраняя структуру.
Ключи произвольного Соответствия должны быть строковыми именами XML-элементов.

Для отдельных значений есть `XMLСтрока`, обратная ей `XMLЗначение("Число", "42.5")` и `XMLТипЗнч`,
подсказывающая имя XSD-типа (`decimal`, `string`, `boolean`, `dateTime`), а для `Неопределено` —
каноническое расширение OneBase `undefined`:
числа пишутся без экспоненты, даты — по ISO 8601, булево — как `true`/`false`.
Имя, возвращённое `XMLТипЗнч`, принимается `XMLЗначение`; дата сохраняет момент, смещение часового
пояса и до девяти цифр дробной части секунды в формате RFC 3339, но не внутреннее имя
`time.Location`. Дробь отделяется точкой; лишняя точность отклоняется без тихого усечения.
Допустимы годы `0000..9999` и минутные смещения от `-14:00` до `+14:00` включительно.
Decimal принимается без экспоненты `e`/`E`, только для конечных чисел; вход, коэффициент и
результат ограничены 10 000 символов/цифр для защиты от неконтролируемого разворачивания нулей.
`XMLЗначение` принимает для `Неопределено` имена `Неопределено`/`undefined`/`nil` только с пустым
текстовым представлением, поэтому пустое имя типа не используется как скрытый маркер.
XML-примитивами считаются строки, булево, целые/конечные числа, даты и Неопределено; произвольные
объекты и их пользовательские строковые представления отклоняются. Аргументы, объявленные
строковыми, не приводят другие типы неявно.
`НайтиНедопустимыеСимволыXML(Текст)` подскажет позицию символа, который нельзя поместить
в документ (0 — значит всё в порядке).
## Запросы: ПЕРВЫЕ N и ИМЕЮЩИЕ

<!-- status: testing -->
<!-- date: 2026-08-10 -->

Две конструкции языка запросов, которых не хватало при переносе модулей из других
учётных систем — и которые полезны сами по себе.

**`ПЕРВЫЕ N`** ограничивает выборку первыми N строками:

```
ВЫБРАТЬ ПЕРВЫЕ 10 Наименование, Цена
ИЗ Справочник.Номенклатура
УПОРЯДОЧИТЬ ПО Цена УБЫВ
```

Сочетается с `РАЗЛИЧНЫЕ` в любом порядке. Английский синоним — `TOP N`.
Во вложенных запросах и отдельных ветвях `ОБЪЕДИНИТЬ` модификатор пока не
поддерживается: компилятор возвращает явную ошибку вместо SQL с другой семантикой.

`ПЕРВЫЕ 0` — валидный запрос с пустым результатом, как в 1С. Это важно, когда
текст запроса собирается конкатенацией и размер порции приходит переменной: на
нулевой порции запрос просто ничего не вернёт, а не упадёт ошибкой компиляции.
Отрицательное количество по-прежнему отвергается.

**`ИМЕЮЩИЕ`** — синоним уже существующего `ИМЕЯ` (фильтр по агрегатам после
группировки). Обе формы работают одинаково:

```
ВЫБРАТЬ Товар, СУММА(Количество) КАК Кол
ИЗ РегистрНакопления.ОстаткиТоваров
СГРУППИРОВАТЬ ПО Товар
ИМЕЮЩИЕ СУММА(Количество) > 0
```

**`ЕстьNULL(Значение, ЗначениеЕслиNULL)`** подставляет значение вместо NULL — то же, что
SQL `COALESCE`. Нужен после левых соединений и виртуальных таблиц остатков, где отсутствие
записи даёт NULL вместо нуля:

```
ВЫБРАТЬ ЕстьNULL(Остатки.КоличествоОстаток, 0) КАК Кол
ИЗ РегистрНакопления.ОстаткиТоваров.Остатки() КАК Остатки
```

**Как попробовать.** Любой отчёт или обработка с запросом: раньше `ВЫБРАТЬ ПЕРВЫЕ 10 …`
падал с ошибкой SQL «syntax error near 10», теперь возвращает десять строк.

## Поле кода в формах: подсветка синтаксиса

<!-- status: testing -->
<!-- date: 2026-08-10 -->

В формах бывают поля, где пользователь правит не текст, а код: правила обмена,
шаблоны писем, запросы, JSON- и XML-настройки. Раньше для них оставалась обычная
многострочная `textarea` — без подсветки, без номеров строк, с пропорциональным шрифтом.

Появился вид элемента `ПолеКода`:

```yaml
- kind: ПолеКода
  name: КодПравила
  data_path: Объект.КодПравила
  language: bsl        # bsl | sql | json | xml | yaml | markdown | javascript | plaintext
```

**Как попробовать.** Добавьте такое поле в управляемую форму справочника со строковым
реквизитом и откройте карточку — вместо текстовой области появится редактор с подсветкой.

Редактор — тот же, что в конфигураторе, и он уже встроен в платформу: внешних загрузок
нет, всё работает офлайн. Ассеты подключаются только на формах, где поле кода
действительно есть. Без JavaScript поле остаётся рабочей `textarea`, а у полей
«только просмотр» редактор не монтируется вовсе.

При импорте форм из 1С `TextDocumentField` теперь превращается в `ПолеКода` — раньше
такой элемент попадал в конфигурацию с неизвестным видом и ронял `onebase check`.

## Движения вне проведения больше не пропадают молча
<!-- status: testing -->
<!-- date: 2026-08-11 -->
<!-- issue: 743 -->

Движения регистров принадлежат документу-регистратору и записываются при его
записи или проведении. Но переменная `Движения` подставлялась в модули и там,
где записывать их некому: в регламентном задании, обработке, на странице, в
отчёте, печатной форме, консоли кода и обработчике события формы. Строки
копились в памяти и выбрасывались — без ошибки и без предупреждения.

Это первое, что пробует человек, пришедший из 1С, и хуже всего то, как дефект
выглядит: обработка отрабатывает, сообщения печатаются, ошибок нет. Расхождение
находят позже — по остаткам.

Теперь `Движения.X.Добавить()` в таком контексте возбуждает ошибку, которая
называет и место, и что делать:

```
Движения.ОстаткиТоваров.Добавить(): здесь движения сохранять некуда —
они принадлежат документу-регистратору и записываются только при его записи
или проведении (регламентное задание). Перенесите формирование движений
в ОбработкаПроведения документа.
```

Ошибка перехватывается `Попыткой`, как любая прикладная. `Очистить()` вне
проведения по-прежнему безобидна: чистить пустой набор не за что отказывать.

## Пауза между повторами (`Приостановить`)

<!-- status: testing -->
<!-- date: 2026-08-10 -->
<!-- issue: 708 -->

Для backoff и выдержки `Retry-After` больше не нужны запуск `Start-Sleep` через
`exec.enabled` или активный цикл, нагружающий CPU. В DSL появились
`Приостановить(Секунды)` и синонимы `Пауза`, `Подождать`, `Sleep`, `Wait`:

```bsl
Задержка = 1;
Для Попытка = 1 По 3 Цикл
    Ответ = HTTPПолучить(URL);
    Если Ответ.КодСостояния < 500 Тогда
        Прервать;
    КонецЕсли;
    Приостановить(Задержка);
    Задержка = Задержка * 2;   // 1 → 2 → 4 секунды
КонецЦикла;
```

Функция принимает конечное неотрицательное число, включая доли секунды;
числовая строка не считается числом. Предел обычного вызова — 300 секунд. В
песочнице все выдержки делят общий `MaxWallClock` запуска и прерываются по его
дедлайну, поэтому цепочка коротких пауз не растягивает выполнение сверх бюджета.

Под `onebase test` после `Часы.Установить(...)` пауза выполняется мгновенно:
сдвигает тестовые часы и добавляет запись в `Мок.Паузы`. Без установки тестовых
часов ожидание остаётся реальным.

```bsl
Часы.Установить(Дата(2026, 8, 10, 12, 0, 0));
ОтправитьСПовторами();
Утверждать.Равно(Мок.Паузы.Количество(), 3, "три выдержки");
Утверждать.Равно(Мок.Паузы[0].Секунды, 30, "первая — 30 с");
```

## Ещё два события строк табличной части
<!-- status: testing -->
<!-- date: 2026-08-11 -->

`ПриИзмененииСтроки` и `ПослеДобавленияСтроки` объявлялись в платформе, но не
вызывались: обработчик можно было написать, а сработать он не мог. Теперь оба
работают.

- **`ПриИзмененииСтроки`** — правка ячейки, с контекстом строки
  (`НомерСтроки`, `ТекущаяСтрока`). Отличие от `ПриИзменении` в намерении:
  «пересчитать эту строку» против «пересчитать форму».
- **`ПослеДобавленияСтроки`** — после добавления строки, следом за
  `ПриДобавленииСтроки`. Удобно разделять «заполнить значения по умолчанию» и
  «пересчитать итоги».

Объявлять можно любое одно — события независимы. Если объявлены оба из пары,
они выполняются **последовательно**, `При…` затем `После…`: оба ответа
применяют значения к форме, и параллельный запуск дал бы гонку «кто последний
записал». Как и раньше, подписка ставится только при объявленном обработчике —
формы без них сеть не гоняют.

## Запрет удаления из конфигурации (ПередУдалением / ПослеУдаления)
<!-- status: testing -->
<!-- date: 2026-08-11 -->

Проверить объект перед удалением было нечем. События `ПередУдалением`,
`ПриУдалении` и `ПослеУдаления` в платформе объявлялись, их можно было написать
в форме — и они **не вызывались ниоткуда**. Запрет молчал, объект удалялся.
Хуже прочих дефектов этого класса: данных уже нет, когда это замечают.

Теперь в модуле объекта (`src/<объект>.os`) работают два хука:

```
Процедура ПередУдалением()
    Если ЕстьДвижения(ЭтотОбъект.Ссылка) Тогда
        ВызватьИсключение("Сначала отмените проведение");
    КонецЕсли;
КонецПроцедуры

Процедура ПослеУдаления()
    ЗаписатьВЖурнал("Удалён " + ЭтотОбъект.Наименование);
КонецПроцедуры
```

`ПередУдалением` отменяет удаление через `ВызватьИсключение`. `ПослеУдаления`
выполняется в той же транзакции и видит данные удалённого объекта; его ошибка
возвращает объект на место.

**Хуки срабатывают на всех путях удаления** — из списка, из формы, пачкой
«Удалить помеченные», из DSL и по REST. Это главное свойство: запрет, который
обходится сменой способа удаления, защитой не является. Поэтому хук живёт в
модуле объекта, а не в форме; если объявить его в форме, `onebase check --lint`
предупредит и подскажет, куда перенести.

Пометка на удаление хуки не вызывает — она обратима и физическим удалением не
считается.

## Запись регистров сведений из модуля
<!-- status: testing -->
<!-- date: 2026-08-11 -->
<!-- issue: 743 -->

Читать регистр сведений из конфигурации можно было всегда — `СрезПоследних` в
языке запросов. А записать было нечем: движения умеют только регистры,
подчинённые регистратору, независимый заполнялся руками через форму или
обменом. Для конфигураций, где регистры сведений — основной рабочий инструмент
(лог обмена, состояние узлов, статистика), это означало, что заполнять их код
не может.

Теперь есть менеджер записи, как в 1С:

```
Запись = РегистрыСведений.СостояниеУзлов.СоздатьМенеджерЗаписи();
Запись.Узел = Узел;
Запись.Состояние = "Готов";
Запись.Период = ТекущаяДатаВремя();   // только у периодического
Запись.Записать();

Запись.Прочитать();                   // заполняет ресурсы по ключу
Если Запись.Выбран() Тогда … КонецЕсли;
Запись.Удалить();
```

Доступен везде, где работает DSL, — в обработках, регламентных заданиях,
HTTP-сервисах.

**Права и строковые политики те же, что у формы.** Путь из модуля зовёт ту же
функцию записи, а не свою копию, — включая проверку **существующей** записи по
ключу: без неё чужую, скрытую политикой строку можно было бы перезаписать
вслепую, не зная её содержимого.

**Период обязателен** у периодического регистра и запрещён у непериодического;
опечатка в имени измерения — ошибка, а не запись с пустым ключом. Оба случая
раньше давали бы запись не там, где её потом ищут.

Регистр, подчинённый регистратору, помечается `recorder: true` и менеджера
записи не даёт: его строки формирует проведение документа, и программная запись
разошлась бы с ним — ближайшее перепроведение снесло бы её без предупреждения.

**Для пакетной работы есть набор записей** — он замещает содержимое по отбору
одной транзакцией:

```
Набор = РегистрыСведений.ЛогОбмена.СоздатьНаборЗаписей();
Набор.Отбор.Узел = Узел;
Набор.Прочитать();
Набор.Очистить();
Стр = Набор.Добавить();
Стр.Событие = "Старт";
Набор.Записать();
```

Прочитанные строки перебираются и правятся на месте (`Для Каждого Стр Из Набор`
или `Набор.Получить(Индекс)`), поэтому цикл «прочитал срез, поправил ресурс,
записал» не требует собирать набор заново.

Строки вне отбора не трогаются, а сбой на середине откатывает всё — половины
старых и половины новых строк не бывает. Отбор обязателен и только по
измерениям: запись без отбора снесла бы регистр целиком, а отбор по ресурсу
задел бы больше строк, чем рассчитывал автор, — оба случая отклоняются.

## Боковая панель деталей записи
<!-- status: testing -->
<!-- date: 2026-08-12 -->
<!-- issue: 670 -->

Смотреть широкую таблицу неудобно, особенно когда среди колонок картинки: каждая
строка тянет свою миниатюру, а прочитать запись целиком всё равно нельзя.

Кнопка **▤** рядом с переключателем вида (☰ ▦ 📂) открывает справа карточку той
записи, на которой стоит курсор. Список при этом можно оставить узким — панель
покажет **все** реквизиты, включая те, что в колонки не вынесены. Стрелки ↑↓
двигают курсор, карточка следует за ним.

Внутри панели — закладки: картинки уезжают на свою, размеченный текст на свою,
остальное в «Основное». Ширина тянется за левый край и запоминается, как и
выбранная закладка и само состояние «включена» — по каждому списку отдельно.

Работает в списках справочников и документов, в журналах документов и в списках
регистров сведений.

**Панель не показывает того, чего вам не видно.** Данные берутся из уже
загруженных строк — тех же, что прошли права, строковые политики и маскирование
персональных данных. Второго пути чтения у панели нет, поэтому и обойти
ограничения ею нельзя: скрытое маской остаётся скрытым, отфильтрованная строка
не появится ни в списке, ни в панели.

По умолчанию панель выключена — состав колонок ваших списков не меняется, пока
вы её не включите.

## Свой состав боковой панели (`detail_panel`)
<!-- status: testing -->
<!-- date: 2026-08-12 -->
<!-- issue: 670 -->

Панель деталей списка сама раскладывает реквизиты по закладкам, и на большинстве
объектов этого достаточно. Если хочется свой порядок и свои названия закладок —
опишите их в YAML объекта:

```yaml
detail_panel:
  title: Наименование
  tabs:
    - name: Основное
      fields: [Артикул, Цена, Поставщик]
    - name: Изображения
      fields: [Фото, ФотоУпаковки]
```

Или короче, без закладок — состав задан, раскладка по типам остаётся
автоматической:

```yaml
detail_panel:
  fields: [Артикул, Цена, Поставщик, Фото]
```

Опечатку в имени реквизита ловит `onebase check`: иначе в панели просто не было
бы строки, и причину пришлось бы искать в данных. Внутри явной закладки
реквизиты не разносятся по типам — вы уже решили, что они вместе.

Блок задаёт **состав**, а не факт показа: панель по-прежнему включает
пользователь кнопкой ▤, и права он ею не обходит — скрытое маской остаётся
скрытым, даже если реквизит перечислен явно.

`width` принимает 220–640 px (`0` или отсутствие ключа означает 320 px).
`fields: []` — явная пустая панель; `tabs: []` и вкладки без полей проверка
отклоняет, не подменяя их автокомпоновкой. Если у управляемой формы списка есть
`СтраницыФормы`, её страницы и порядок полей имеют приоритет над этим блоком.

## Код справочника и маски даты в номере
<!-- status: testing -->
<!-- date: 2026-08-12 -->
<!-- issue: 658 -->

Блок `numerator:` теперь работает и у справочников — раньше он там объявлялся,
парсился и **молча ничего не делал**:

```yaml
name: Контрагенты
numerator:
  prefix: "К-"
  length: 6
```

Новые элементы получают код автоматически; введённый вручную не переписывается.
Существующие элементы остаются без кода — массовая правка данных при правке YAML
не то, чего ждёшь. Дозаполнить их можно командой `onebase renumber`: без ключей
она показывает объём, с `--write` заполняет.

**В префиксе заработали маски даты**, которые документация обещала давно:

```yaml
numerator: {prefix: "РТ-{YYYY}-", length: 4, period: year}
# → РТ-2026-0001, РТ-2026-0002 …
```

Доступны `{YYYY}`, `{YY}`, `{MM}`, `{DD}`.

Если включена `unique: true`, маска должна отличать каждый период сброса:
`year` требует год, `month` — год и месяц, `day` — полную дату. Просто наличие
фигурной скобки или `{MM}` без года недостаточно: значение позднее повторится.

**Период номера считается по дате документа предсказуемо.** Если дат несколько,
берётся «Дата» (или «Период»), а не случайная: раньше у такого документа счётчик
мог оказаться разным от запуска к запуску. Заодно расчёт номера свели в одну
точку — до этого он жил в пяти местах (интерфейс, REST, модуль документа,
ИИ-действия, `Нумераторы.СледующийНомер`) и копии уже начали расходиться.

## Префикс базы в кодах и номерах
<!-- status: testing -->
<!-- date: 2026-08-12 -->
<!-- issue: 658 -->

Когда баз несколько — центральная и филиалы, боевая и тестовая, — по коду
объекта не понять, откуда он приехал. Задать префикс в конфигурации нельзя: она
одинакова во всех базах, и все выдали бы одно и то же.

Теперь префикс задаётся **в самой базе**:

```bash
onebase base prefix --sqlite base.db            # показать текущий
onebase base prefix --set Ф- --sqlite base.db   # задать
```

и подставляется в коды тех объектов, у которых это объявлено:

```yaml
numerator: {prefix: "К-", length: 6, base_prefix: true}
```

```
Центр:  Ц-К-000001    Филиал: Ф-К-000001
```

Без `base_prefix: true` формат номеров не меняется — включение префикса на базе
не должно молча переформатировать всё сразу.

**При восстановлении из резервной копии префикс снимается.** Копия могла уехать
в другую базу, и клон, сохранивший префикс оригинала, выдавал бы те же коды —
обмен склеил бы разные объекты. Команда восстановления сообщает об этом и
предлагает задать префикс заново.

**Обмен стал терпимее к разной конфигурации узлов.** Раньше приём пакета требовал
точного совпадения набора реквизитов: стоило добавить объекту новое поле — и
приём падал целиком, вместе с пакетами, которые уже лежали в очереди. Теперь
недостающее поле считается незаполненным, а неизвестное по-прежнему отклоняется.
Обновлять узлы можно поэтапно.

## Уникальность кода и номера
<!-- status: testing -->
<!-- date: 2026-08-13 -->
<!-- issue: 658 -->

Раньше платформа могла выдать код и не мешала записать такой же руками: два
контрагента с кодом `К-000042` уживались в базе, а расходились уже в отчётах и
при обмене. Теперь уникальность объявляется:

```yaml
name: Контрагенты
numerator: {prefix: "К-", length: 6, unique: true}
```

Разрез — глобально по объекту, как контроль уникальности в 1С: один код — один
элемент справочника. Попытка записать занятое значение отклоняется по-русски:

```
Контрагенты: значение Код «К-000042» уже занято другой записью
```

вместо `UNIQUE constraint failed: контрагенты.код`, которое пользователю ничего
не объясняет.

**Включение на живой базе безопасно.** Если у части записей код пуст или дубли
уже есть, миграция не пройдёт молча, а скажет, что именно мешает и что делать:

```
Контрагенты: уникальность Код включена, но у 37 записей значение пусто;
пустые значения уникальный индекс не ловит — дозаполните их командой onebase renumber
```

Проверка именно до создания индекса, а не после: пустое значение хранится как
NULL, а NULL в уникальном индексе не конфликтуют — без проверки уникальность
включилась бы «успешно» и без всякого эффекта, а дубли всплыли бы позже, при
первом же дозаполнении кодов. Дозаполнить:

```bash
onebase renumber --project . --sqlite base.db            # сначала показать объём
onebase renumber --project . --sqlite base.db --write    # заполнить
```

Без `unique: true` ничего не меняется — уже работающие конфигурации, где дубли
кодов допустимы, обновление не ломает.

## Нумерация настраивается мышью, а состав формы объекта наконец работает
<!-- status: testing -->
<!-- date: 2026-08-13 -->
<!-- issue: 658 -->

Три вещи из плана 117, которые видит пользователь.

**Блок «Нумерация» в конфигураторе.** Префикс, разрядность, сброс счётчика,
разрез, уникальность и префикс базы задаются в карточке объекта, как всё
остальное. Раньше конфигуратор блок `numerator:` только сохранял при правке
других настроек — задать его можно было лишь правкой YAML руками. Заодно
конфигуратор перестал терять `unique:` и `base_prefix:` при обычной правке
реквизитов.

Настройки, которые сломались бы позже, отклоняются сразу: сброс счётчика у
справочника (выдал бы уже занятый код) и уникальность со сбросом без маски даты,
которая действительно различает периоды (значение повторилось бы позже — и не
при обновлении, а на рабочих данных).

**`item_form:` начал работать.** Ключ существовал давно: сохранялся, отдавался в
описание конфигурации, проходил проверку — и не читался ни одним рендерером.
Пользователь снимал в конфигураторе галочку видимости реквизита, сохранял, и на
форме ничего не менялось.

```yaml
name: Контрагенты
item_form: [Наименование, ИНН, Комментарий]   # состав и порядок карточки
```

Скрытые реквизиты продолжают ездить в форме скрытыми полями: иначе «скрыть»
означало бы «стереть» — сохранение формы обнулило бы то, чего в ней нет. Это
видимость, а не защита: ограничивать доступ к реквизиту нужно политикой поля.
Опечатка в имени теперь отклоняется `onebase check`.

**`НайтиПоКоду` объясняет отказ.** У справочника без объявленного нумератора
реквизита «Код» нет вовсе, и поиск по нему возвращал `Неопределено` — ровно то
же, что «такого кода нет». Модуль молча уходил в ветку «не нашли», а причину
автор искал в данных:

```
НайтиПоКоду(Склады): у справочника нет реквизита «Код» — объявите блок numerator:
(тогда код будет выдаваться автоматически) или ищите по другому реквизиту
```

`Неопределено` теперь означает ровно одно: подходящей записи нет. Отказ БД,
опечатка в имени реквизита и вызов без аргумента стали ошибками. Число вместо
строки больше не молчит — код «42» и число 42 ищутся одинаково.

## WebSocket-клиент: приём внешних событий потоком (`transport: ws`)
<!-- status: testing -->
<!-- date: 2026-08-13 -->
<!-- issue: 738 -->

База умеет сама подключаться к внешнему WebSocket-серверу и принимать события
без входящего HTTP: объявите входной шлюз с `transport: ws` и адресом `url:`
(`ws://` или `wss://`) — остальное как у обычной приёмки (план 90): конверт
JSON, ключ идемпотентности, обработчик `Обработать(Конверт)`, карантин.

```yaml
# intake/событияобмена.yaml
name: СобытияОбмена
transport: ws
url: wss://hub.example.com/stream
auth: token
secret: ${env:WS_TOKEN}
idempotency:
  key: event_id
handler: ОбработчикСобытий
```

Как попробовать:

1. Запустите базу (`onebase run`) — соединение поднимется само и будет
   переподключаться после обрывов с нарастающей выдержкой.
2. Состояние смотрите в **Администрирование → Входная приёмка**: подключено ли,
   когда пришло последнее сообщение, сколько реконнектов и что в карантине.
3. Повторная доставка после обрыва не создаёт дублей — её гасит ключ
   идемпотентности приёмки.
4. Отправить сообщение в то же соединение из кода:
   `ВебСокет.СобытияОбмена.Отправить(ЗначениеВJSON(Данные))`; проверка —
   `ВебСокет.СобытияОбмена.Подключён()`. Гарантий доставки нет: обрыв — сразу
   перехватываемая ошибка.
5. Рассылка события открытым формам — из обработчика шлюза обычным
   `ОтправитьУведомление(Кому, Событие, Данные)`.

Предохранитель: при выключенном `net.enabled` соединение не поднимается —
причина видна в мониторе приёмки.

## Табличная часть в модулях: методы и понятные отказы
<!-- status: testing -->
<!-- date: 2026-08-13 -->
<!-- issue: 842 -->

`Товары.Количество()` в обработчике проведения не работал: табличная часть
приезжала туда без методов. Обход строк при этом работал, поэтому поломка
выглядела выборочной, а типовая проверка

```
Если ЭтотОбъект.Товары.Количество() = 0 Тогда
  ВызватьИсключение("Табличная часть не заполнена — проводить нечего.");
КонецЕсли;
```

просто ничего не делала: вызов молча возвращал `Неопределено`, сравнение с нулём
давало Ложь, и документ проводился с пустой табличной частью.

Теперь у табличной части один и тот же набор методов везде, где до неё можно
добраться — при записи из модуля, в обработчике проведения и в событии формы:
`Количество()`, `Добавить()`, `Получить(Индекс)`, `Удалить(Индекс)`,
`Очистить()`, `Итог("Колонка")`.

Пустая табличная часть — именно пустая, а не `Неопределено`, так что проверка
«строк нет» срабатывает тогда, когда нужна. Опечатка в имени метода отклоняется
со списком доступных.

Заодно чинится заполнение демо-баз: `onebase procrun --proc ЗаполнитьТестовуюБазу`
на `examples/trade` падал на первой же реализации.

## Интерфейс переведён полностью на всех 16 языках
<!-- status: testing -->
<!-- date: 2026-08-16 -->
<!-- issue: 960 -->

Раньше в тринадцати языках была переведена примерно половина интерфейса, а
непереведённая строка показывалась **по-русски** — в армянском, грузинском или
китайском окне это просто нечитаемо. Теперь переведены все экраны, которые
рисует сервер: лаунчер, настройки отчётов, диагностика, тексты ошибок.

Одно исключение, чтобы не искать его на ощупь: часть надписей редактора макетов
живёт в скрипте конструктора и в переводы пока не попала — они показываются
по-английски. Читаемо, но не переведено; доберём отдельно.

Языки: русский, английский, немецкий, французский, испанский, португальский,
румынский, сербский, украинский, турецкий, азербайджанский, узбекский,
казахский, армянский, грузинский, китайский.

**Как попробовать.** Профиль → язык интерфейса, либо язык по умолчанию для базы
в её настройках. Проверьте разделы, куда раньше редко заглядывали: окно списка
баз, настройку отчёта, страницу диагностики.

Если перевод где-то звучит неестественно или искажает смысл — напишите, какой
язык и какая надпись. Учётные термины («Провести», «Остатки», «Регистр
накопления») переводились людьми и не менялись; новые строки — это интерфейсная
обвязка, и они прошли машинный проход без проверки носителем языка. Такие строки
лежат отдельно, в `internal/i18n/locales/machine/<язык>.json`: правка там видна
как правка непроверенного перевода, а не как спор с переводчиком.

Строка, для которой перевода нет вовсе, теперь показывается по-английски, а не
по-русски.

## Запуск регламентного задания из кода
<!-- status: testing -->
<!-- date: 2026-08-17 -->
<!-- issue: 742 -->

Регламентное задание больше не привязано к расписанию намертво: его можно
запустить из кода формы, обработки или HTTP-сервиса — «обменяться сейчас»,
«пересчитать сейчас», кнопкой, а не ожиданием ближайшего тика.

```
ИдПрогона = РегламентныеЗадания.Запустить("ОбменСУзлами");

Прогон = РегламентныеЗадания.Прогон(ИдПрогона);
Если Прогон <> Неопределено И Прогон.Статус = "success" Тогда
    Сообщить("Обмен занял " + Прогон.ДлительностьМс + " мс");
КонецЕсли;
```

`Запустить` сразу возвращает управление и отдаёт идентификатор прогона — по нему
позже спрашивают статус через `Прогон(Ид)`. Ждать завершения прямо в коде нельзя
и не нужно: сессия однопоточная, а обмен может идти часами.

Полезный побочный эффект — задание с выключенным расписанием (`enabled: false`)
превращается в «задание по требованию»: по расписанию не тикает, а из кода
запускается.

Чего эта возможность не даёт — параллелизма. Одно задание в один момент
выполняется один раз: попытка запустить занятое даёт исключение «уже
выполняется», которое ловится `Попытка/Исключение` и обычно означает «идёт, и
хорошо». Несколько одновременных обменов — это очередь с пулом исполнителей,
отдельная задача.

Работу и результат задание берёт и отдаёт через данные (регистр сведений), а не
через параметры и возвращаемое значение: журнал прогонов — это журнал
исполнения, а не транспорт бизнес-данных. И ещё одно ограничение, о которое
легко споткнуться: внутри транзакции — в том числе из обработчика проведения —
задание запустить нельзя, запускайте после записи.

## Внешние обработки, отчёты и печатные формы
<!-- status: stable -->
<!-- date: 2026-06-03 -->

Обработка, отчёт или печатная форма, которую администратор загружает **в
работающую базу**, не трогая конфигурацию проекта: без пересборки, без деплоя,
без ветки в репозитории. Аналог внешних обработок 1С. Работает с июня 2026, но
до сих пор нигде не была описана — отсюда статус «тестируем».

Такой артефакт живёт в самой базе (таблицы `_ext_printforms`, `_ext_reports`,
`_ext_processors`), попадает в бэкап вместе с данными и **не** попадает в
`onebase check` и в git конфигурации: это отдельный контур, точка расширения
конкретной установки, а не изменение продукта.

| Что | Что внутри | Ограничение |
|---|---|---|
| Печатная форма | декларативный YAML макета, привязка к документу полем `document:` | только декларативный макет; `.os`-формы с DSL-рендером в контур не входят |
| Отчёт | YAML отчёта (запрос + показатели) | только запрос — кода нет |
| Обработка | YAML метаданных + поле `code` с исходником на встроенном языке | исполняет код, поэтому под моделью доверия (ниже) |

**Как настроить.** Системное меню → **«Расширения и оборудование»** →
«Внешние печатные формы» / «Внешние отчёты» / «Внешние обработки». Раздел
доступен только администратору. На странице — загрузка файла, включение/
выключение, удаление и **экспорт**.

Файл принимается в двух видах: «голый» YAML (ровно такой же, как лежал бы в
конфигурации) либо переносимый бандл `*.obform` — тот же YAML плюс шапка
`manifest` с автором, версией и минимальной версией платформы:

```yaml
manifest:
  kind: processor          # printform | report | processor
  name: ПроверкаСвязи
  author: Иванов
  version: "1.0"
  # min_platform: "0.9.0"  # необязательно: база более старой версии откажет
form:
  name: ПроверкаСвязи
  title: Проверка связи
  params:
    - { name: Сообщение, type: string, default: "привет" }
  code: |
    Процедура Выполнить()
        Сообщить("Внешняя обработка говорит: " + Параметры.Сообщение);
    КонецПроцедуры
```

`min_platform` в примере закомментирован намеренно: версия платформы в релизной
сборке — это номер тега, и файл, требующий версию выше текущей, в базу не
попадёт. В сборке из исходников проверка вообще пропускается (версия там
выглядит как `dev-1a2b3c4` и не сравнивается с номером), так что заполнять это
поле стоит только когда обработка правда опирается на возможность конкретной
версии.

Экспорт собирает такой бандл из записи в базе и проставляет `min_platform`
текущей версией — то есть перенос между базами (тест → продуктив, типовая
доработка на несколько установок) идёт файлом, без копирования строк в СУБД.

Загруженное подхватывается **сразу, без перезапуска сервера**. Отчёты и
обработки проверяются в момент загрузки, а не в момент запуска: у отчёта запрос
компилируется в SQL и проверяется на исполнимость, у обработки код должен
компилироваться и содержать процедуру `Выполнить()`. Заведомо нерабочий файл в
базу не попадёт.

**Модель доверия для обработок.** Внешняя обработка исполняет произвольный код
с правами интерпретатора, поэтому: загружать может только администратор, а
свежезагруженная обработка **недоверенная** — её видит и запускает только
администратор. Кнопка «Доверять» открывает её обычным пользователям (право
`processor/<Имя>/run` при этом проверяется как у своей). Признак сбрасывается
при каждом обновлении содержимого: подменённый код снова проходит через решение
администратора. Каждый запуск пишется в журнал аудита (`extprocessor.run`),
загрузка, включение и удаление — тоже.

**Как попробовать.** Сохраните пример выше в `ПроверкаСвязи.obform` → системное
меню → «Внешние обработки» → загрузите файл. Обработка появится в списке
обработок с пометкой «(внешняя)» — запустите её. Затем нажмите «Доверять» и
проверьте под обычным пользователем: до этого её в списке не было, после —
появилась.

Чего в контуре нет: внешняя обработка не привязывается к объекту и не даёт
кнопку в форме документа или справочника (аналога «Дополнительных отчётов и
обработок» БСП) — она живёт только в общем списке обработок. Привязка к объекту
есть лишь у печатных форм, через поле `document:`. Имя, совпавшее с обработкой
конфигурации, не перекрывает её: приоритет у конфигурации, в лог пишется
предупреждение.

## Реквизит «только просмотр» в карточке объекта
<!-- status: testing -->
<!-- date: 2026-08-19 -->
<!-- issue: 1011 -->

Служебный реквизит теперь можно показать в карточке, не давая его править.
Раньше «видно, но не трогать» умела только управляемая форма — и ради одного
вычисляемого поля приходилось описывать в YAML всю карточку целиком: все
реквизиты и все табличные части.

**Как настроить.** В блоке `item_form:` рядом с обычными именами разрешена
запись с признаком:

```yaml
name: Клиенты
item_form:
  - Наименование
  - ИНН
  - name: ТелефоныНорм        # пересобирается модулем в ПриЗаписи
    readonly: true            # видно, но руками не правится
```

В конфигураторе то же самое — галочка **«только просмотр»** рядом с галочкой
видимости на вкладке состава форм.

**Как попробовать.** Пометьте так вычисляемый реквизит (нормализованный
телефон, итог, служебный статус), откройте карточку: значение видно, поле
подсвечено серым и не принимает ввод, у подписи стоит пометка. Запишите объект
— значение на месте: оно по-прежнему уходит на сервер, просто вводить его
руками нельзя.

Это, как и видимость, свойство интерфейса, а не ограничение доступа: значение
лежит в HTML страницы, и запретить чтение реквизита можно только политикой поля.

## Сборки под ARM: Raspberry Pi, мини-ПК, Mac на Apple Silicon
<!-- status: testing -->
<!-- date: 2026-08-18 -->
<!-- issue: 988 -->

Раньше в релизе лежали только сборки под x86-64, и на ARM-железе платформа либо
не запускалась вовсе, либо работала под эмуляцией. Теперь каждый релиз содержит
шесть архивов вместо трёх — под обе архитектуры для Windows, Linux и macOS.

**Как попробовать.** Скачайте со страницы релизов архив с `-arm64` в имени:

| Железо | Архив |
|---|---|
| Raspberry Pi, мини-ПК, NAS | `onebase-linux-arm64.tar.gz` |
| Mac на M1 и новее | `onebase-darwin-arm64.tar.gz` |
| Ноутбуки и планшеты Windows на ARM | `onebase-windows-arm64.zip` |

Дальше всё как обычно: распаковать и запустить `onebase start`. PowerShell-установщик
(`irm .../install.ps1 | iex`) определяет архитектуру сам и берёт нужный архив.

Автообновление под ARM работает без настройки: платформа подбирает ассет по своей
архитектуре, и кнопка «Обновить» в лаунчере теперь видит сборки там, где раньше
сообщала, что их нет.

Одно ограничение: в `onebase-windows-arm64.zip` нет `onebase-gui.exe` — нативное
окно требует WebView2 через CGo, а C-тулчейна под ARM в релизной сборке нет.
Сервер и CLI там нативные, лаунчер открывается в браузере. Кому нужно именно
нативное окно — подойдёт amd64-архив, Windows запустит его через эмуляцию x64.

## Полный справочник языка — теперь и без скачивания платформы
<!-- status: testing -->
<!-- date: 2026-08-18 -->
<!-- issue: 988 -->

Реестр встроенного языка в платформе был всегда — из него живут автодополнение
и подсказки в конфигураторе, `onebase explain` и `AGENTS.md` для ИИ-агентов. Но
посмотреть язык целиком можно было только после установки: QUICKSTART показывает
десяток функций, `explain` — по одной.

Теперь весь язык выложен одной страницей:
[`docs/dsl-reference.md`](dsl-reference.md) — **155 функций, 150 методов
объектов, 18 конструкций языка и 37 элементов языка запросов**, с сигнатурами,
параметрами, возвращаемыми значениями, примерами и английскими синонимами.
Открывается прямо на GitHub, оглавление кликабельное.

**Как попробовать.** Откройте файл в репозитории или сгенерируйте свой:

```
onebase langref                                  # markdown в терминал
onebase langref --output my-reference.md         # в файл
onebase langref --format json                    # машиночитаемо
```

Справочник собирается из того же реестра, что питает конфигуратор, поэтому
отстать от платформы не может: если реестр поменялся, а файл не перегенерирован,
сборка краснеет.

## Включение и выключение регламентных заданий администратором
<!-- status: testing -->
<!-- date: 2026-08-18 -->
<!-- issue: 991 -->

`enabled` в YAML — это исходное состояние задания, каким его поставил разработчик
конфигурации. Теперь поверх него есть административное решение конкретной базы:
включить или выключить задание не трогая исходники — и это решение переживает
обновление конфигурации, как «Использование» в 1С.

Живой кейс: база с поставляемым выключенным `ТелеграмПоллингом`. Раньше включить
бота администратору рабочей базы означало править YAML и перезапускаться; теперь —
один тумблер.

**Как попробовать.** **Админка → Регламентные задания** (`/ui/admin/scheduled`).
Страница показывает источник состояния: «✓ активно» / «— отключено» — из
конфигурации, «✓ включено администратором» / «— выключено администратором» —
решение этой базы. Кнопка «Включить/Выключить» действует со следующего тика
расписания, без перезапуска; «Вернуть как в конфигурации» убирает решение. Ручной
«Запустить сейчас» работает и для выключенного. Действия аудируются
(`scheduled.enable/disable/reset`).

Для скриптов развёртывания и headless-окружения то же самое делает CLI:

```
onebase jobs enable ТелеграмПоллинг --sqlite base.db
onebase jobs disable Автобэкап --sqlite base.db
onebase jobs reset Автобэкап --sqlite base.db    # снова действует конфигурация
onebase jobs status Автобэкап --sqlite base.db
```

Решения переезжают в `.obz`-копию безусловно: восстановленная база ведёт задания
так же, как источник.

Попутно исправлено давнее несоответствие: задание с включённым `enabled` и пустым
расписанием больше не мешает базе стартовать — оно честно «по требованию»: по
расписанию не тикает, запускается кнопкой или из кода.

## Очередь фоновых заданий с пулом исполнителей
<!-- status: testing -->
<!-- date: 2026-08-18 -->
<!-- issue: 848 -->

Когда одну и ту же работу надо сделать много раз и параллельно — обойти сотни
узлов обмена, разослать пачку уведомлений, пересчитать список объектов, — есть
очередь задач и пул исполнителей. Одно регламентное задание, много задач, каждая
со своими параметрами.

```
// продюсер — обычно задание по расписанию — раскладывает работу по очереди
Для Каждого Узел Из Узлы Цикл
    ФоновыеЗадания.Поставить("ОбменСУзлом",
        Новый Структура("Узел", Узел.Код),
        "обмен-" + Узел.Код);       // ключ идемпотентности
КонецЦикла;

Сообщить("Ждут исполнителя: " + ФоновыеЗадания.Глубина());
```

Ключ идемпотентности удерживает работу в одном экземпляре: пока задача с этим
ключом ждёт или исполняется, повторная постановка не плодит дубль, а возвращает
ту же задачу. Продюсеру, который тикает раз в минуту по всем узлам, только это и
нужно.

Статус задачи спрашивают через `ФоновыеЗадания.Задача(Ид)`, снимают —
`ФоновыеЗадания.Отменить(Ид)`. Упавшая задача повторяется с нарастающей
отсрочкой, а когда попытки кончились — уходит в карантин и ждёт решения человека.

**Как настроить.** Пул работает сразу: без настройки — 4 исполнителя. Режим
«круглосуточный обход» задаётся блоком `queue:` в `config/app.yaml`:

```yaml
queue:
  workers: 16          # 0 — очередь выключена совсем
  lease_sec: 300       # аренда задачи: по истечении зависшую изымают
  max_attempts: 3      # попыток до карантина
  retention_days: 14   # сколько хранить выполненные задачи
```

**Как попробовать.** Заведите задание с `enabled: false` и параметром, поставьте
из кода формы несколько задач и откройте «Администрирование → Очередь фоновых
заданий»: видно глубину очереди, что исполняется прямо сейчас и что застряло в
карантине — там же кнопки «Повторить» и «Отменить».

Две вещи, которые надо знать заранее. Первая: доставка «хотя бы один раз» —
задача, чей исполнитель умер вместе с процессом, вернётся в очередь и будет
исполнена ещё раз, поэтому обработка обязана быть идемпотентной. Вторая:
настоящий параллелизм требует PostgreSQL — на SQLite соединение к базе одно, и
исполнитель тоже будет один (об этом честно скажет и лог, и монитор очереди).

## Запуск базы не удался — платформа предлагает исправить
<!-- status: testing -->
<!-- date: 2026-08-21 -->

Если база не открывается, окно запуска больше не показывает системное
сообщение с кнопкой OK. Причина выводится в диалоге отдельным блоком: текст
выделяется мышью и копируется кнопкой «Скопировать текст ошибки» — его есть чем
показать тому, кто будет разбираться.

А класс ошибок, у которого лекарство известно, платформа лечит сама. Самый
частый случай: у справочника включили уникальность кода, а старые элементы
остались без кода — база не стартует, пока их не дозаполнить. В диалоге видно,
о каком объекте и скольких записях идёт речь, и есть кнопка «Дозаполнить и
запустить»: платформа проставит пустые коды (уже заполненные не трогает) и
откроет базу. Раньше в такой ситуации предлагалось выполнить в консоли
`onebase renumber --write` — команда никуда не делась, но нажимать кнопку
из окна запуска больше не нужно уметь работать в командной строке.

**Как попробовать.** Включите у справочника `numerator.unique: true` на базе,
где часть элементов заведена до нумератора, и откройте базу из окна запуска.

## Картинка и форматированный текст — в списке типов реквизита
<!-- status: testing -->
<!-- date: 2026-08-22 -->

В Конфигураторе у реквизита объекта и у поля табличной части в списке типов
появились «картинка» и «форматированный текст». Раньше их там не было: завести
такое поле можно было только правкой YAML руками.

Заодно список перестал терять типы, которых сам не предлагает. Тип, лежащий в
конфигурации, теперь всегда присутствует в списке отдельным пунктом и остаётся
выбранным — сохранение реквизитов больше не может подменить его соседним
значением. Это касается не только картинки: так же сохраняются перечисление у
измерения регистра и булев тип у константы.

**Как попробовать.** Откройте в Конфигураторе справочник с полем-картинкой
(например Номенклатуру в конфигурации trade), добавьте любой новый реквизит и
нажмите «Сохранить типы полей» — тип поля «Фото» останется картинкой, а плитка
списка продолжит показывать фотографии.

## Ошибка запуска называет время изменения конфигурации
<!-- status: testing -->
<!-- date: 2026-08-22 -->

Если база не запускается из-за сломанного `config/app.yaml`, сообщение теперь
говорит, когда этот файл меняли в последний раз, и подсказывает команду
проверки:

```
load app config: project: parse .../config/app.yaml: yaml: line 2: did not find expected key
файл изменён 09.06.2026 20:05; проверить конфигурацию целиком: onebase check --project ...
```

Строка отвечает на вопрос, который возникает первым, когда база перестала
открываться сразу после обновления платформы: сломало ли её обновление или
давняя собственная правка. Если дата свежая — виновата последняя правка; если
файл не трогали месяцами, значит поломка была и раньше, просто прежняя сборка
её не замечала.

**Как попробовать.** Испортите отступ в `config/app.yaml` и запустите базу.

## Перечисление у измерения регистра выбирается в Конфигураторе
<!-- status: testing -->
<!-- date: 2026-08-22 -->

У измерений, ресурсов и реквизитов регистров накопления и сведений в списке
типов появилось «перечисление →», а соседний список показывает перечисления,
а не справочники. Раньше такое измерение можно было только завести кнопкой
«+ Добавить»: у уже существующего тип в списке не отображался, и сменить
перечисление можно было лишь правкой YAML руками.

Заодно Конфигуратор перестал принимать поле регистра, у которого выбран
ссылочный тип, но не выбран объект: раньше в файл уходил несуществующий тип
`reference` или `enum` без имени. Теперь сохранение отклоняется с указанием
поля — как это давно работает у реквизитов справочника и документа.

**Как попробовать.** Откройте регистр с измерением-перечислением (например
ВоронкаПродаж в конфигурации crm) и смените перечисление в списке.

## Белорусский язык интерфейса
<!-- status: testing -->
<!-- date: 2026-08-27 -->

К шестнадцати языкам интерфейса добавился семнадцатый — белорусский. В списке
языков он называется «Беларуская»; выбирается там же, где остальные: Профиль →
язык интерфейса либо язык по умолчанию для базы в её настройках. Без явного
выбора он подхватывается из `Accept-Language` браузера.

Переведены все строки, которые рисует сервер, — лаунчер, конфигуратор, формы,
настройка отчётов, диагностика, тексты ошибок: 1410 строк, ни одна не остаётся
по-английски. Учётные термины взяты белорусские, а не транслитерированные с
русского: «Правесці», «Рэшткі», «Рэгістр назапашвання», «Даведнік»,
«Справаздача», «Рахунак». Имена процедур и ключевые слова встроенного языка
(`ОбработкаПроведения`, `ВЫБРАТЬ`, `ОБЪЕДИНИТЬ`) остались как есть — их
по-прежнему набирают по-русски.

Перевод машинный, носителем языка не проверенный, поэтому лежит целиком в
`internal/i18n/locales/machine/be.json`. Правка там — правка непроверенного
перевода, а не спор с переводчиком; строка, прошедшая ревью носителя,
переносится в `internal/i18n/locales/be.json`.

**Как попробовать.** Выберите «Беларуская» в языке интерфейса и пройдитесь по
разделам: список баз, форма документа, настройка отчёта, страница диагностики.
Если формулировка звучит неестественно или искажает смысл — напишите, какая
именно надпись.

## `Документы.X.Провести(Ссылка)` — проведение документа по ссылке
<!-- status: testing -->
<!-- date: 2026-08-30 -->
<!-- issue: 1168 -->

У менеджера документов появился метод `Провести(Ссылка)` — парный к давно
существующему `ОтменитьПроведение(Ссылка)`. Раньше провести документ можно было
только объектом (`Ссылка.ПолучитьОбъект().Провести()`), а вызов у менеджера
молча возвращал `Неопределено`: документ оставался непроведённым, ошибки не
было, и это выглядело как ошибка прикладной логики.

```bsl
Реф = Документы.ПубликацияПравила.НайтиПоНомеру("ПП-001");
Документы.ПубликацияПравила.Провести(Реф);
```

Метод проводит **уже записанный** документ: запускает `ОбработкаПроведения`,
пишет движения и признак проведения. `ОбработкаЗаписи` при этом не вызывается —
как и у кнопки «Провести» в списке. Если нужно записать и провести за одну
операцию, остаётся объектный путь `Ссылка.ПолучитьОбъект().Провести()`.

Прикладная процедура `Провести` в модуле менеджера (`X.manager.os`) сохраняет
приоритет над встроенным методом: конфигурации, которые обходили пробел
собственной процедурой, продолжают работать по-прежнему.

**Как попробовать.** В обработке найдите непроведённый документ и вызовите
`Документы.<Имя>.Провести(Ссылка)`; проверьте признак проведения в списке и
движения в регистре. Помеченный на удаление документ метод не проводит —
ошибка ловится `Попыткой`.

## Плотные строки управляемой формы
<!-- status: testing -->
<!-- date: 2026-09-01 -->

У управляемой формы появился ключ `ref_card_button: false`: он убирает кнопку
🔍 «Открыть карточку» у ссылочных полей всей формы. Кнопка выбора `…` остаётся,
а формы без ключа выглядят по-прежнему. Это полезно для плотных экранов,
повторяющих знакомую операторам раскладку 1С, где отдельная кнопка у каждой
ссылки заметно расширяла колонку. Ячейки табличных частей ключ не затрагивает —
их рисует SlickGrid.

Флажок внутри горизонтальной группы теперь тоже занимает только ширину самого
флажка и подписи, а не полную колонку поля ввода, поэтому пара «поле + флажок»
остаётся в одной строке, когда места для неё достаточно.

**Как попробовать.** Добавьте ключ в заголовок объектной формы и откройте её:

```yaml
form:
  name: ФормаОбъекта
  kind: object
  entity: Обращение
  ref_card_button: false
```
