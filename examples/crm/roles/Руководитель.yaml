name: Руководитель
description: Полный доступ ко всем данным и отчётам
permissions:
  catalogs:
    Клиент: [read, write]
    КонтактноеЛицо: [read, write]
    Сотрудник: [read, write]
    Воронка: [read, write]
    СтадияСделки: [read, write]
    ТипАктивности: [read, write]
    КаналСвязи: [read, write]
  documents:
    Сделка: [read, write, post, unpost]
    Активность: [read, write, post, unpost]
    ЗадачаCRM: [read, write, post, unpost]
    Звонок: [read, write, post, unpost]
    КоммерческоеПредложение: [read, write]
    Оплата: [read, write, post, unpost]
  registers:
    ВоронкаПродаж: [read]
    Взаимодействия: [read]
    ОплатыКлиентов: [read]
    ВыполнениеЗадач: [read]
  inforegs:
    ИсторияСтадийСделки: [read]
  reports:
    ВоронкаПродаж: [run]
    СделкиПоМенеджерам: [run]
    СделкиПоИсточникам: [run]
    ДинамикаСделок: [run]
    АктивностьМенеджеров: [run]
    ВзаимодействияПоКлиентам: [run]
    КонверсияСтадий: [run]
    ПричиныПроигрыша: [run]
    СреднийЧек: [run]
    ЗагруженностьСотрудников: [run]
